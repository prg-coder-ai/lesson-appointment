<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="src.main.java.com.reservation.mapper.BookingMapper">;

    <!-- 通用结果集映射 -->
    <resultMap id="bookingResultMap" type="com.language.reservation.entity.Booking">
        <result column="booking_id" property="bookingId"/>
        <result column="student_id" property="studentId"/>
        <result column="teacher_id" property="teacherId"/>
        <result column="course_id" property="courseId"/>
        <result column="booking_time" property="bookingTime"/>
    &lt;/resultMap&gt;

    <!-- 插入预约记录 -->
    <insert id="insertBooking" parameterType="com.language.reservation.entity.Booking">
        INSERT INTO booking (
            booking_id, student_id, teacher_id, course_id, booking_time
        ) VALUES (
            #{bookingId}, #{studentId}, #{teacherId}, #{courseId}, CURRENT_TIMESTAMP()
        )
    </insert>

    <!-- 根据订单ID查询预约记录 -->
    <select id="selectBookingByOrderId" parameterType="String" resultMap="bookingResultMap">
        SELECT * FROM booking WHERE booking_id = #{orderId}
    </select>
    <!-- 根据学生ID查询预约记录列表 -->
    <select id="selectBookingsByStudentId" parameterType="String" resultMap="bookingResultMap">
        SELECT * FROM booking WHERE student_id = #{studentId}
    </select>
    <!-- 根据教师ID查询预约记录列表 -->
    <select id="selectBookingsByTeacherId" parameterType="String" resultMap="bookingResultMap">
        SELECT * FROM booking WHERE teacher_id = #{teacherId}
    </select>
    <!-- 根据课程ID查询预约记录列表 -->
    <select id="selectBookingsByCourseId" parameterType="String" resultMap="bookingResultMap">
        SELECT * FROM booking WHERE course_id = #{courseId}
    </select>
    <!-- 根据订单ID删除预约记录 -->
    <delete id="deleteBookingByOrderId" parameterType="String">
        DELETE FROM booking WHERE booking_id = #{orderId}
    </delete>
      <!--指定预约号，取消预约，对预约做标记而不删除-->
    <update id="cancelBookingByOrderId" parameterType="String">
        UPDATE booking SET status = 'canceled' WHERE booking_id = #{orderId}
    </update>


</mapper>