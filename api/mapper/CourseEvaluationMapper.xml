<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.language.reservation.mapper.CourseEvaluationMapper">

    <!-- 通用结果集映射 -->
    <resultMap id="evaluationResultMap" type="com.language.reservation.entity.CourseEvaluation">
        <result column="evaluation_id" property="evaluationId"/>
        <result column="order_id" property="orderId"/>
        <result column="student_id" property="studentId"/>
        <result column="teacher_id" property="teacherId"/>
        <result column="course_id" property="courseId"/>
        <result column="score" property="score"/>
        <result column="content" property="content"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <!-- 新补充：插入课程评价 -->
    <insert id="insertEvaluation" parameterType="com.language.reservation.entity.CourseEvaluation">
        INSERT INTO course_evaluation (
            evaluation_id, order_id, student_id, teacher_id,
            course_id, score, content, create_time
        ) VALUES (
            #{evaluationId}, #{orderId}, #{studentId}, #{teacherId},
            #{courseId}, #{score}, #{content}, CURRENT_TIMESTAMP()
        )
    </insert>

    <!-- 新补充：根据订单ID查询评价 --><!-- 新补充：根据教师ID查询评价列表 -->
<!-- 新补充：根据课程ID查询评价列表 -->
    </mapper>
