# 2026-09-10 工作日志

## 前端站点地址硬编码治理（承接 09-09 注册跳 :8080 事故）
- 约定落地：**前端不拼任何 host+port**。同源用 `location.origin`，请求用相对路径 / `window.API_BASE_URL`（默认空=同源）；
  需跨站（独立子域管理端）由页面注入 `window.ADMIN_ORIGIN` / `window.FRONTEND_ORIGIN` 覆盖，而非硬编码兜底。
- 新增 `frontend/tools/check-origin.js`：扫 4 类违规
  `HOSTNAME_PORT`(`'http://'+location.hostname+':端口'`) / `PROTO_HOSTNAME` / `LOCALHOST_URL` / `IP_URL`；
  跳过 `js/test/`、`node_modules/`、`dist/`、`tools/`；命中行加 `/* ORIGIN-LINT-DISABLE */` 可豁免（界面说明文案如"业务后台（:8081）"不误报）。
- `frontend/build.js` 在**构建前**自动以 `--strict` 调用：发现即中止构建（**不会删已有 dist**，因为 rmSync 在 lint 之后）；
  `SKIP_ORIGIN_LINT=1` 可临时跳过。package.json 加 `lint:origin`、`lint:origin:strict` 两个 script。
- tools/*.js 不会被打进 dist（buildJs 只扫 `js/`，copyAssets 跳过 `*.js`），已实测 `dist/tools` 为空。
- 清理遗留：`js/admin-template.js` 旧注释 `//const API_BASE_URL='http://localhost:8081'` 删除（唯一命中项）。
- 文档：`frontend/readme.md` 第 1/2/3/4.1/6.2/8 节同步（api.js 示例改 location.origin、新增 6.2 规则表、事故条改写）；
  腾讯云手册 5.4 前端发版加"构建前会自动做地址检查"说明。
- dist 已重建（10:28）；服务器侧仍需 `rsync -av --delete frontend/dist/ root@152.136.254.127:/var/www/frontend/` 才生效。

## tCode 跳转保持排查（10:45~）
- **本地前端连远程服务的正确姿势（重要）**：服务器已按建议收紧 8081/8090（`SERVER_ADDRESS=127.0.0.1` + 安全组只放 80/443），
  因此 `dev-frontend-remote.js` 默认端口已不可达。**改用统一入口**：
  `BOOKING_PORT=80 MSG_PORT=80 node doc-develop/dev-frontend-remote.js`（本地 :8080 → 远程 Nginx 80 → 由远程 Nginx 做 /api/{message,sse,users} 分流）。
  已写入该脚本头文件注释。
- 产出排查脚本 `doc-develop/itest_tcode_propagation.js`：Node vm 加载**真实源码**（api.js / auth.js / booking.html 内联），
  配 mock 浏览器（location 用 getter/setter 捕获 href、localStorage、document stub、BroadcastChannel、addEventListener），
  真实执行跳转函数后断言目标 URL 是否含同一 tCode。**当前 PASS 18 / FAIL 9**。
- 9 处实证丢失点（详见 `doc-develop/tCode跳转参数保持-问题清单.md`）：
  P0：api.js:209/212 `?tCode=undefined`（user.tenantCode 缺失，源于 auth.js saveCurrentUserSession 不存 tenantCode）、
      api.js:217/225 写死 `?tCode=default`、api.js:584 goBack、booking.html:59/66/73/75 四个分支；
  P1：auth.js:60 handleLogout、api.js:156 getToken 跳不存在的 '/login'、admin/auditLog/logBrowser/platform_admin/student/teacher 内联回登录页。
- 已正确的不要改：api.js:92 InitUserInfo、622-630 forceEntryLogin、index.html:511 注册跳转、654-752 tenantCode 表单规则。
- 统修方案已写入清单第 3 节（新增 `resolveTenantCode()` + `pageUrl()` helper），**待用户确认后再动手改代码**。

## tCode 保持修复（11:00~，承接上条排查）
- **helper 落地**（`js/public/api.js` 末尾 helper 区）：`resolveTenantCode(user)` + `pageUrl(file, extra, absolute, user)`，导出到 window。
  优先级：platform_admin 恒 platform > URL tCode > user.tenantCode > localStorage.currentUser.tenantCode > ''。
  **关键设计：兜底不回填 'default'** —— 登录页一见到 tCode 就隐藏/锁定租户输入框，回填会把普通入口写死成 default 入口（曾导致 A2 回归失败）。
  `pageUrl` 里 tCode 最后 set，防 extra 重名覆盖。
- 根因修复：`auth.js: saveCurrentUserSession` 补存 tenantCode（顺序 user.tenantCode → window.getUrlParam('tCode') → 旧 currentUser），
  这是 `?tCode=undefined` 的真正来源（platform_admin.html:205 走 redirectToUserPage(autoLoginCheck())）。
- 改全的跳转点：api.js（InitUserInfo/getToken/redirectToUserPage 四分支+兜底/autoLoginCheck1/goBack/forceEntryLogin）、
  auth.js handleLogout、booking.html 五分支（新增 toPage 薄封装）、platform_admin/admin/auditLog/logBrowser/student/teacher/index 内联、
  utility_request.js 与 messages-inbox.js 的 401 兜底。所有 HTML 内联统一用 `(typeof window.pageUrl==='function') ? pageUrl(..) : './x'` 降级写法。
- 回归：`node doc-develop/itest_tcode_propagation.js` → **PASS 32 / FAIL 0**（新增 F 段 helper 行为 5 项）。lint + build 均通过，dist 已重建。
- 未处理（待确认）：`js/admin-user.js:441` 跳 `teacherInfo.html`（该 HTML 不存在，死链）；`api.js autoLoginCheck1` 死码段未删。
- 上线提醒：dist 改动需 `rsync -av --delete frontend/dist/ root@152.136.254.127:/var/www/frontend/` 才生效。

## tCode 修复收尾（11:17~11:30）
- 上一轮被打断的提交已落地：`f2f835f` tCode 全链路保持。回归复核 **PASS 32 / FAIL 0**；
  dist 已重建（11:17），含 pageUrl helper（api.js）、tenantCode 落库（auth.js）、booking.html 的 toPage 五个分支。
- **dist 尚未 rsync 到服务器**：`rsync -av --delete frontend/dist/ root@152.136.254.127:/var/www/frontend/`。
- dev-frontend-remote.js **默认端口由 8081/8090 改为 80**（提交 `00cc8b3`）：
  服务器已收紧后端端口，8081/8090 外网不可达；现在裸跑 `node dev-frontend-remote.js` 即可，
  由远程 Nginx 做 /api/v1/{message,sse,users/} 分流，密钥域仍一致。旧部署用 BOOKING_PORT=8081 MSG_PORT=8090 覆盖。
- 本机验证（默认 80，源码模式）：首页/booking.html/booking info/message info 全 200。
- 坑记：`&` 后台起的 node 代理会随工具调用结束被回收，`curl` 得到 502 是"端口已无监听 + 环境代理"的假象；
  需常驻时用 run_in_background 启动，且本地探测加 `--noproxy '*'`。

## <title> 术语化（11:50~，标签页标题混乱修复）
- 根因两条：① `applyTerms(root=document.body)` 只扫 body，`<title>` 在 head，**data-term 永不生效**；
  ② `<title>` 是 RCDATA，admin.html 曾写 `<span data-term="lessonSystem">`，标签被当**纯文本**显示 → 标题出现尖括号乱码。
- 新增 `frontend/js/public/documentTitle.js`（UTF-8 新文件），约定：
  `<title data-term-title="{lessonSystem} - 管理端">语言教学预约系统 - 管理端</title>`；
  模板 `{key}` 替换为当前词；无 data-term-title 的旧页面走兜底（替换锚点词"语言教学预约系统"）。
  优先级 **租户品牌 > 服务端合并词 > 本地行业词 > 锚点词**；未知 key 保留 `{key}`（绝不出现 undefined）。
  同时同步页内 `#brand-title`：跳过有 data-term（applyTerms 负责）与带 `data-tenant-brand` 标记（租户品牌）的元素。
- 8 个页面 title 全改 + 引入脚本（在 termsFunction.js 之后）。
- **termsFunction.js 是编码混杂文件（主体 UTF-8，含非法字节），不可用 Edit/文本方式改写**，
  只能按 **bytes 精确替换**（python 读写 rb/wb，中文用 .encode('utf-8')）。改了 4 处：
  DOMContentLoaded、loadTermMapFromServer 成功后、applyTenantTitle（改调 applyDocumentTitle({brand}) 并打 data-tenant-brand 标记）、switchIndustry。
- 回归脚本 `doc-develop/itest_document_title.js` → **PASS 23 / FAIL 0**（含 H 段：vm 按页面顺序加载
  terms.js→termsFunction.js→documentTitle.js 并触发 DOMContentLoaded 的真实链路验证）。
  H 段 mock 需提供 `URLSearchParams`（getTenantCodeParam 依赖），否则报 undefined。
- dist 已重建；仍需 rsync 上传服务器才生效。

## 枚举字段行业词显示分析（12:20，仅分析未改码）
- 现象：legal 行业"课程模板"下拉显示正常（婚姻/劳动/刑事/行政），但**列表显示裸 code（english/English）**；课程列表同样。
- 根因三层：①列表直接渲染 DB 原始 code（admin-template.js:401、admin-course.js:390/73/105、datamaintain_delete.js:100）；
  ②全项目无 code→显示词转换函数，显示词只存在于 <option> 的 DOM 文本（data-term 换词），JS 拼串拿不到；
  ③**classType 用序号型 key（classType1..4），而 classForm/classLevel 用 code 后缀型（classForm1p1/classLevelB1）**，
    所以语言类型套用不了 `data-term="classForm${code}"` 土办法（admin-course.js:405 正是这么写的）。
- **附带 Bug（比列表更严重）**：code↔序号映射 5 处写了 3 套且互相矛盾——
  正确：french→1 english→2 chinese→3 spanish→4（admin-course/schedule/student-bookingCards 一致）；
  错：admin-template.js 筛选（english→1 french→2）、新增表单（english→1 french→2 spanish→3 chinese→4）。
  → 同一条数据在课程页与模板页显示的行业词不同，且模板页新增与筛选对同一 code 指向不同词。
- termsFunction.js:23 的 getOptions() 为此而生（key=tagKey+'.'+code），但 sys_term 里**无点号 key**（0 条）→ 零使用。
- 方案：推荐 A（唯一权威 COURSE_TYPE_OPTIONS + courseTypeText() 反查，沿用 classType1..4，纯前端、顺带修错位）；
  B=词条改点号 key（需后端加词）；C=模板串拼序号（脆，不解决错位）。
- 待核实：库里 language_type 是否存了"English"/中文等显示名（脏数据）；线上 sys_term 是否已有 classType.*。
- 产出：doc-develop/枚举字段行业词显示-分析与方案.md

## 方案A落地：枚举字段（语言类型）行业词显示 + 修正 admin-template 映射颠倒（13:00~13:15）
- 新增 `frontend/js/public/enumTerms.js`（唯一权威 code↔词条映射，导出 6 个 helper）：
  `COURSE_TYPE_OPTIONS` / `courseTypeText` / `courseTypeCellHtml` / `courseTypeOptionsHtml`
  / `enumTermText(prefix,code)` / `enumTermCellHtml(prefix,code)`。
  权威映射：**french→classType1 english→classType2 chinese→classType3 spanish→classType4**（对齐 TERM_DICT.education）。
  兜底链：服务端合并词 > 当前行业词 > 锚点词；脏数据归一化('English'/'英语'/别名)；未知 code 原样输出。
- 改动：terms.js TERM_KEYS 的 classType1..4 anchor 订正为 法语/英语/汉语/西语（原 英语/法语/韩语/西语，
  与 TERM_DICT.education 不一致 → restoreAnchorTerms 会还原错）；
  admin-template.js（表单下拉/筛选下拉 → courseTypeOptionsHtml；列表三列 → courseTypeCellHtml/enumTermCellHtml，**english↔french 颠倒已修**）；
  admin-course.js（筛选下拉 + 选模板/选教师下拉文本 + 列表模板信息列）；admin-schedule.js、student-bookingCards.js（筛选下拉）；
  js/public/datamaintain_delete.js（删除确认文案）；admin-dataMaintainPage.js（列定义新增 `fmt` 钩子 + 渲染支持列级格式化）；
  admin.html / student.html / teacher.html 引入 enumTerms.js（在 termsFunction.js 之后）。
- **坑1（重要）**：`terms.js` 里 `const TERM_DICT` 是**词法声明，不挂 window**（浏览器与 Node vm 一致），
  模块内须用裸标识符 + `typeof TERM_DICT !== 'undefined'` 探测，写 `window.TERM_DICT` 取不到。
- **坑2**：服务端词表可能含空串，`Object.assign` 合并后把行业词覆盖成 '' → 若直接掉锚点词，legal 下会显示 education 的"英语"。
  故兜底链单独加"当前行业词"一层。
- 回归脚本 `doc-develop/itest_enum_terms.js` → **PASS 55 / FAIL 0**（含 G 段静态防回退：禁止硬编码 code↔classTypeN 配对、
  6 个消费方必须引用 helper、3 个页面必须引入脚本；H 段 TERM_KEYS.anchor 与 TERM_DICT.education 一致性）。
  其余：tcode 32/32、title 23/23、origin lint 通过、dist 已重建（13:04，helper 名跨文件保留）。
- 遗留：本地库 language_type 是否存显示名（脏数据）仍未核实（直连 MySQL 被审批拦）；上线需 rsync dist。

## 13:10-13:55 教师详情页缺失（点教师姓名 → 跳回登录页）根因与修复

**现象**：管理端【教师管理】列表点教师姓名 → 跳 `/teacherInfo.html?userId=xxx`，无 tCode，落地显示登录界面。

**根因（两个，后者才是主因）**
1. `frontend/js/admin-user.js` 的 `teacherInfoBoard()` 裸拼 URL，未走 `pageUrl()`，故不带 tCode → 即便页面在也会被 `guardEntryPage` 踢回登录。
2. **teacherInfo.html 整个页面不存在**：随提交 `00ace80`（2026-09-07 前后端分离）连同 `teacherInfo{,-common,-edit,-publish}.js` 被删除、**未迁入 frontend**；配套公开名片页 `teacherPublishedProfile.html` 同样漏迁（共 6 文件 ~2400 行）。Nginx `try_files $uri $uri/ /index.html` 把 404 静默兜底成登录首页 → 表象完全伪装成"登录态失效"。
   - 查证手段：`find` 全仓 + `git log --diff-filter=D --name-only`；历史文件用 `git show 00ace80^:api/src/main/resources/static/<file>` 取回。

**修复（commit 44fc6b0，未 push）**
- admin-user.js：`teacherInfoBoard` 改走 `pageUrl()`，`userId` 加 `escAttr`。
- 从 `00ace80^` 取回 6 个文件迁到 `frontend/`，按新体系适配：
  - 依赖顺序 axios→terms→termsFunction→**documentTitle/enumTerms**→utility_request→auth→**api**→业务 JS。**api.js 必须早于业务 JS**（提供 escapeHtml/escapeAttr/pageUrl/goBack/getCurrentUserInfo）。
  - 接口**保持裸路径** `/teacher/professional/...`，由 `utility_request.js` 的 `normalizeUrl` 自动补 `/api/v1`（不要手拼 API_BASE_URL，其默认为空串）。
  - 独立页健壮化：`guardEntryPage('admin')` + `syncIndustryFromTenant` + 未登录回跳 `pageUrl('index.html')`；`<title>` 改 `data-term-title`（原 `<title>` 内嵌 `<span>` 会当纯文本渲染）。
  - 新增 `publicProfileUrl()`（teacherInfo-publish.js）：公开名片页**刻意不带 tCode**（对外分享链接）。公开页不引 api.js，`window.API_BASE_URL` 为空 → 必须自拼 `/api/v1` 前缀，否则 Nginx 当静态资源返回 404。
  - `formAvaliableTimesDiv(list, {withOptionCheckbox})`：查看模式带"优先推荐"复选框，**发布快照必须是纯文本**。
  - 顺手修旧 bug：原 `loadTeacherInfo()` 里 `if (!result)` 分支又读 `(result && result.message)` 恒空。
- **新增 `frontend/js/public/missingPageGuard.js`**（由 index.html 在 `<body>` 开头引入）：识别"当前 URL 其实不是首页"的 try_files 兜底请求 → 顶部红色告警条 + 返回首页/复制报错信息。**用途：让同类漏迁立刻暴露，不再伪装成登录异常。**

**验收**：`check-origin --strict` 通过；`node build.js` 通过（5 新文件进 dist，dist 的 6 个入口齐全）；5 个新 js `node --check` OK；混淆后跨文件全局名（teacherInfoBoard/publicProfileUrl/formAvaliableTimesDiv/escapeAttr）均保留。
**上线动作**：`rsync -av --delete frontend/dist/ root@152.136.254.127:/var/www/frontend/`（dist 尚未同步）。

### 13:55 现象仍复现的原因定位（同一问题被二次反馈）
用 curl 直接核验远端，证明**修复代码从未上线**，而非修复无效：
- `curl http://152.136.254.127/teacherInfo.html` → HTTP 200，body 31184 字节，`<title>` = 「语言教学预约系统 - 统一登录入口」，即**仍是 index.html 被 try_files 兜底**。
- `curl http://152.136.254.127/` → 同为 31184 字节，`grep -c missingPageGuard` = 0 → 服务器 `/var/www/frontend` 是**旧版 dist**（本地新 dist/index.html = 34953 字节且含 missingPageGuard）。
- 结论：`frontend/dist` 构建完成后未 rsync 上线，任何静态 GET 都命中旧产物。**判定"前端问题是否仍存在"的第一步应先比对线上 index.html 与本地 dist/index.html 的大小/特征串**，别急着改代码。
- 免上线快速验证法：`cd doc-develop && DIST=1 node dev-frontend-remote.js`（FRONTEND_ROOT 自动指向 ../frontend/dist），浏览器开 localhost:8080 直连远程后端，即可在未上线状态下验收 dist。

### 14:05 booking.html 多角色路由体检（用户要求顺便查）+ 本地 dist 验收环境
**体检结论：无漏页风险。** 五条分支目标页（index/student/admin/teacher/platform_admin）源码与 dist 均在；深链参数契约逐页对齐：
- student.html 解 scdid+tid+sid → booking 传这三个 ✅
- admin.html 只解 scdid+sid（无 tid） → booking admin 分支传 scdid+sid ✅
- teacher.html / platform_admin.html / index.html 均不解析深链（`pendingDeepLink` 计数为 0）→ booking 不带参数，一致 ✅
（注意：grep teacher.html 的 "sid" 会命中 `aside` 标签，别被误导。）

**发现并修掉的真缺陷（commit 9b921e5）** —— booking.html 的 `toPage()` 降级路径：
1. `adminOrigin` 用 `var` 声明在 `toPage` **之后**（第 59 行），var 提升使 fallback 里读到 undefined → platform_admin 分支拼成 `undefined/platform_admin.html`。→ 声明提前到 toPage 之前。
2. fallback 里 `'?' + String(extra)` 遇到对象参数会输出 `?[object Object]`。→ 改为按 key 序列化、跳过空值，空则不带查询串；absolute 且无 ADMIN_ORIGIN 时退化为站点根 `/`。
⚠️ 教训模式：**函数里引用"稍后才 var 声明"的变量**这类缺陷，静态 grep 看不出来，必须在 review 时按"声明顺序 + 降级分支"逐条走查。

**本地验收环境已就绪**：8080 上原有源码模式代理（PID 14424，未动）；新起 8088 为 `DIST=1` dist 模式（`cd doc-develop && DIST=1 DEV_PORT=8088 node dev-frontend-remote.js`）。实测 `curl localhost:8088/teacherInfo.html` = 29847 字节真页面（对比线上 152.136.254.127 同路径返回 31184 字节的旧 index.html，坐实 dist 从未上线）。
**上线比对口径**：本地新 dist/index.html = 34953 字节且含 missingPageGuard；线上 = 31184 字节、missingPageGuard 计数 0。

### 14:20-14:35 teacherInfo「职业信息不存在」→ 进编辑界面（功能恢复）
**现象**：teacherInfo 页面能加载后，若该教师尚无职业信息记录，停在「加载失败：职业信息不存在」，看不到编辑/发布等功能。
**根因（三重证据）**：
- 后端 `TeacherProfessionalService.queryTeacherProfessionalInfo`：tp==null → `Result.fail(404,"职业信息不存在")`
- `utility_request.js` 响应拦截器：非 200 业务码统一 `Promise.reject(res)`（res 带 code/message），且 `customErrorMsg!==false` 时才弹通用错误
- 原前端 `loadTeacherInfo` 把"不存在"当异常，catch 里直接 `renderError`，没走新增模式（try 内 `if(!data.professional)` 分支是死代码，因为后端不存在走 fail 不会到 success 分支）
**修复（commit c645dab，未 push）**：`frontend/js/teacherInfo.js`
- request 调 `customErrorMsg:false`（不弹通用错误）
- catch 识别 `code===404 || message含"职业信息不存在"` → 调新增 `enterAddMode(teacherId)`，与 try 内 add 分支同源
- `enterAddMode`：buildEmptyForm→fillEditForm(空,isAdd)→switchSection('edit')→showActionButtons('edit')
**验证（vm 真实加载，mock 后端抛 {code:404}）**：setPageTitle 含「新增」、无 renderError、`section-edit` 与 `btns-edit` 的 style.display 均='' → 已进入编辑界面，PASS。
**确认**：teacherInfo.html 三模式容器+按钮齐全（btns-view 含 编辑/发布·转发/删除；enterPublishMode 实现完整，依赖 `/teacher/published/list`、`/teacher/published/get` 接口已存在）；建档成功回 view 即可见发布按钮。=> 编辑/发布功能非缺失，只是被 error 页挡住入口。

### 14:33 teacherInfo 详情页姓名/手机/邮箱显示成编码串（AES 密文）→ 后端漏解密
**现象**：teacherInfo 详情页 view 模式，姓名/手机号/邮箱显示为 AES 密文（编码字符串）而非可读文本。
**根因（完整证据链）**：
- 前端 fillView 直接 setText('view-name'/'view-phone'/'view-email', data.xxx)（teacherInfo-common.js:130-133），字段来自 normalizeDetail → 后端 queryTeacherProfessionalInfo 返回。
- User 实体 name/phone/email 无 MyBatis 自动加解密注解（普通 private String，grep 仅 orgName 有 @TableField(exist=false)）。
- 入库加密：UserService 写操作调 cryptoUtil.encryptWithIndex（UserService.java:73-75）。
- 查询解密：UserService.decryptUserFields 对 name/phone/email 调 cryptoUtil.decrypt（79-84）；/user/page 走此路径 → 教师管理列表显示明文（admin-user.js:251/253/254 用 tea.name/maskPhone/maskEmail）。
- **漏点**：TeacherProfessionalService.queryTeacherProfessionalInfo（167-173）直接 teacher.getName() 塞 VO，未 decrypt → 返回密文。
- CryptoUtil.decrypt 容错完善（空/非加密格式/解密失败均降级返回原值，不抛）。
- 前端 js/public 无任何 decrypt 工具 → 解密责任只能在后端。
**修复（commit ce22948，未 push）**：TeacherProfessionalService 注入 CryptoUtil，组装 VO 前 decrypt name/phone/email（account 按系统惯例不解密，与 decryptUserFields 一致）。
**验证**：saas-api-build-smoke 直接 launcher 编译+package 均 BUILD SUCCESS；api/target/booking_api-2.0.0.jar 已重建（41.6MB）。需部署新 jar + 重启 booking 服务生效。
**教训（长期）**：凡从 User 冗余敏感字段到 VO 的接口必须走 decryptUserFields 等价解密；新增此类接口极易漏（本次即漏）。User.name/phone/email = 加密存储、service 层手动解密。

## 15:00 课程列表「待审核」行新增「确认」按钮（admin-course.js）
- 需求：课程列表表格每行，status=pending（待审核）时，操作列追加「确认」按钮 → 置该课程 status=active。
- 改动：frontend/js/admin-course.js `renderCourseTable` 操作列 `<td>` 内新增
  `${Course.status === "pending" ? \`<button class="btn btn-success" onclick="changeCourseStatus('${Course.courseId}', 'active')">确认</button>\` :'' }`
- 复用既有 `changeCourseStatus(id,'active')` → POST /course/updateStatus（与「发布」按钮同一路径，无状态流转限制，管理员有权限）→ pending→active 闭环成立。
- 提交 50f574c（branch frontend_opt，未 push）。
- ★ 附带发现的潜在缺陷（未改，待确认）：admin-course.js 状态筛选下拉框（course-status-select）选项与真实状态模型不一致——
  实际渲染 pending→待审核 / active→正常 / inactive→待启用 / frozen→已删除，
  但下拉写的是 active=有效 / pending=冻结 / delete=删除（值用 "delete" 而非 "frozen"，且把 pending 标"冻结"与行内"待审核"矛盾），
  → 管理员无法通过筛选稳定定位"待审核"课程。建议单独修下拉选项。

## 15:10 课程状态收敛为三态 + 完善操作按钮（承接上条，用户定调）
- 用户确认采用筛选下拉框的模型为权威：**active=有效 / pending=冻结 / delete=删除**（"待审核"即"冻结"，同一状态）。
- 改动（admin-course.js，提交 1f2f68a，未 push）：
  - 状态渲染四态→三态：active→有效 / pending→冻结 / delete→删除（旧 待审核/正常/待启用/已删除 全移除；未知值兜底显示原串）。
  - 操作列新生命周期：active→【冻结】(→pending)；pending→【启用】(→active)+【删除】(→delete)；修改恒显示。
  - 删除函数 rename `deleteCourseByFrozen`→`deleteCourse`，落库状态 frozen→delete（排期仍按自身枚举 frozen 冻结）。
  - 旧 发布/撤回/确认(inactive) 按钮移除；`50f574c` 的"确认"按钮在新模型即"启用"。
  - 筛选下拉框本就为三态，无需改动；沿用 changeCourseStatus → POST /course/updateStatus（后端无状态流转限制）。
- 注意：`Course` 实体默认 status="pending" → 新建课程即"冻结"，管理员点"启用"激活，生命周期自洽。
- 范围：仅 admin-course.js（课程列表页）。teacher/student 端为浏览卡片，未涉及课程状态按钮，未动。

## 15:16 课程三态改动本地测试（vm 真实源码渲染 + 语法冒烟）
- 用户要求"完成后进行本地测试"。
- **环境注意**：前端已与 booking jar 解耦（静态页现由 Nginx 伺服 `frontend/dist`，`target/classes/static` 已无前端 JS），
  故 browserless-frontend-itest 技能原本"读 target/classes/static/js"的约定对本项目**已不适用** → 直接读 `frontend/js` 源文件进 vm 测试（更贴近将上线代码）。
- 探测：本地 booking :8081 返回 502（未稳定起），:8080 与远程 152 返回 200；远程缺系统 JWT secret、本地 8081 未起 → **本会话未跑真·HTTP 状态流转**（POST /course/updateStatus），仅测前端渲染+按钮接线逻辑。
- 测试脚本 `C:/Temp/course_status_test.js`：Node 22 vm 加载真实 `admin-course.js`，注入最小 DOM/依赖垫片
  （document.getElementById/write/addEventListener、Pagination、templateList/teacherList、courseTypeText/enumTermText 桩、confirm=()=>true），
  调 `renderCourseTable([mockCourse])` 逐态捕获 tbody.innerHTML 并断言。
  - 必补垫片：`document.write`（admin-course.js 用 document.write 引入脚本，不补即 `document.write is not a function` 加载失败）。
- 结果 **15 PASS / 0 FAIL**：
  - 三态徽标：active→有效 / pending→冻结(待审核同态) / delete→删除 ✅
  - 按钮矩阵：active→【修改】【冻结】；pending→【修改】【启用】【删除】；delete→仅【修改】 ✅
  - 无残留旧徽标(待审核/正常/待启用/已删除)与旧按钮(发布/撤回/确认) ✅
  - 空列表渲染"暂无数据" ✅
- 语法冒烟：`node --check` 通过 admin-course.js + 上轮 5 个术语文件（共 6 文件全 OK）；grep 确认无残留 `deleteCourseByFrozen` 引用。
- 边界说明（如实告知）：实际点击"冻结/启用/删除"触发的 `changeCourseStatus`/`deleteCourse` → 后端流转，需本地 8081（同 882 源密钥）或远程 80 入口才能量测；本会话未执行，建议用户经 `dev-frontend-remote.js`（默认 80）自测点击闭环。

## 15:20-15:40 发送对话框「用户名称（账号）:id」前缀 + 分类下拉 + 接收人列表解密（commit 9579b19，未 push）
- 需求（用户 #11-#14）：①发送对话框指定用户框前加"用户名称（用户账号）:"前缀；②该账号也是密文需解码；③分类编码输入改下拉+手输 combobox；④"按范围"接收人列表显示一长串未解密串 → 同 teacherInfo 经验须后端解密。
- **后端根因修复**：`UserService.resolveMessageRecipients`（~803-807）返回前对每行 `decryptUserFields(u)` + `u.setAccount(cryptoUtil.decrypt(u.getAccount()))`。account 库内明文→decrypt 容错降级返回原值；name 解密成可读。影响所有消费 `/api/v1/user/message-recipients` 的界面（含"按范围"列表）。
- **前端**：
  - `messages-inbox.js` `openComposeToUser(userId,role,name,account)` 第四参；`buildComposeHtml` 单用户预设拼 `名称（账号）:id`；`parseIds` 发送时 `lastIndexOf(':')` 剥前缀只留 id（保留多 id/粘贴安全）。
  - 分类编码 `<input list="msg-category-list">` + `<datalist>`；新增 `loadCategoryOptions(root)` 调 `GET /api/v1/message-categories/tree` 填充（失败静默降级保留手输）。
  - `platform-admin-user.js:132` / `admin-user.js:270,32-33` 发消息按钮透传 name+account 四参（admin-user 用 `data-compose-account` 属性）。
- 验证：`saas-api-build-smoke` 直接 launcher 编译 BUILD SUCCESS（仅 UserSessionMapper.java 既有 UTF-8 警告无关）；3 个前端 js `node --check` 通过；grep 无残留三参调用。
- ★ 上线提醒（与 teacherInfo 同一模式）：**修复代码已落地但运行实例未重建** → "按范围"列表仍显密文仅因 jar/dist 从未重建+重启/rsync。需 ①重建 api jar 并重启 booking（8081 或 8083 冒烟）②`rsync -av --delete frontend/dist/ root@152.136.254.127:/var/www/frontend/` 才生效。

## 21:10 电子邮箱脱敏改造（commit 22e055b，未 push）
- 需求：邮箱脱敏改为"地址中间4字符用*代替"（居中）；长度≤6时仅替换中间最多4个、前后各保留1个字符。并把脱敏补到 platform-admin 用户管理 + 租户管理。
- **单一来源**：`frontend/js/public/api.js` 新增全局 `maskEmail`(新规则) + `maskPhone`(沿用旧行为：11位前3后4、≤4全盘星)。api.js 是经典全局脚本（无 IIFE），顶层 function 即 window 全局，与 escapeHtml 同机制。
- `admin-user.js` 删除本地重复 maskPhone/maskEmail（旧实现只遮本地部分），改引用全局；租户(admin)端用户列表 email/phone 列继续脱敏。
- `platform-admin-user.js:126-127` 用户管理列表 phone/email 列改 `maskPhone`/`maskEmail`（此前是裸 escapeHtml 未脱敏）。
- `platform-admin-tenant.js:92` 租户管理列表 联系电话 列改 `maskPhone`（租户无 email 字段，仅 phone 为 PII）。联系人(姓名)按既有约定不遮。
- 测试：`C:/Temp/mask_test.js` 从 api.js 抽取真实函数 vm 执行，**PASS 17 / FAIL 0**（覆盖 L=1/2/3/4/5/6/10/20、11位手机、空/null、长度不变）。4 文件 `node --check` 通过。
- 范围外（待确认）：platform-admin-dashboard.js 也展示 t.phone（escapeHtml 未遮），本次未动，按需可一并改 maskPhone。

## maskEmail 规则细化（用户要求提高辨识度）
- 改 `api.js` maskEmail：仅遮蔽 @ 之前本地部分，保留域名与前后缀。
- 规则：本地 ≤1 → *；=2 → 首字符+1*；3~4 → 首字符+其余*；≥5 → 前2+后2+中间最多4*；无@原样返回。
- 示例：a@b.cn→*@b.cn；zhangsan@example.com→zh****an@example.com；ab@c→a*@c。
- 测试 `C:/Temp/mask_email_test.js` 抽取真实函数 vm 执行 **PASS 14 / FAIL 0**；commit `15ee1ed`（仅 commit 不 push）。
- 仍受"运行实例需重建 dist 才生效"约束（静态页 nginx 伺服 frontend/dist）。

## 重建 api jar 并本地重启 booking（用户要求，服务器自行上传）
- Maven launcher 直启 `package -DskipTests`：`BUILD SUCCESS`，产出 `api/target/booking_api-2.0.1.jar`（21:35，含 UserService 解密改动）。
- 启动：先 `unset SERVER__PORT SERVER_PORT`（避沙箱 SERVER__PORT=55058 宽松绑定覆盖 server.port 撞 IDE），`java -jar ... --server.port=8081`，后台任务 `zy39lg`/PID 23248。
- 启动 17.37s，Hikari 连 MySQL lesson_appointment 成功；`GET /`→200；jar 内 UserService.class 含 decryptUserFields 确认打入。
- 未 rsync（用户自行上传服务器）。前端改动不在此 jar，需另 `node build.js` 重建 dist。

## 仪表盘补脱敏（用户要求"同样处理"）
- platform-admin-dashboard.js:143『即将到期租户』联系电话 t.phone 由 escapeHtml 改 maskPhone（与用户/租户管理一致）。
- contact(联系人姓名)/orgName(机构名)按约定不遮；用量表仅 orgName/tenantCode/quotaLevel 无 PII。commit `122a3a6`。

## 消息发送卡片"嵌在页面"修复（用户报"又出现"）
- 根因：openComposeMessage 自身未调 ensureStyle()，样式仅在 renderMessagesPage/initMessageBadge 注入；从用户/租户管理『发消息』直达（未先开收件箱）时 .msg-compose-card 无 position:fixed → 退化普通块元素落 body 末尾=嵌页面。
- 修复：openComposeMessage 入口补 ensureStyle()（幂等，守卫 msg-inbox-style）。commit `eea7c03`。

## 分类编码下拉"只能选一次"（用户报不友好）
- 根因：原生 `<datalist>` 在 Chromium 下一旦 input 值等于某 option，再次聚焦不再弹出建议列表（浏览器缺陷，非代码 bug）。
- 修复：改 JS 可控组合框（input + ul.combo-list）。聚焦/点击恒 showAll 全部预设可重复选；输入按 code/name 过滤；点箭头 mousedown+preventDefault 切换显隐；点项 mousedown 填值关列表；blur 延迟 150ms 收起。setupCategoryCombo 在 openComposeMessage 无条件装配（分类树拉取失败仍可自由输入）。commit `d41dfd7`。

## 前端构建元信息（选项 A 落地）
- 需求：获取前端打包时间 / 打包时 git 信息。结论：API 已有 buildTime（/api/v1/system/info），前端此前无。
- 实现：frontend/build.js 构建末尾生成 dist/build-info.json（buildTime + gitCommit/gitBranch/gitDirty，取自 git rev-parse/status），并烤进 index.html 的 window.__BUILD_INFO__；platform-admin-backend-info.js 新增"前端构建信息"块（优先读 window.__BUILD_INFO__，缺失回退 fetch build-info.json）。commit `e345f06`。真实构建验证通过（gitDirty 反映构建时未提交改动）。
- 注：dist 被 gitignore，前端改动需 `node build.js` 重建 dist 才生效；本次已实际跑过一次真实构建。

## 后台信息页改"程序信息"+前端第三行 / 消息中心两 bug 修复（commit ca15152）
- 用户两诉求：①数据维护"后台信息"Tab 的"后台程序信息"标题改为"程序信息"，并把前端构建信息作为第三行；②消息中心点击标题不显内容、标记已读后角标数字不变。
- Part1 `admin-dataMaintainPage.js`：标题"后台程序信息"→"程序信息"；新增 `fetchFrontendBuildInfo()`（读 window.__BUILD_INFO__ 回退 fetch build-info.json），在 renderBackendBriefInfo 表格追加第三行"前端(静态包)"，展示 dist/buildTime + gitCommit@branch（工作区脏标"脏"）。
- Part2 `messages-inbox.js`：
  - 根因①`openDetail` 原 `catch(e){return;}` 静默吞错 → 详情拉取失败 modal 永不渲染=点标题无内容。改为渲染"加载失败"modal（含错误信息+关闭钮）。
  - 根因②`markRead` 原 `catch(e){return Promise.reject(e);}` 静默吞错 → 失败即跳过 loadMessages/refreshBadge=角标不变。新增 `bumpBadge(delta)` 乐观更新角标（已读-1/未读+1），失败 toast 提示，成功才 loadMessages+refreshBadge。
  - 后端 UserInboxController `/api/v1/users/{userId}/messages/{messageId}` detail 字段(title/content/senderType/categoryName/sendTime/isRead)与前端期望一致，已读核对路径无误——纯前端静默 catch 问题。
- 验证：`node --check` 两文件 OK；browserless 集成测试 C:/Temp/msg_inbox_test.js（vm+真实源+mock axios）**3 PASS/0 FAIL**（bindRowEvents 绑标题点击、点标题渲染正文、标记已读乐观角标-1 即便 unread-count 接口失败）。
- 仍受"运行实例需重建 dist 才生效"约束：前端改动需 `node build.js` 重建 dist + rsync 到服务器（用户自理）。仅 git commit，不 push。

## build-info 404 修复（commit f517808）
- 现象：admin-dataMaintainPage.js:378 `GET http://localhost:8080/build-info.json 404`。
- 根因：`fetchFrontendBuildInfo` 优先读 `window.__BUILD_INFO__`，缺失才 `fetch('build-info.json')`；而 `build.js` 的 `injectBuildInfoGlobal` 原硬编码只注入 `index.html`，数据维护页是独立入口 `admin.html`（非 index SPA），无 `window.__BUILD_INFO__` → 回退 fetch → dist 未含 build-info.json 即 404。
- 修复：`injectBuildInfoGlobal` 改为遍历 dist 全部 `*.html` 入口页注入 `window.__BUILD_INFO__`（已注入则跳过防重复）。已实际跑 `node build.js` 重建 dist：build-info.json 生成、admin.html/index.html 等均含 `__BUILD_INFO__` → fetch 不再发出，404 消除。
- 注：dist 被 gitignore，本次本地 dist 已重建好，用户 rsync `frontend/dist` 即生效；build-info gitDirty=true 因构建时 build.js 尚处未提交态（提交后下次构建转 false）。仅 git commit，不 push。

## 前端构建信息改为只读本地、不依赖后端接口（commit ec57bca）
- 用户反馈：admin.html「数据总览/程序信息」加载超时；明确要求"build-info.json 存在就读文件数据，不要请求后台数据"。
- 根因：`fetchFrontendBuildInfo` 的回退 `fetch('build-info.json')` 无超时——若前端站点的未知路径被转发到（正在变慢的）后端，该 fetch 永久挂起整个 renderBackendBriefInfo（后端行有 axios 15s 超时，唯独此 fetch 没有）；且 fb 在后端 Promise.all 之后串行 await，前端行被后端请求阻塞。
- 修复（admin-dataMaintainPage.js + platform-admin-backend-info.js 同款）：
  - 优先 `window.__BUILD_INFO__`（构建时烤入=build-info.json 数据，零请求）；回退 fetch 同源静态文件加 **AbortController 5s 限时**，永不挂起。
  - `renderBackendBriefInfo` 改为 `fbPromise` 与后端 Promise.all **并行**，前端行不等后端接口。
  - 后端行维持 15s 超时各自显示错误，不影响前端行。
- 已重建 dist（build-info gitCommit=ec57bca, gitDirty=false），工作树干净，仅 commit 不 push。
