# 2026-09-01 工作记录

## 10:25 分析 tenantId 与 tenantCode 用法区别
- 输出 租户标识分析-tenantId与tenantCode.md
- 结论：tenantCode 只用于「入口解析」+「页面展示」（12处/7文件），解析出 tenantId 后全部环节（存储/JWT/插件/额度/统计）只用 tenantId（243处/54文件）；前端 0 处 tenantId、12 处 tenantCode
- 三条链路：登录（code→id→JWT）、请求（JWT→TenantContext→插件）、注册（code/URL tcode/平台默认→id→归属）
- ★ P0 阻断缺陷（我的疏漏）：平台管理员角色值有两套写法
  - 标准 platform_admin（下划线）：RoleConst、PermissionCheck、authController 登录、admin.html:185
  - 错误 platform-admin（连字符）：index.html:176 注册选项、index.html:610 PLATFORM_ROLE、UserController:98
  - 后果：注册的平台管理员 role 为连字符 → 登录时 platform_admin 判断 false → 走租户分支查 "platform" 租户失败 → 无法登录；且 PermissionCheck 不认，无任何平台权限
- P1：TenantService.getByCode 不过滤 deleted/status（两个调用方都手动补了，暂无问题但易漏）
- P2：tenantCode 大小写依赖库 collation（默认不敏感，前端填 default 匹配 DEFAULT）；改租户编码会使 ?tcode= 邀请链接失效
## 10:35 修复平台管理员角色值不一致（P0，BUILD SUCCESS）
- 三处统一为 platform_admin：index.html 注册 option value、index.html PLATFORM_ROLE 常量、UserController:101 改用 RoleConst.ADMIN/PLATFORM_ADMIN
- 顺带：authController 登录判断改用 RoleConst.PLATFORM_ADMIN，"platform" 抽为 PLATFORM_TENANT_CODE 常量并注释说明它不是真实租户编码
- 验证：全项目仅剩注释中的 "platform-admin" 字样；node --check 通过；静态资源已同步 target/classes
- ⚠ 存量数据需单独修正：UPDATE `user` SET `role`='platform_admin' WHERE `role`='platform-admin';
- 教训：改前端已有字面量前必须核对后端常量类，不能跟着错误写法走

## 11:22 为平台管理员注册/登录链路添加调试输出
- 统一前缀 `[DEBUG-PLATFORM]`，便于日志检索
- 后端 UserController.adminRegister：入参 role/account/tenantCode/name、命中管理员分支、返回 code
- 后端 authController.toLogin：入参 account/tenantCode/role、命中平台分支(tenantId=0)/租户分支(解析tenantId)、调 login 完成后的 code
- 前端 index.html submitRegister：提交前 role/account/tenantCode，平台管理员时提示 tenantCode 锁定值
- 前端 auth.js login()：打印发往 /auth/login 的请求体；index.html login 提交：提示前端登录表单当前未发送 tenantCode/role
- ★ 关键发现：前端登录表单只发 {account,password}，**不携带 tenantCode/role**，因此后端平台管理员登录分支（需 tenantCode=platform、role=platform_admin）从 UI 无法命中，只能用 API 工具触发。该缺口由调试日志暴露，待决定是否扩展登录表单。
- 环境限制：本机 maven lib/boot 缺失无法编译（classworlds Launcher 缺失），仅做语法核对（调用均为 Lombok @Data 生成的 getCode()/getTenantCode()，安全）。target 已还原。
- 静态资源已同步 target/classes/static；Java controller 需重新 mvn compile 才生效（当前环境无法编译）。

## 11:39 排查 compile 报错根因（已解决，BUILD SUCCESS）
- 用户怀疑"java 程序路径被改动"。实际根因是 **Git Bash 的 `mvn` POSIX 脚本把 `/c/...` 路径传给 Windows 版 java.exe，而本机 MSYS 路径转换是坏的**：`/c/Program Files/...` → 被扭曲成无效的 `\c\Program Files\...`，java 找不到 `boot/plexus-classworlds-2.9.0.jar`，报 ClassNotFoundException（非代码问题，Maven 安装本身完整）。
- 佐证：`jar tf` 报 `NoSuchFileException: \c\Program Files\...`；`mvn -version` 跟踪显示 exec 行 classpath 为 `/c/...` 但 java 实际收到 `\c\...`。
- 绕过方案（已验证可用）：不用 `mvn` 脚本，直接调 Maven launcher，路径一律用 Windows 风格（盘符+正斜杠，无前导 `/`，MSYS 不转）：
  ```
  JAVA="C:/Program Files/Eclipse Adoptium/jdk-21.0.8.9-hotspot/bin/java"
  BOOT="C:/Program Files/apache-maven-3.9.11/boot/plexus-classworlds-2.9.0.jar"
  MH="C:/Program Files/apache-maven-3.9.11"
  PRJ="C:/Users/Administrator/WorkBuddy/2026-08-30-17-19-24/api"
  "$JAVA" -classpath "$BOOT" "-Dclassworlds.conf=$MH/bin/m2.conf" "-Dmaven.home=$MH" "-Dlibrary.jansi.path=$MH/lib/jansi-native" "-Dmaven.multiModuleProjectDirectory=$PRJ" --add-opens java.base/java.lang=ALL-UNNAMED --enable-native-access=ALL-UNNAMED org.codehaus.plexus.classworlds.launcher.Launcher <goals>
  ```
- 另一坑：**VS Code Java 语言服务锁定 target**，会导致 maven-compiler-plugin 写 `createdFiles.lst` 时 BUILD FAILURE；但 javac 已把 149 个文件编译进 target/classes（调试代码已进 class，javap/grep 验证 [DEBUG-PLATFORM] 存在）。解决：先 `mv target $TEMP/...` 释放锁再编译，即可 BUILD SUCCESS（本次已成功）。
- 给用户的永久建议：编译别在 Git Bash 里跑 `mvn`，改用 PowerShell/cmd（路径原生）或 IDE 内 build；本机 JAVA_HOME=Temurin 21 正常，`mvn.cmd` 在 cmd 下可用（但 Bash 调 cmd.exe 被安全策略拦截，故走 launcher 直调）。
- 结论：平台管理员注册/登录调试输出（前后端）已 BUILD SUCCESS 编译通过，调试串已写入 target/classes/static 与 controller class。

## 12:31 方案 A 落地：登录表单支持平台管理员从页面登录
- 改动文件（前端 2 个，后端已就绪无需改）：`index.html` + `js/public/auth.js`。
- 发现 index.html 登录表单 HTML、登录提交处理、auth.js 的 `authenticateUser` 已被外部（用户/并行会话）部分改过：加了 `#login-role` 下拉 + 隐藏 `#login-tenant-code`，提交处传 `{tenantCode, role}`，但 tenant-code 是 hidden（租户端无法填值）。
- 我补全：① hidden→可见输入框+label+error-tip；② 新增 `applyLoginTenantRule()`（镜像注册页逻辑：平台管理员锁定 tenantCode=platform，租户端取 URL ?tcode= 或手填）；③ 修正 auth.js 一处字段名 bug：`extraInfo.tenantInfoCode` → `extraInfo.tenantCode`（与 index.html 传入的 `tenantCode` 及后端 LoginDTO 对齐，否则后端收不到租户编码）。
- **链路闭环**：index.html 透传 `{tenantCode, role}` → auth.js 构造 loginInfo 原样发 POST /auth/login → 后端 LoginDTO 绑定 tenantCode/role → toLogin 分支命中。
- 顺带根治：此前页面登录连普通租户都登不进（表单不发 tenantCode，后端 getByCode(null)→403）；现在租户端也能填 tenantCode 登录。
- **部署阻塞**：`target/classes/static` 被运行中的进程锁住（cp 报 Permission denied），无法热同步。源文件已正确；需停掉运行中的服务释放锁，再 build/restart 才能生效（重启同一已构建进程不会改磁盘内容，必须重建拷贝资源）。

## 12:55 编译并运行（已成功起服务，端口 8081）
- 编译：launcher 直调 `compile` 仍 BUILD SUCCESS（移开 target 释放 VS Code 锁）。
- **运行不能用 Maven**：本地仓库 `E:\MAVEN\.m2\repository-jeewx-h5-os2018` 是**只读/拒绝写入**的，`mvn spring-boot:run` 解析插件时写 `resolver-status.properties` 失败 → BUILD FAILURE（`compile` 只读取已缓存插件故成功）。
- **运行绕过 Maven**：直接用 `java -cp` 跑主类。依赖 classpath 已存在 `/c/Temp/cp.txt`（105 jar，由早先 `dependency:build-classpath` 生成，不受只读仓库影响，因只读取）。命令：
  ```
  cd /c/Temp/apprun
  CP=$(cat /c/Temp/cp.txt)
  APPCP="C:/Users/Administrator/WorkBuddy/2026-08-30-17-19-24/api/target/classes;$CP"
  java -Dserver.port=8081 -cp "$APPCP" com.reservation.SpringBootMergeApplication
  ```
- **端口诡异点**：`application.properties` 第2行 `server.port=8081` 实际已顶格（无前导空格），但首次不带 `-D` 用 `java -cp` 启动时 Tomcat 仍以 port 0（随机 35187）启动——疑似当时 target/classes 是旧构建残留。显式加 `-Dserver.port=8081` 后正确绑定 8081（端口配置本身正确，无需改文件）。
- Logback 写 `./logs/spring-boot-app.log` 被 IDE 日志查看器锁死（rename 也 Permission denied）；从 `C:/Temp/apprun` 作 CWD 启动使 `./logs` 落到新目录，规避锁。
- 验证：8081 LISTENING，curl `/` 返回 200 且含 `login-role`/`login-tenant-code`（更新后的登录页已生效）。
- 注意：用 `java -cp` 直跑时，VS Code Java LS 仍锁 target/classes（只读无妨）；要改 Java 后需移开 target 重新 compile，再重启此 java 进程。

## 13:47 固化一键启动脚本 run.sh
- 用户确认把启动流程固化。已在 `api/run.sh` 创建一键脚本（语法 bash -n 通过，已 chmod +x）。
- 脚本绕过本机四大坑：坏 MSYS 路径转换、只读 Maven 仓库、VS Code target 锁、IDE logs 文件锁。
- 流程：① mv target 释放锁 → ② launcher 直调 compile（仅读已缓存插件）→ ③ 复用/生成 `/c/Temp/cp.txt` 依赖 classpath → ④ cd /c/Temp/apprun，`java -Dserver.port=8081 -cp` 跑主类。
- 用法：`bash run.sh`（前台）或 `bash run.sh &`（后台）。当前 8081 服务（PID 22100）仍在运行，未强制重启；用户可用 run.sh 重启以验证脚本。
- **长效建议**：将来编译/运行一律用 `bash run.sh`，不要再在 Git Bash 里直接敲 `mvn`。

## 15:05 取消 account 加密：查漏 + 运行测试平台管理员注册/登录
- 用户决定 account 字段不再加密（之前已部分改）。检查改动完备性，发现并修复：
  1. **致命 bug（已修）**：`UserMapper.selectByAccount` SQL 被写成 `'%#{plain}%%'`——`#{plain}` 包在单引号字符串字面量内被 MyBatis 当字面量，实际执行 `account LIKE '%?%%'`，JDBC 把 `?` 当字面问号、无参数占位符 → 运行抛 `Parameter index out of range`，注册(existAccount)/登录/重置密码全 500。改为 `account = #{plain}`（account 已明文，精确匹配）。
  2. **冗余遗漏（已修）**：`UserService.doRegister` 返回处仍 `cryptoUtil.decrypt(user.getAccount())`，account 已明文（decrypt 对明文原样返回不报错，但语义脏）→ 改为 `user.getAccount()`。同步更新 encryptUserFields/decryptUserFields 注释（account 移出加密范围）。
  3. **SecurityConfig permitAll 漏配（已修）**：白名单只有不存在的 `/user/admin/register`，漏了实际映射的 `/user/register` → 自助注册被 Spring Security 拦 401；补 `/user/register`（与 WebMvcConfig/JwtFilter 对齐）。
  4. **user.role 列太短（已修 DDL）**：`role varchar(10)`，但统一后的角色值 `platform_admin` 有 13 字符 → 注册 500 `Data too long for column 'role'`。已 `ALTER TABLE user MODIFY role VARCHAR(32) NOT NULL`（mysql 客户端在 D:\program\mysql84\bin\mysql.exe）。
- 运行方式：mysql 命令行不在 PATH，用 `D:\program\mysql84\bin\mysql.exe -uroot -p123456 lesson_appointment`。应用因 ClassNotFound 改用 `java -cp`（Windows 路径 classpath）启动；run.sh 的 `API` 变量原为 `/c/...` 导致 classpath 里 target/classes 不被 Windows java 识别 → 已改 `C:/...`（TMP 同步改 `C:/Temp`）。
- **测试结果**：
  - 注册 `POST /user/register`（role=platform_admin, tenantCode=default）成功 200，返回 account 明文 `platadmin001`、token 中 tenantId=**1**（归属 default 租户）→ 印证 account 加密改动完备生效。
  - 登录 `POST /auth/login`：tenantCode=**default** → 200 登陆成功；tenantCode=**platform**（前端平台管理员规则）→ 404 账号不存在。
- **★ 既有设计缺陷（未修，待决）**：平台管理员 tenant_id 语义不一致。注册时平台管理员归属 default 租户 → tenant_id=1；而登录平台分支 `authController.PLATFORM_TENANT_CODE="platform"` 把平台管理员解析为 tenantId=**0**。按前端"平台管理员"规则（tenantCode=platform）登录时 user.tenant_id(1)≠请求 tenantId(0) → 跨租户拒绝 404。修复建议：注册 `role=platform_admin` 时显式 `user.setTenantId(0L)`（与登录平台分支一致），或登录平台分支放宽。需用户定夺是否改。

## 15:59 方案 A 落地：平台管理员注册 tenant_id 对齐 0（已修 + 验证通过）
- 用户确认按方案 A 修复 tenant_id 不一致。改动只在 `UserService.java` 两处：
  1. `UserService.Register`（约 143-151 行）：新增 `regRole`，当 `RoleConst.PLATFORM_ADMIN.equals(regRole)` 时 `regTenantId = 0L` 并跳过 `resolveTenantByCode`（sys_tenant 无 "platform" 记录，否则会误报 403）；否则仍走原真实租户解析。随后 `user.setTenantId(0L)` + `TenantContext.setTenantId(0L)`。
  2. `UserService.doRegister`（约 185-196 行）：配额占用 `tenantQuotaService.acquire` 包进 `if (regTenantId != null && regTenantId > 0)` —— 平台管理员(tenantId=0)无套餐记录，跳过否则 tryAcquire(0) 找不到记录抛 BusinessException 致注册 500/403。
- **验证**（后台启动，日志 `/c/Temp/app.log`，端口 8081）：
  - 注册 `POST /user/register` {role:platform_admin, tenantCode:platform, phone:必填(类级约束 @AtLeastOneNotBlank)} → 200。
  - 登录 `POST /auth/login` {tenantCode:platform, role:platform_admin} → 200，token 内 tenantId=**0** ✅（与注册对齐，404 问题解决）。
  - 隔离验证（全新账号仅用 platform 登录）同样 200，无副作用。
- **⚠ 遗留观察（非缺陷，设计使然）**：登录逻辑把 `tenant_id=0` 的用户当「存量未归属」在 `bindUserToTenant` 中自动重绑到请求租户。若平台管理员误用真实 tenantCode（如 default）登录，会被改归属到该租户(tenant_id=1)，此后用 platform 登录反而 404。属既有迁移逻辑与新增平台管理员的相互作用；如需彻底隔离可让 `bindUserToTenant` 对 role=platform_admin / tenant_id=0 直接 return。暂不改动，待用户定夺。
- 注册额外校验坑：User 有类级 `@AtLeastOneNotBlank(phone,email)`，缺二者会 400「参数错误」，平台管理员注册需带 phone 或 email。

## 16:16 理清 tenant_id 语义：0=平台管理员，1(default)=存量未归属
- 用户明确逻辑关系：**tenant_id=0 → 平台管理员**（合法的特殊租户）；**tenant_id=1(default) → 存量未归属用户已归属的租户**；tenant_id=NULL → 真正未归属。
- 排查全项目对 `tenant_id<=0` 的处理，发现唯一会把「0=平台管理员」误当「未归属」自动重绑的地方：`UserService.login`（约 244 行）`if (userTenantId == null || userTenantId <= 0)` → 调 `bindUserToTenant`。
  - **已修**：改为 `if (userTenantId == null)`（仅 NULL 才自动绑定到当前请求租户）。tenant_id=0 走后面的 else-if：与请求 tenantId 不等则 404，无法被真实租户冒领。
  - 同步更新 `bindUserToTenant` 注释，厘清三种边界（NULL/0/>0）。
- 配置层无需改：`MyBatisPlusConfig.ignoreTable` 对 tenant_id=0 返回 true（平台管理员查全量），`getTenantId` 对 null 用 -1 兜底；与「0=平台管理员」完全一致。
- `TenantQuotaService.getPackageOfTenant` 对 tenant_id<=0 返回 null（无套餐/不限），平台管理员无配额，亦一致。
- **验证**（后台重启，日志 /c/Temp/app.log，端口 8081）：全新平台管理员注册 200(token tenantId=0)；`tenantCode=platform` 登录 200；`tenantCode=default` 登录 404 且平台管理员未被改归属（再次 platform 登录仍 200）。隔离闭环正确。

## 17:00 整理 admin.html 菜单（平台管理员 vs 租户管理员）
- **Bug1（菜单空白根因）**：`templates/admin.html` 的 `loadSideMenu()` 读 `localStorage.getItem('userRole')`，但登录流程只写 `currentUser.role`（从不写 `userRole`）→ role 恒 null → `menus=[]` → 侧边栏空白。改为从 `currentUser.role` 读取。
- **Bug2（点击白屏）**：原菜单项用英文 key + `onclick="switchMenu(key)"` → `loadAdminPageContent(英文key)`，而 switch 只认中文标题（`数据总览`/`教师管理`…）→ 全部落空白屏。改为菜单标题用中文，点击统一由 `window.onload` 绑定的监听器 `loadAdminPageContent(中文标题)` 命中。
- **两套菜单（按 role 显示）**：
  - 平台管理员(platform_admin)：平台总览 / 租户管理 / 套餐管理 / 运营统计 / 系统设置
  - 租户管理员(admin 及未明确角色)：首页概览 / 课程管理 / 教师管理 / 学生管理 / 预约管理 / 数据统计
  - 非 platform_admin 一律走租户侧菜单，避免空白。
- **新增 4 个前端页**（均在 `static/js/`，已引入 admin.html）：
  - `admin-tenant.js`（租户管理：/tenant/page、/insert、/update、/{id}/status、/{id}/renew、/{id}/package、DELETE /tenant/{id}）
  - `admin-package.js`（套餐管理：模板 /package/template/* + 租户套餐 /tenant/package/*，含 tab 切换）
  - `admin-dashboard.js`（运营统计 + 平台总览：/dashboard/overview、/tenant/usage/page、/tenant/trend、/expire-warning）
  - `admin-config.js`（系统设置：/sys/config/list、/sys/config/update）
  - 后端接口此前已齐全；用平台管理员 token 验证 7 个接口全部 HTTP 200、结构符合预期。
- 分页用各文件自管的 `XxxPage` 对象 + 自定义分页栏（不依赖 pagefoot.js 全局注册）；模态框用独立 id（tenantModal/pkgTplModal/tpkgModal）规避 admin.html 既有 `#modalTitle`/`#closeModal` 重复 id 冲突。
- 启动沿用 `nohup bash run.sh > /c/Temp/app.log 2>&1 </dev/null & disown`（避免被 codebuddy 后台任务树连坐 kill；run_in_background=true 方式会被整棵进程树回收）。
- **本地坑**：Git Bash 下 `node --check /c/...js` 会被 node(Windows) 误解析成 `c:\c\...` 报 MODULE_NOT_FOUND（非语法错）；须用 Windows 路径 `C:/...` 才正确。

## 18:40 HTML 移回 static 后页面恢复 + /booking 转发 401 根因
- 用户把 templates/ 下的 html 移回 static/。先还原了"为 templates 当静态目录"加的多余配置：`application.properties` 的 `static-locations` 去掉 `classpath:/templates/`（回原 4 项）。
- **/booking 仍 401 的真因（与 templates 移动无关）**：`PageController.bookingPage()` 用 `return "forward:/booking.html"` 转发。Spring MVC 拦截器 `TenantInterceptor`（`WebMvcConfig` 注册 `addPathPatterns("/**")`）在 **forward 时也会执行**，而转发目标 `/booking.html` 不在 `EXCLUDE_PATTERNS` → `preHandle` 发现无 Authorization 头 → 返回 `401 未登录，请先登录`。`/admin.html` 等能开是因为它们已在 EXCLUDE_PATTERNS。
- **修复**：`WebMvcConfig.EXCLUDE_PATTERNS` 增加 `"/booking.html"`（与 SecurityConfig permitAll、JwtAuthenticationFilter WHITELIST 中已有的 `/booking.html` 三处保持一致）。三处白名单缺一不可：SecurityConfig permitAll + Jwt 白名单负责直接访问 /booking.html 放行，WebMvcConfig 排除负责 forward 重入时放行。
- 验证：/、/index.html、/admin.html、/student.html、/teacher.html、/teacherInfo.html、/teacherPublishedProfile.html、/logBrowser.html、/auditLog.html、/booking、/booking.html 全部 HTTP 200。

## 18:49 拆分平台管理功能到 platform_admin.html
- **需求**：把 admin.html 中的平台管理员功能（平台总览/租户管理/套餐管理/运营统计/系统设置）拆到独立页 platform_admin.html，登录/注册后按角色分别进入。
- **改动**：
  1. 新建 `static/platform_admin.html`：仅平台菜单 + 公共 JS + 4 个平台功能 JS（admin-tenant/package/dashboard/config.js）；onload 校验未登录跳 index，非 platform_admin 角色调 redirectToUserPage 回退，默认进"平台总览"。
  2. `static/admin.html`：`loadSideMenu` 去掉 platform_admin 分支（仅保留租户侧菜单）；`loadAdminPageContent` 删除 5 个平台 case；移除 4 个平台功能 JS 的 script 引用。
  3. `static/js/public/api.js`：`redirectToUserPage` 的 platform_admin 分支 → `./platform_admin.html?tCode=platform`；`autoLoginCheck1` 中 `'platform_Admin'`（大写 A，永不命中）修正为 `'platform_admin'` 并跳 platform_admin.html。
  4. `static/index.html`：注册成功回调里原 `setTimeout(()=>redirectToUserPage(userInfo),800)` 中 `userInfo` 未定义（会跳回 index 且抢在 submitRegister 跳转之前执行），移除该行，交给 submitRegister 统一按角色跳转。
  5. **后端三处白名单必须同步加 `/platform_admin.html`**（否则页面 401）：`SecurityConfig` permitAll、`JwtAuthenticationFilter` WHITELIST_PATHS、`WebMvcConfig` EXCLUDE_PATTERNS（与 /admin.html 对齐）。
- 验证：/platform_admin.html、/admin.html、/index.html 均 200；4 平台 JS 200；admin.html 不再引用平台 JS；平台管理员登录返回 role=platform_admin、token 正常。

## 19:11 菜单项改为 key 属性驱动（admin/teacher/student/platform_admin 四个页面统一）
- 需求：取消 loadSideMenu，menu-item 加 key 字段，loadAdminPageContent 按 key 而非菜单 title 分支
- admin.html：已改（上一轮）；本轮补齐 teacher/student/platform_admin
- 各页 key 映射：
  - admin：overview/schedule/booking_audit/lesson_notice/teacher/student/course_template/course_list/data_maintain/test_data/sys_log/audit_log
  - platform_admin：overview/tenant/package/statistics/config
  - teacher：today_course/my_course/booking_manage（另有注释掉的 course_notice/student_review/profile）
  - student：today_course/booking/my_booking（另有注释掉的 course_review/profile）
- 统一做法：点击/深链/初始加载都改用 getAttribute('key')；移除 normalizeTermText 归一化；student 原 data-menu 属性改为 key
- 静态文件由 run.sh 的 compile 拷进 target/classes/static，dev 无缓存，直接 cp 即可热更

## 20:24 新增「行业」字典（表+后端+前端，挂系统设置菜单）
- 表 sys_industry(id BIGINT 自增, code VARCHAR(50) 唯一, name, status TINYINT 1启用/0停用, remark, create_time, update_time)，迁移脚本 migration-20260901-industry.sql，已建表
- 后端（仿 SysConfig/Tenant 模式，MyBatis-Plus）：entity/Industry.java(@TableName sys_industry)、mapper/IndustryMapper extends BaseMapper、controller/IndustryController(/industry/*: list/insert/update/{id}/status/delete)，均 checkPlatformAdmin
- 前端：新建 js/admin-industry.js(renderIndustryPage + 增删改/状态切换/模态框)；改造 admin-config.js 的 renderConfigPage 增加「行业管理/系统参数」两个 Tab（默认行业管理）；platform_admin.html 引入 admin-industry.js；admin.css 补 .tab-bar/.tab-btn.active
- 验证：run.sh 重启 8081 成功；/industry/* 无 token 返回 401（路由+鉴权正常）；用 mint 的 platform_admin JWT 实测 list/insert(重复编码400)/update/status/delete 全部 200；测试数据已清空
- 注意：curl -d 含中文会触发 Jackson UTF-8 解析 500，需用 --data-binary @utf8文件 或让浏览器发（前端正常）

## 20:51 租户表加 industry_id 字段并前端显示行业名称
- DB：migration-20260901-tenant-industry.sql，ALTER sys_tenant ADD industry_id BIGINT（关联 sys_industry.id，NULL=未指定）
- 实体 entity/Tenant.java 加 private Long industryId
- 前端 admin-tenant.js：loadIndustryMap() 拉 /industry/list 建 id->name 映射；renderTenantCards 列表加「行业」列、renderTenantRows 用 industryName(id) 显示名称；openTenantModal 改为先 loadIndustryMap().then(buildTenantForm)，编辑表单加「所属行业」下拉（name=industryId），submitTenant 带回 industryId
- 验证：run.sh 重启 8081 成功；/tenant/page 实测返回 industryId 字段（设 id=1 的 industry_id=1 后列表正确带出）；前端 JS 关键改动就位；测试数据已清空

## 20:51 租户表加 industry_id 字段并前端显示行业名称
- DB：migration-20260901-tenant-industry.sql，ALTER sys_tenant ADD industry_id BIGINT（关联 sys_industry.id，NULL=未指定）
- 实体 entity/Tenant.java 加 private Long industryId
- 前端 admin-tenant.js：loadIndustryMap() 拉 /industry/list 建 id->name 映射；renderTenantCards 列表加「行业」列、renderTenantRows 用 industryName(id) 显示名称；openTenantModal 改为先 loadIndustryMap().then(buildTenantForm)，编辑表单加「所属行业」下拉（name=industryId），submitTenant 带回 industryId
- 验证：run.sh 重启 8081 成功；/tenant/page 实测返回 industryId 字段（设 id=1 的 industry_id=1 后列表正确带出）；前端 JS 关键改动就位；测试数据已清空
