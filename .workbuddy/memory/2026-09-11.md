# 2026-09-11 工作日志

## 修复：消息中心「消息不存在 + 未读数不更新」（frontend 消息中心 / message-service）

- **现象**：收件箱点开消息弹「加载失败 / 消息不存在」；点「标记已读」后未读角标不变。
- **根因**：`msg_message.message_id` 是 MyBatis-Plus `ASSIGN_ID` 雪花 ID（19 位，≥2.1e18），超过 JS `Number.MAX_SAFE_INTEGER`(9007199254740991)。后端以 JSON **数字**下发 → 浏览器 `JSON.parse` 静默丢精度（`…418` → `…400`）→ 前端拿错 ID 回查/标记 → 后端 `InboxService.require()` 查不到行，抛 `MessageBizException(404,"消息不存在")`；`markRead` 在 catch 里 return，`bumpBadge/refreshBadge` 都不执行 → 未读数不动。`/messages/ids`、已发列表的 `messageId` 同样中招（全部已读、收回也失效）。
- **实测证据**（恢复 `--server.port=8097` 启动 message-service，用源 `jwt.secret` 自签 token 走真实 HTTP）：
  - 列表原始响应 `"messageId":2097477561867124738`，JS 解析后 `2097477561867124700`
  - `GET /messages/2097477561867124738` → `code:200`（正常）；`GET /messages/2097477561867124700` → `code:404 消息不存在`
  - 用错 ID 标记已读 → 404，未读数 `5 → 5` 不变；用对 ID → `5 → 4`
- **修复**：新增 `api/message-service/.../config/JacksonConfig.java` —— `Jackson2ObjectMapperBuilderCustomizer` 把 `Long`/`long` 一律序列化为字符串（`serializerByType(Long.class, ToStringSerializer.instance)`），一次性覆盖实体 / `Map<String,Object>` / `List<Long>` 所有出口。反序列化仍兼容数字与字符串（路径变量、`List<Long> messageIds` 照常绑定）。前端**无需改动**（本就以字符串拼 URL / 传 body）。
- **回归**：修复后列表 `"messageId":"2097477561867124738"`（string）→ 详情 200「续费通知」→ 标记已读 200 → 批量全已读 `未读数 4 → 0` → 按快照还原为 4，`diff` 与备份完全一致。
- **构建**：message-service 是独立 Maven 工程（不在 `api/pom.xml` 的 modules 里），用 Maven launcher JAR + **`-f "<abs>\pom.xml"`**（Bash 工具每次调用会重置 cwd，不传 `-f` 会报「no POM in this directory」）。`package -DskipTests` BUILD SUCCESS。
- **排查陷阱**：本沙箱 `curl` 完全不可用（`http_code=000`，`--noproxy '*'` 也无效），改用 Node `http` 模块探测；Node 把 `/tmp/x` 当 `C:\tmp\x`，跨工具路径要过 `cygpath -w`。
- 已同步更新技能 `saas-api-build-smoke`（message-service 构建/启动、雪花 ID 精度丢失判定与修复模式、Node 探测 token 自签配方）。
- **未做**：修复只在源码 + 本地 jar，尚未部署到远程（152.136.254.127）。

## 修复：编译报「编码 UTF-8 的不可映射字符 (0xB8)」+ 全仓编码排查

- **报错**：`api/.../mapper/UserSessionMapper.java:[18,8] 编码 UTF-8 的不可映射字符 (0xB8)`。
- **根因**：该文件**整个是 GBK** 存的（`0xB8 0xF7` = 「各」），注释里的中文全是 GBK 字节，javac 按 UTF-8 读就报错。
- **修复**：GBK → UTF-8 转码（1300 → 1366 字节，CRLF 29 个不变）；`mvn -o compile` 复验 **BUILD SUCCESS**。
- **顺带扫出第二个文件**：`frontend/js/public/termsFunction.js` **不是合法 UTF-8**——
  - 类型 A：第 17–21 行整行是 GBK（GBK 解出来完全通顺）；
  - 类型 B：另有 ~66 行是 UTF-8 但个别汉字**丢了最后一个字节**（如 `：`=EF BC 9A → 只剩 EF BC）。
  - 定损结论：258 个替换字符**全部落在注释内**，0 个在代码/字符串 → 无功能影响，但文件非法 UTF-8。
  - 损坏引入点：commit `246b7a9`（坏 238 处），`79f31ed`（涨到 382 处）；最后一次完好版本是 `e4b8bc7`。
- **修复方式**：只补注释，代码字节零改动。5 行整行 GBK 用 gb18030 还原；17 行靠「与 `e4b8bc7` 长度相等 + 片段有序唯一命中」精确还原；49 行按行号逐点补字（补字个数 == 坏点数，脚本自校验）；再润色 46 处丢字节连带丢失的空格/尖括号/引号。
- **验证**：合法 UTF-8 ✔ / `node --check` 通过 ✔ / **去注释后与修复前逐字节相等 ✔**（证明代码没被碰、行数没变 317 行）。
- **判别陷记**：判断「整行 GBK」不能用坏字节占比（短行、含大量 ASCII 的 GBK 行会漏判），也不能用「命中词典字数 - 2×坏点数」打分（会把正常中文行误转成乱码）。可用判据：`cjk(gb18030结果) > 3*cjk(UTF-8容错结果) + 2`。
- **现状**：全工作区非 UTF-8 文本文件 = **0**。
- 新建技能 `source-encoding-repair`（含三类判据、只补注释的安全策略、代码零改动的验证方法）。

## 修复：页面顶部「刷新」按钮点完内容区空白（frontend 通用刷新）
- **现象**：消息中心页内「刷新」正常；点**页面顶部**的「刷新」，中央内容区被清空后不再渲染（空白）。
- **根因**：`refreshRightPage()`（`frontend/js/public/appointmentNotes.js`）把**标题文本**当 key 传给了 `loadAdminPageContent()`：
  ```js
  loadAdminPageContent(pageTitle.textContent);   // 传的是「消息中心」
  ```
  而 `loadAdminPageContent` 只认菜单 key（`messages`/`overview`/`schedule`…），switch 落空 → 而函数开头已 `innerHTML = ''` → 内容清空且无分支渲染。**四个入口页（admin / platform_admin / student / teacher）全部中招**，不是消息中心独有。
- **修复**（符合用户要求："把本页面的刷新函数封装在通用刷新函数中"）：
  1. `appointmentNotes.js` 里 `refreshRightPage()` 升级为通用入口，三级回退：
     ① `window.pageRefreshHandlers[key]` 有注册 → 调页面自有刷新函数（保留分页/标签/搜索词）；
     ② 没注册 → `loadAdminPageContent(key)` 整页重渲染；
     ③ 最后才回退历史行为（标题文本，且加了 `typeof === 'function'` 守卫防 TypeError）。
  2. 新增 `registerPageRefresh(menuKey, fn)` 注册表 + 用 IIFE 包装各页面自己的 `loadAdminPageContent` 以记录 `window.currentMenuKey`（不改动原逻辑）。
  3. `messages-inbox.js` 新增唯一刷新函数 `refreshMessagesView()`（列出列表 + 未读角标 + 离线队列），**页内刷新按钮与顶部刷新共用它**；容器不在文档中则返回 `false` 交通用逻辑兜底。`state` 增加 `container` 字段。
- **验证**（Node `vm` + 伪 DOM/axios 接线测试，源码与压缩后 `dist` 各跑一遍）：**17 PASS / 0 FAIL**；含"顶部刷新 → 重拉列表"、"保留当前标签页（仍请求 `/messages/sent` 而非重置回 `/inbox`）"、"未走整页重渲染"、"其他菜单按 key 重渲染"、"容器卸载后兜底"、"无 loadAdminPageContent 时不抛异常"。
- 已重建 `frontend/dist`（`node build.js`，BUILD done）；跨文件全局名 `pageRefreshHandlers` / `registerPageRefresh` / `refreshMessagesPage` / `currentMenuKey` 在压缩产物中均**未被混淆**。

## 消息中心：隐藏页内重复的「刷新」按钮（2026-09-11）
- **需求**：页内工具条「发送通知」左侧的「刷新」（`#msg-refresh`）位置紧贴内容区顶部，与页面顶部通用刷新功能重复 → 不再显示。
- **改动**（仅 `frontend/js/messages-inbox.js`，共 -2 行按钮/绑定 + 注释）：删除按钮 HTML 与 `#msg-refresh` 的 click 绑定（若只删 HTML 不删绑定，真实 DOM 下会 `querySelector` 返回 null 抛错）。`refreshMessagesView()` 保留并继续由顶部通用刷新调用，另留 `window.refreshMessagesPage` 对外接口。
- **验证**：接线测试扩到 **19 PASS / 0 FAIL**（源码与 dist 各一遍），新增断言「渲染出的工具条不含 `msg-refresh`」「`msg-compose` 与其余工具条按钮仍在」「对外刷新函数仍能重拉列表 + 同步角标」；`dist/js/messages-inbox.js` 中 `msg-refresh` 计数为 **0**。

## 修复：学生端「课程预订」切换排期时「我的时区」显示慢一步（2026-09-11）
- **现象**：`student.html` → 课程预订，选好课程+排期后「排期信息」右侧「我的时区」列（预约时间/开始日期）在切换「选择排期」时**总显示上一个排期的值**。
- **根因**（`frontend/js/student-bookingCards.js` 的 `displaySchedule()`）：
  ```js
  renderSchedule(scheduleObject);   // async，内部第一行就 await 查已预订人数 → 未 await，DOM 还没写
  getMyDatetime();                  // 立刻从表单读 originalTimeZone/startDate/startTime
  ```
  时区换算读到的是**上一个排期残留在表单里的旧值** → 换算结果自然"慢一步"。附带：**首次选中排期时右侧是空的**（表单里还没有任何残值），从第二次起才显示上一个的值——这个特征可确诊此 bug。
- **修复**（三处）：
  1. `displaySchedule` 改 async，**先 `await renderSchedule()`**（并把 `reloadBooking_student()` 也 await），再算时区；
  2. 换算不再从 DOM 读值，改为传**排期快照**（新增 `renderUserTzBlock(snapshot)`），与 DOM 写入时机彻底解耦；`getMyDatetime/getMyEndDatetime` 签名扩为 `(snapshot, seq)`，缺省仍回退读表单（向后兼容）；
  3. 新增会话内自增序号 `bookingTzSeq` + `clearUserTzBlock()`：换算返回时序号不匹配就丢弃（防"快速连续切换"时慢响应覆盖新结果）；排期时区==用户时区、或该课程无排期时清空并隐藏右侧列（原先会残留上一门课程的换算结果）。
- **验证**（`C:/Temp/student-tz-race-test.js`，vm + DOM 垫片跑真文件；两排期故意设为不同时区：巴黎 09-20 09:00 / 纽约 10-05 14:30）：
  - **修复后 15 PASS / 0 FAIL**（源码与 `dist` 产物各一遍）；
  - **修复前（`git show HEAD:` 版本）同套用例 7 PASS / 8 FAIL**，FAIL 内容正是用户症状：选中 S2 后右侧显示的仍是 S1 的值（`2026-09-20 16:00`），且换算入参仍是 `Europe/Paris / 2026-09-20 09:00`；
  - **阴性对照**：把序号守卫替换成注释后重跑 → 竞态用例立刻 FAIL 并打印出旧排期的值，证明该用例有判别力。
  - 关键断言是**接口收到的入参**（`tzSwitchTo(fromZone, dateTime, toZone)`），比断言显示文案更能暴露"读到旧值"。
- `frontend/dist` 已重建（`node build.js`）；`bookingTzSeq`/`renderUserTzBlock` 等局部名在压缩产物中被混淆属正常（行为用例仍全过），跨文件全局名 `displaySchedule` 保留。
- 已把「异步渲染未 await 导致读旧值」的复现范式、`git show HEAD:` 新旧版本对照法、以及"删掉修复点做阴性对照"写进技能 `browserless-frontend-itest`（v1.6.0）。

## 修复：学生端「课程预订」切换课程时「排期信息」未重置（2026-09-11）
- **现象**：`student.html` → 课程预订，选中课程 A 的排期后再切换到课程 B，排期列表已自动刷新、等待选择排期，但「排期信息」里仍是 A 那个排期的内容（时区/日期/上课时间/剩余席位/按钮状态）。
- **根因**（`frontend/js/student-bookingCards.js` 的 `loadSchedule()`）：只在「该课程没有有效排期」（`cnt == 0`）的分支里清理显示区，**课程有排期时 `return` 直接返回 → 从不重置**。另外那条"无排期"分支本身也是坏的：`let cnt` 声明在 `try` 块内、块外引用 → `ReferenceError: cnt is not defined`（`node --check` 查不出来），使该分支**长期完全失效**，且 `scheduleSelect` 也在 `if` 块内用 `const` 声明却在块外引用。
- **修复**（同一文件，新增 2 个函数 + 重写 `loadSchedule`）：
  1. 新增 `resetScheduleInfoPanel()`（唯一的重置入口）：作废 pending 换算（`bookingTzSeq++`）+ 清空右侧「我的时区」列并隐藏；`selectedScheuleId`/`scheduleObject` 置 `null`；清空排期字段（scheduleId/originalTimeZone/startDate(+星期)/startTime/endDate/interval/status/availableSites/now_availableSites）；重复类型回 `none` 并收起周/月勾选；预订状态回「无预订」+ 按钮组合回初始；清空「排期结果」列表与日历；右侧列隐藏。
  2. 新增 `resetScheduleSelect()`：把排期下拉恢复为「请选择课程排期」占位。
  3. `loadSchedule()` **在发请求之前**先调这两个函数（关键：等请求返回才清的话，整个往返周期用户看到的都是旧值）；`cnt` 提到函数级；`scheduleSelect` 提到函数级；选回「请先选择课程」时连 `courseId`/教师名一并清空；「无排期」分支复用同一个重置函数。
- **验证**（`C:/Temp/student-course-switch-reset-test.js`，vm + DOM 垫片跑真文件；3 门课程：C1 两个排期 / C2 一个 / C3 无排期）：
  - **修复后 26 PASS / 0 FAIL**（源码与 `dist` 产物各一遍）；
  - **阴性对照**（把 `resetScheduleInfoPanel` 换成空实现）**15 FAIL**，失败信息正是残留值（`scheduleId="S2"`、`originalTimeZone="America/New_York"`、`display=block`、按钮 `cancel:block` 等）→ 断言有判别力；
  - **历史版本对照**（`git show HEAD:`）**19 FAIL**，其中实证 `ReferenceError: cnt is not defined`（无排期分支失效），另含"重置后仍能正常显示"因缺 `await` 修复而 FAIL。
  - 踩坑（已写进技能 v1.7.0）：① 沙箱桩 `renderStudentBookingStatus` 被**被测文件内部的同名函数遮蔽** → "桩没被调用"是假 FAIL，应改为断言真实副作用；② DOM 垫片的 `value` 必须字符串化（`el.value = 1` → `"1"`）、`innerHTML` 必须清空子节点并让 `select.value` 归位，否则 `options.length`/`value` 断言假 FAIL。3 条假 FAIL 全是垫片的锅，产品代码本身正确。
- `frontend/dist` 已重建（`node build.js`）；文件仍为合法 UTF-8。
- **注意（待确认）**：排期信息区内的「刷新」按钮走 `refreshData_student()` → `loadSchedule()`，现在也会一并重置排期信息（更一致，但会丢失当前选中的排期）。若希望"刷新保留当前排期"，需在重载后按旧 `selectedScheuleId` 自动选回（尚未实现，等用户拍板）。
- **未做**：远程服务器上的 `frontend/dist` 未更新，部署后才生效。

## 新功能：学生端「候补预订」（名额已满时的候补）+ 连带修掉 3 个前置缺陷（2026-09-11）
- **需求**：① 剩余席位数 = 0 时点「预定排期」→ 提示“该排期名额已满，可候补”，且不进入预定处理；②「预定排期」右侧新增「候补预订」，流程与预定一致、status 置为 `waiting`；「预定状态」中 `waiting` 显示为“候补”。
- **改动清单**：
  - `frontend/js/student-bookingCards.js`（主改动）：新增 `waitBtn`（`btn-warning`，默认 `display:none`）；`#bookingStatus` 增加 `<option value="waiting">候补</option>`；新增 `currentRemainingSites` / `currentBookingObj` / `ALWAYS_SHOW_WAITLIST_BTN` / `WAITLIST_TIP` 模块常量；新增 `isScheduleFull()`、`applyBookingButtons()`（**按钮组合唯一判定入口**，renderSchedule 与 renderStudentBookingStatus 都调它，避免两处各写 `style.display` 相互覆盖）、`makeOneWaitBooking_student()`（走 `window.` 挂载，供按钮 onclick 调用）；`makeOneBooking_student()` 满员守卫改为 `isScheduleFull()` + 新文案；`reloadBooking_student()` 改为优先取“仍然有效”的记录（`waiting/booking/booked/canceling`），避免同一排期多条历史记录时取到旧的已取消记录。
  - `frontend/js/public/courseAndBooking.js`：`checkStatus_booking()` 增加 `waiting → '候补'`（管理端/教师端/数据维护的预订列表状态列）。
  - `frontend/js/public/appointmentNotes.js`：`checkAppointmentStatus()` 增加 `waiting → '候补'`（兜底）。
  - `frontend/js/admin-booking.js`：筛选下拉增加「候补」；按钮矩阵增加 `waiting` 分支（确认候补 → `booked`；拒绝候补 → `cancelled`），否则候补记录在管理端无法处理（功能会死在这里）。
  - `api/.../mybatis/mapper/BookingMapper.xml`：`countBookingByScheduleId` 增加 `AND status <> 'waiting'` —— **候补不占席位**，否则候补人数会把剩余席位越吃越少、名额释放后也无法正确反映可用席位（该 SQL 同时被 `CourseScheduleService.getAvailableSchedule` 使用，语义一致）。
  - `api/.../BookingController.java` + `MessageNotifyService.java`：新增 `notifyWaitlistCreated()`，候补走“新的候补申请”文案（分类仍复用 `BOOKING_CREATED`，避免引入未在 `msg_category` 登记的编码）。
- **顺带修掉的 3 个前置缺陷（不修则本功能必然误判）**：
  1. **学生页「总席位数」从不从排期写入**（`admin-schedule.js` 里有这行，学生页漏了），加上 `resetScheduleInfoPanel` 把它重置为 1 → 「剩余席位 = 1 - 已预订数」，**总席位 > 1 的排期一律被误判为满员**（当前库里 `available_sites` 全是 1，所以一直没暴露）。已在 `renderSchedule` 中先写 `#availableSites = scheduleObject.availableSites` 再算剩余席位（实测接口返回确有 `availableSites` 字段）。
  2. **预订主键字段名错误**：`renderStudentBookingStatus` 写的是 `bObj.id`，而实体 `Booking` 的 `@TableId` 是 `bookingId`、实测响应也是 `bookingId` → 输入框被写成字符串 `"undefined"`，导致**取消/删除/改订都带着错 id 提交**。已改为 `bObj.bookingId || bObj.id || ""`。**技术手段**：读 `application.properties` 的 `jwt.secret` 用 Node crypto 自签 HS512 token，打本机 8081 探针打印 `Object.keys(首行)`（探针脚本跑完即删）。
  3. **「刷新」按钮在 booked/cancelling 状态下被隐藏**（原 renderStudentBookingStatus 的设置），已统一为始终显示（页面级动作不该被预订状态藏起来）。
- **验证**（无浏览器接线测试 `C:/Temp/student-waitlist-test.js`，vm + DOM 垫片跑真文件；场景：满员无预订 / 有余位 / 已候补 / 已确认+满员 / 已取消+满员 / 无排期）：
  - 修复后 **34 PASS / 0 FAIL**（源码与 `dist` 产物各一遍，dist 重建后复测）；
  - **历史版本对照**（`git show HEAD:` = 16ea4fd）**23 FAIL / 9 PASS**，真实差异含「剩余席位 0（应为 4）」「bookingId 为空（应为 B-WAIT-1）」「取消动作携带空 id」「满员提示仍为旧的『排期已满』」「无候补按钮/无 waiting 选项」；
  - 回归：`student-course-switch-reset-test.js` 26/0、`student-tz-race-test.js` 15/0（源码 + dist）。
  - 后端：`api` 模块 `compile` BUILD SUCCESS；`BookingMapper.xml` 用 Python ElementTree 校验 XML 合法；SQL 语义用只读 `group by` 查询核对。
- **未做**：远程服务器 `frontend/dist` 与 api jar 均未部署；`api` 只 compile 未 package。
- **待用户拍板**：① 候补名额释放后**不会自动转正**，需管理员在「预订列表」点「确认候补」；若要自动化（有人取消即按候补顺序补位）需另做。② 候补按钮默认只在满员时出现（`ALWAYS_SHOW_WAITLIST_BTN = false` 可一键改为常显）。

## 修正：学生页「总席位数」的口径 —— 管理员设定、学生页只读（2026-09-11）
- **用户口径**：学生页「总席位数」不由学生页产生，它是**管理员在「排期设置」时设定的**（`course_schedule.available_sites`），学生页只展示 —— 即该字段在学生页必须**只读**。上一轮只做了“从排期对象写入”，漏了“只读”，本轮补上。
- **数据链路核实**：`fetchScheduleList(cid, status)` → `GET /schedule/selectByCourseId/{cid}?status=active` → mapper `selectByCreateDto` 是 `SELECT *`，`mybatis-plus.configuration.map-underscore-to-camel-case=true` → 响应确实含 `availableSites`，与 `admin-schedule.js` 看到的同源。
- **改动**（`frontend/js/student-bookingCards.js`）：
  1. 学生页 `#availableSites` 加 `readonly class="readonly"`（`class="readonly"` 是本页既有约定：灰底 + `pointer-events:none`；但**只有 class 挡不住 Tab+键盘输入，必须同时带 readonly 属性**）。`admin-schedule.js` 的同名字段保持可编辑 —— 那是管理员设定的地方。
  2. `renderSchedule` 重构为**单一数据源**：`scheduleTotalSites = Number(scheduleObject.availableSites)` → 写入 `#availableSites` → `剩余席位 = 总席位 − 已预订数`；不再“写进去再从 DOM 读回来”。排期数据缺该字段（异常）才回退读页面现值。
  3. `resetScheduleInfoPanel()` 的席位两项由硬编码 `1` 改为**清空（未知）**——按上述口径学生页不该凭空产生这个数，且 `isScheduleFull()` 对“未知”按未满处理，不会误阻断预定。
- **测试**：
  - 新增 `C:/Temp/student-total-sites-source-test.js`（源码级断言 + 阴性对照）**11 PASS / 0 FAIL**：学生页带 readonly 属性、管理员页不带、数据源断言（取自排期对象）、以及 3 条对照证明断言非恒真。
  - 回归全绿：`student-waitlist-test.js` 34/0、`student-course-switch-reset-test.js` 26/0（其中“席位回到 1”的断言按新口径改为“席位清空为未知”）、`student-tz-race-test.js` 15/0。
  - `frontend/dist` 已重建，产物中确认：学生页 `<input ... readonly class="readonly">` / 管理员页无 readonly。
- **两个教训（已写进 MEMORY.md 与技能）**：
  1. **同一文件不要在同一轮里并行发多个编辑**：后写入者基于旧快照，静默覆盖前一次改动（本轮 `#availableSites` 的 readonly 就是这样丢的，表现为“编辑报成功但文件没变”）。
  2. **断言只认 `readonly` 这个“词”是弱断言**：`class="readonly"` 里也含该词，会让“只加样式类、没加只读属性”的字段假通过 —— 必须用 `(?:^|\s)readonly(?=[\s>]|$)` 判**属性**形态。这条是阴性对照抓出来的（对照首跑还误删了别的字段的 readonly，已改为锚定本字段标签）。
- **待拍板（安全缺口，尚未动代码）**：后端**没有任何名额校验** —— `countBookingByScheduleId` 只用于算剩余席位。学生页只读只是 UI 层约束，直接调 `/course/booking/create` 或并发提交仍可超额预订。若要补，落点在 `BookingController` 的创建/改订路径（新增“已确认预订数 < 排期总席位”校验，候补不占位），会改既有接口语义，故先不动。

## 澄清：「今日课程」不能承载候补（2026-09-11，已核实数据链路）
- 用户提问：学生页「今日课程」是否应列出 `waiting` 预约、并在搜索栏「状态」加「候补」选项。**结论：诉求合理，落点不合理（该选项会是死选项）。**
- **数据链路（三处证据）**：
  1. 今日课程走 `AppointmentController.listByDaysByPage`（:206）→ `AppointmentService.getBetweenTimeByPage`（:134）：先用 userId/role 从 `booking` 取 bookingId，再查 **`appointment`** 表（`between appointment_datetime` + `eq(Appointment::getStatus, status)`）→ **状态筛选过滤的是 `appointment.status`**。
  2. `appointment` 只有两个写入入口：`CourseScheduleService.asgn_student`（:400，管理员「指定学生」→ booking=`booked` + appointment=`active`）、`POST /course/appointment/add`（请假单条）。`BookingService.create`（:29）只 `bookingMapper.insert`。**全后端没有任何路径把 `waiting` 写进 appointment** → 下拉里加 `value="waiting"` 恒返回 0 行。
  3. 语义冲突：今日课程 = 最近 7 天**已成课次**；候补 = 排队等位、无课次。且该下拉是 appointment 状态机（`active/noted1/noted2/completed/cancelling/...`，见 `admin-AppointmentNotes.js:34-52`），混入 booking 状态机会污染语义。
- **连带发现的真问题**：**候补「确认候补」后不会生成课次**（`BookingService.updateStatus` 只改 booking.status，不生成 appointment；生成课次唯一入口是排期页「指定学生」，而它会**另建一条 booking**）→ ① 候补转正后学生今日课程仍空白；② 再点「指定学生」会让同一学生同一排期出现两条 booking，席位被重复占用。建议把"确认预订/确认候补 → 生成 appointment"收敛成一个动作。
- 已向用户给出 3 个方案（A 顶部提示条 + 跳预订页〔推荐，零后端改动〕／B 今日课程内单独「候补中」区块／C 后端联合查询并入状态筛选，需处理联合分页）待选。

## 落地：今日课程「候补」提示条 + 我的预订页候补支持（2026-09-11）
- **用户拍板**：选 A（今日课程顶部提示条）；「候补转正 → 生成课次」先看方案再定（下一轮）。
- **改动清单**：
  - `js/admin-AppointmentNotes.js`（今日课程页面模板，admin/student/teacher 共用）：卡片开头新增 `<div id="waitlist-banner">`（默认隐藏，inline 样式琥珀色提示条），渲染后调用 `renderWaitlistBanner()`。
  - `js/public/appointmentNotes.js`：新增 `renderWaitlistBanner()` + `goToStudentMyBooking()`（挂在 `window`）。守卫：仅 `userRole==='student'` 且有 `userId`；数据源 `POST /course/booking/list {userRole:'student',userId,status:'waiting'}`（服务端过滤）；明细用 `fetchSchedule(scheduleId)` 取排期名+开始日期时间，最多 3 条 + 「另有 N 条」；**无候补/接口异常/非学生一律隐藏并清空 innerHTML**（防残留）；文案附一句“名额释放时由管理员确认，确认后才会排上课次”以对齐当前设计。跳转优先 `document.querySelector('.menu-item[key="my_booking"]').click()`（复用页面菜单逻辑），无菜单项才回退 `loadAdminPageContent('my_booking')`。
  - `js/student-bookingBrowserCards.js`（我的预订＝提示条的落地页，此前完全没有候补支持）：① 状态下拉加 `<option value="waiting">候补</option>`；② 状态映射加 `waiting: "候补"`；③ 新增按钮分支 `waiting → 「撤销候补」→ 状态置 cancelled`（学生不做“直接确认候补”，避免绕过名额校验）；④ waiting 列入“不显示预约详情”（没有 appointment 行，点开必然空表）。
  - **顺带修掉一个同族真缺陷**：该文件第 160 行写的是 `bookingId: booking.id`，而实体 `Booking` 只有 `bookingId`（无 `id` 字段）、分页接口返回的也是 `bookingId` → 卡片上「撤销/申请取消/重新申请」全部带着字符串 `"undefined"` 提交，后端 `updateStatus` 按 id 更新、查不到就静默无事发生。已改为 `booking.bookingId || booking.id || ''`（阴性对照实测：改回旧写法立刻退化成 `actionForButton('undefined',...)`）。
- **测试（两个新脚本，vm + DOM 垫片跑真文件，支持 `DIST=1` 复测压缩产物）**：
  - `C:/Temp/student-waitlist-banner-test.js`：源码 **28 PASS / 0 FAIL**，dist **24 / 0**（dist 轮跳过对照）。覆盖：接线（`refreshAppointmentNotes()` 真会渲染提示条）、有/无候补、teacher+admin 不显示且不发请求、接口异常与排期缺失降级、明细上限 3+「另有 N 条」、跳转两条路径、阴性对照 ×2（改坏隐藏分支 / 去掉学生守卫，均立刻 FAIL）。
  - `C:/Temp/student-mybooking-waitlist-test.js`：源码 **17 / 0**，dist **13 / 0**。覆盖：候补卡片状态文案、撤销候补按钮、**从 HTML 里取出 onclick 表达式并真的执行**（断言以 `cancelled` + 正确 id 提交）、下拉参与查询、waiting 无详情按钮 + booked 有（互为对照）、阴性对照 ×2。
  - 回归全绿：total-sites 11/0、waitlist 34/0、course-switch-reset 26/0、tz-race 15/0。`frontend/dist` 已重建并核对产物（`waitlist-banner`、`option value="waiting">候补`、`bookingId:a.bookingId||a.id||""` 均在；全局名未被混淆）；改动文件均 UTF-8 无 BOM。
- **测试自身踩的两个坑（已写进技能）**：① 阴性对照用带 `\n` 的字面量锚点匹配不上——被测文件是 **CRLF**，必须用 `[\s\S]` 跨行正则锚定**目标分支**；② 断言用粗略切片定位目标对象（从第一张卡片的状态文案往后切）会把后面卡片的元素圈进来 → 假 FAIL，要从卡片边界切分。
- **未做**：远程 152.136.254.127 未部署（dist 与 jar 都是旧的）。

## 候补状态机方案评审（用户提出 join_waitlist / waitlist_pending / available 三态 + cancelling 行挂「递补」）
- **结论**：方向可行，但不采纳 3 项。① `join_waitlist` 与已上线的 `waiting` 同义（DB 实存 1 条），重复；② `available` 全流程**没有任何触发点**把它置位、也没有出口（无 offer TTL、无超时回退、无定时任务）→ 一旦置位永久卡死；③ 候补次序应是**计算值**（`ORDER BY create_time`），不能进状态字符串（有人取消/转正就得批量重排）。
- **收敛后的 4 态**：`waiting`（申请候补，已有）─管理员确认─► `waitlist_pending`（唯一新增）─递补─► `booked`（生成 appointment）；取消侧 `booked ─学生申请─► canceling ─管理员确认─► cancelled`。
- **实测到的硬约束**：
  - `booking.status` = **varchar(20)**（`information_schema` 实查；`beforeRun/sql/lesson_appointment_schema_fixed.sql:97`）。现网实际值只有 `booked`(153) / `waiting`(1)。`waitlist_pending`=16 字符，**只剩 4 字符余量** → 后续再加 `waitlist_offer_expired`(21) 必爆。
  - 字段注释与实体注释**都已腐烂**：`Booking.java:29` 注释写 `varchar(10)`、schema 注释只写 `booked/cancelled/completed`，实际值域远大于此。
  - **拼写分裂已存在**：`canceled`(单l) 与 `cancelled`(双l) 并存、`canceling` 与 `cancelling` 并存（`admin-booking.js:219` 就是"两个都判"的补丁）；`bookProved` 只在 Java 注释里、前端已无。状态字符串散落在 9 个前端文件硬编码。
- **方案里两个会静默出错的点（务必同步改）**：
  - **`countBookingByScheduleId` 只排除 `'waiting'`**（`BookingMapper.xml:179`）→ 引入 `waitlist_pending` 后候补态被当成正式占位 → 剩余席位少算 → 有空位却显示满员、递补入口不出现。**静默错**。
  - **`asgn_student`（`CourseScheduleService.java:400`）无条件 `insert` 新 booking**（不按 `(scheduleId,studentId)` 查重）→ 递补若走"指定学生"会给同一学生同一排期建第二条 booking，席位重复占用，且学生端出现两条记录。
- **递补按钮落点**：用户原定挂在 `cancelling` 行的操作区 → 两处错：① `cancelling` 只是"申请取消待确认"，**该 booking 仍占席位**（`countBookingByScheduleId` 含它），此时放出递补，原学生一点「撤回」就两人同时占位；② 按钮长在 A 的取消记录上却处理 B，归属错位。**改为排期维度入口**：`cancelled` 落库后该排期空位 +1，在排期分组头显示"空位 N · 候补 M 人 →[查看候选]"，弹层按 `create_time ASC` 列候补队列选人。
- **原子性要求**：`BookingService.updateStatus` 目前是**裸 UPDATE**（`UPDATE booking SET status=? WHERE booking_id=?`，无 CAS、无事务、无席位校验）→ 两个管理员同时递补会选同一人/超额。递补须：`UPDATE ... WHERE booking_id=? AND status='waitlist_pending'`（影响行数 0 = 被抢先，报错）+ 重新校验剩余席位 + 复用已有 booking + 生成 appointment（幂等）。
- **兼容性变更**：现有 `admin-booking.js:227`「确认候补 → booked」是**直接转正**，新方案改为 `waiting → waitlist_pending`（仅入队），是行为变更；调用方清单：`admin-booking.js`（按钮矩阵 + `confirmOrCancelBooking`）、`BookingService.updateStatus`、`BookingMapper.updateStatus`、`js/public/appointmentNotes.js`（提示条）、`js/student-bookingBrowserCards.js`（我的预订）。
- **建议第 1 步**：先把状态字符串收敛为单一常量来源（前端一份 + 后端一份），否则每加一个状态，排查面就是 9 个文件。
- 用户询问形式为"是否可行、可否再优化"，**本轮只做分析未改代码**，待其拍板下一步。

## 候补递补落地 + 超额预订闸门（用户拍板 4 点后的实现与测试）
- **用户拍板**：① 不要 `waitlist_pending` 这一层；② 递补入口改到**排期维度**（预订管理页取消行点「查询递补」→ 跳「课程排期」并带入 scheduleId，在该页点「递补」）；③ 递补成功后自动发消息给学生；④ 一并处理超额预订。
- **最终状态机（4 态，无新增状态）**：`waiting` ─递补─► `booked`；取消侧 `booked → canceling/cancelling → cancelled`。候补次序 = `ORDER BY create_time ASC, booking_id ASC` 计算值，不入库。
- **新增/改动文件**：
  - 新 `api/.../common/BookingStatus.java`：状态常量 + `NON_OCCUPYING=[waiting,cancelled,canceled,rej-booking]`（**canceling/cancelling 仍占位**）+ `occupiesSeat()`。占位规则的单一来源。
  - 新 `api/.../service/BookingSeatService.java`：名额闸门 + `lockScheduleAndAssertExists`（排期行锁）+ `countOccupyingForUpdate`（锁定读计数）。
  - `BookingMapper`(+XML)：`countBookingByScheduleId` 改参数化排除状态 + `excludeBookingId`；新增 `selectOccupyingBookingIdsForUpdate` / `selectWaitlistByScheduleId` / `updateStatusIfCurrent`(CAS) / `selectLatestByScheduleAndStudent`。
  - `courseScheduleMapper`：新增 `selectByIdForUpdate`。
  - `BookingService`：5 条写路径接入闸门；新增 `promoteWaitlist` / `getWaitlistQueue`；`updateStatus` 的 `waiting→booked` 转走 promoteWaitlist。
  - `CourseScheduleService.asgn_student`：改为**复用已有记录**（查重）+ 幂等生成课次（抽出 `generateAppointmentsForBooking`）。
  - `BookingController`：`GET /waitlist/{scheduleId}`、`POST /waitlist/promote`（带 `@Audit`）。
  - `MessageNotifyService`：新增 `notifyWaitlistPromoted`（含首节课时间）。
  - `.workbuddy` 外的前端：`admin-booking.js`（waiting 行「确认候补」→「查询递补」+「拒绝候补」；cancelled 行加「查询递补」；`gotoScheduleForWaitlist` 复用深链）、`admin-schedule.js`（`#waitlistSection` 面板 + `renderWaitlistPanel` + `clickPromoteWaitlist` + 序号守卫 + `reselectScheduleOption`）、`public/courseAndBooking.js`（`fetchWaitlistQueue` / `promoteWaitlist`）、`public/appointmentNotes.js`（候补提示条显示排队序号）。
- **并发安全三件套**（实测 3 轮并发递补均只成功 1 个）：排期行锁 `SELECT ... FOR UPDATE` + **锁定读计数**（普通 `COUNT` 在 REPEATABLE READ 下读的是事务首次读定格的快照，`selectById(bookingId)` 已消耗快照 → 锁内普通 count 仍读旧值 → 并发双双成功）+ CAS `updateStatusIfCurrent`（影响行数 0 = 被抢先）。加锁顺序统一「先 `course_schedule` 行锁、再 `booking` 行」避免死锁。
- **通知一直没发出去的根因**：`message_center.msg_category` **从未登记 `BOOKING_CREATED` / `BOOKING_CONFIRMED` / `LEAVE_CREATED`** → message-service 以业务码 404「消息分类编码不存在」拒收。而 `MessageNotifyService.send` 是 best-effort：**HTTP 是 200，只看"没抛异常"就记「消息自动发送成功」**，所以静默失败了很久（`msg_message` 里这三个分类码 0 条）。修复：① 补登分类（`api/beforeRun/sql/messagecenter-patch-20260911-booking-categories.sql`，`schema.sql` 也同步补 2006~2008）；② `send` 补 **body.code 校验**，非 200 记 warn 并打印后端 message。**这条是"日志骗人"的典型案例，值得记住。**
- **测试**（两套，均真实后端 8083 + 真实 MySQL + 真实 message-service 8090）：
  - `C:/Temp/waitlist-promote-e2e-test.js`：**62 PASS / 0 FAIL**。12 组场景（名额闸门、候补通道、队列次序、无空位时递补被拒、确认取消腾位、幂等、旧入口不得绕过、通知、并发 ×3、指定学生复用、满员拦截、撤销候补）。
  - `C:/Temp/waitlist-frontend-wiring-test.js`：**48 PASS / 0 FAIL**。vm + DOM 垫片跑真源码，A 行内按钮 / B 深链跳转 / C 候补面板 / D 竞态 + 阴性对照 / E 递补（真实落库+通知+1）/ F 深链锁定课程排期 / G 阴性对照。
- **测试抓到一个真缺陷**：`clickPromoteWaitlist` 里 `loadSchedule()` 会重建 `scheduleSelect`、选中复位到「请选择排期」→ 紧接着 `displySchedule()` 因"未选排期"早退 → **候补面板消失、管理员点完递补就丢失上下文**。修复：抽 `reselectScheduleOption(scheduleId)`（同时给深链复用，去掉重复循环），递补后先重选再刷新。G 组阴性对照（删掉重选 → 面板丢失）证明该断言有判别力。
- **测试脚本自身踩的坑（已写进技能）**：
  - **按明文查加密列**：`msg_message.title` 存的是 `HMAC索引:AES密文`，`WHERE title='候补递补成功'` 永远 0 行 → 曾误判成"消息没发出去"。正确做法：按 HMAC-SHA256 索引前缀 `LIKE 'idx:%'` 查，并自行 AES-256-GCM 解密来断言内容。
  - **mysql CLI 的多表别名 DELETE 需要默认库**：`DELETE i FROM db.a i JOIN db.b ...` 报 `ERROR 1046 No database selected` → 改「单表 + 子查询」并写全库名。配套：`execFileSync` 的 `stdio` stderr 要 `pipe` 不要 `ignore`，否则看不到真实报错。
  - **vm 新 context 没有 Node 的 Web 标准类**（`URLSearchParams`/`URL`/`TextEncoder`…），`api.js` 顶层 `InitUserInfo()` 会直接崩 → 必须逐个注入。
  - **必须先加载 `public/api.js`**（`getUserNameById` 等公共函数在那里），且**不能预置 api.js 会 `const` 声明的名字**（`API_BASE_URL`）否则整文件不执行。
  - **DOM 垫片的 `select` 要支持 `appendChild(option)` 并入 `options`**（`loadSchedule` 就是这么填下拉的），只实现 `innerHTML` 解析不够。
- **回归**：`frontend/dist` 已重建并核对产物（`查询递补`、`reselectScheduleOption`、`clickPromoteWaitlist` 等跨文件全局名未被混淆）；改动文件全部 UTF-8 无 BOM。
- **环境遗留**：旧的后端实例（PID 8324，8081 端口，`java -jar target/booking_api-2.0.1.jar`）锁住 jar 导致 clean 失败，已停止；最终只保留修复版在 **8083**。若需 8081，用同一 jar 重启即可。

## 环境观察：后台服务会在回合结束时被回收
- 用后台任务方式启动的 `java -jar`（8083/8090 等）**在 agent 回合结束时会被宿主终止**，日志会戛然而止在最后一条正常 INFO、无任何 ERROR/Exception。看到 task-notification 报 failed 时先看日志尾部，**没有异常就不是崩溃，是回收**，不必排查代码。
- 想长期驻留服务请在自己的终端里启（或在本会话内起、会话内用完）。

## 修订：「查询递补」入口改由「是否占席位」驱动（用户二次拍板）
- **用户原话**：「查询递补」按钮，应该在提出取消预订的一行在管理员「确认取消」或者管理员「撤回」预订确认后等操作处理，**空出已预订席位的情况下**也要显示。
- **理解与取舍**：判据是「**这条记录当前还占不占席位**」，而不是再列一遍状态名。理由：腾出席位的操作今后还会增加，枚举必然漏——`rej-booking`、`frozen` 就是这么漏掉的。
- **改法**：
  - `frontend/js/public/courseAndBooking.js` 新增 **`bookingOccupiesSeat(status)`**（与后端 `BookingStatus.NON_OCCUPYING` 镜像，注释写明必须同步；null/空视为占位）。
  - `frontend/js/admin-booking.js` 的 `formBookingTr`：把 `cancelled/canceled` 分支里内联的「查询递补」删掉，改为**分支之后统一追加** `cardInfo.status !== 'waiting' && !bookingOccupiesSeat(cardInfo.status)`。`waiting` 行保留内联（否则会重复出现两个按钮）。
  - 结果覆盖：`cancelled` / `canceled` / `rej-booking` / `frozen` 都显示；`waiting` 照旧；`booking`/`booked`/`cancelling`/`canceling`/`rej-cancelling` 不显示。
- **顺带挖出并修复一个真实缺陷**：`booking` 表**没有 `is_deleted` 列**，管理员的「删除」= 把 status 置成 `frozen`（前端 `deleteBookingByFrozen` → `operateBookingStatus(id,'frozen')`，同时把该预订的 appointment 一并置 frozen）。而 `frozen` **不在** `BookingStatus.NON_OCCUPYING` 里 → 被当成占位 → **删掉一条预订后席位被永久吃掉，排期一直显示满员、候补再也递补不进来**（与当初 `cancelled` 同类）。已在 `BookingStatus` 加 `FROZEN` 常量并入名单。
  - caller 清点（改前先列，6 处，全是「席位」语义的读判断，加 frozen 安全）：`BookingSeatService:59/98/104`、`BookingService:70/71/103/247`、`CourseScheduleService:541`。
- **「撤回」的语义澄清**（用户提到的操作之一，实测确认）：`booked` 行「撤回」= `confirmOrCancelBooking(id,'booking')` → **删掉该预订全部课次 + status 置回 `booking`（预定待确认）**，**席位不释放**（booking 占位）。所以按「空出席位才显示」的条件它**不**显示入口；走「撤回 → 拒绝」（→ `rej-booking`，不占位）后入口出现，闭环成立。
- **测试（两套全绿，真实后端 8083 + MySQL + message-service 8090）**：
  - `tests/waitlist-promote-e2e-test.js`：**70 PASS / 0 FAIL**（新增第 13 组 8 例：frozen 落库 → 释放席位 → 候补递补成功 → 生成课次 → 发通知 → 席位被占后 frozen 记录撤回成占位被闸门拒）。辅助函数 `occupyingCount` 同步加 frozen。
    - **判别力来源**：13.4「删除后候补可直接递补成功」是**行为级**断言——若 frozen 仍被算作占位，`promoteWaitlist` 必被名额闸门拒 → 本组直接红，与辅助函数是否同步无关。
  - `tests/waitlist-frontend-wiring-test.js`：**66 PASS / 0 FAIL**（原 48 + 新增 A9~A14 共 14 + 阴性对照 H 组 4）。
    - **A12 是护栏**：直接读 `BookingStatus.java`，解析 `NON_OCCUPYING` 名单，与前端 `bookingOccupiesSeat` 的判定结果逐项比对——以后前后端名单任何一边改了另一边没跟上，测试立刻红。
    - **H 组阴性对照**：把内存里的源码副本退回「只认 cancelled/canceled」的枚举写法，验证 `rej-booking`/`frozen` 会失去入口 → 证明 A9 不是恒真断言。
- **产物**：`frontend/dist` 已重建（`bookingOccupiesSeat`、frozen 名单、3 处「查询递补」均在 dist 中）；改动文件全部 UTF-8 无 BOM。
- **环境**：用户的实例跑在 **8081**（PID 19588）并锁住 jar → `clean/package` 会报 rename 失败。**没有动用户的进程**，改用 `mvn -o compile` + `spring-boot:run` 起 8083 做验证。下次要打包需先让用户停掉 8081。
