# 2026-09-18 工作记录

## 决策：暂缓微信多绑实现，先测试验证小程序功能
- 用户前一回合认可"微信一对多绑定"方案（索引改普通索引 + 身份选择器 + 管理员强制密码 + 解绑入口），但本回合改口：**"暂时不要处理微信登录、绑定的处理，只测试验证小程序功能"**。
- 即：多绑 WeChat 实现**暂缓**；当前优先级 = 验证小程序现有功能（排除微信登录/绑定路径）。

## 小程序功能验证（无浏览器，browserless-frontend-itest 思路适配）
- 原生小程序无 DOM，改用 `wx` mock + 可控 request 路由，把真实 `miniprogram/core`+`miniprogram/shared` 拷为 ESM 副本（`tests/miniprogram-itest/_sut`，带 `package.json {"type":"module"}`）用 Node 动态 import 跑真实逻辑。
- 新增 `tests/miniprogram-itest/static.mjs`（静态健康）与 `logic.mjs`（核心逻辑冒烟）。
- **静态 4/4 PASS**：32 个 .js 按 ESM 语法校验(`node --check` 拷 .mjs)、22 个 .json 解析、16 注册页四件套、usingComponents 引用完整（注意：小程序 `/components/...` 相对**项目根**，测试脚本初版误当文件系统绝对路径导致 9 假 FAIL，已修）。
- **逻辑 41/0 PASS**，覆盖非微信全路径：storage 会话往返、requireAuth(无会话延后 reLaunch/有会话直返)、isRoleTenantCodeMatch、密码 login 全流程(写态+触发 term/map 与 tenant/industry)、goHome 角色路由、loadTermMap(tokenOnly 不带 Bearer——正是 term/map 401 修复的客户端前提)、服务端词表覆盖+行业切换、terms 多行业兜底、constants 角色映射、message 收件箱+previewText/fmtTime、acquisition 首次触达不覆盖、401 通用处理(清会话+onAuthFail)。
- **关键约束断言通过**：全程未触达 `/auth/wechat-login`、`/auth/bind-wechat`；wechatSilentLogin/bindWechat 仅导出未调用——满足"排除微信登录/绑定"。
- 说明：本验证为 hermetic（mock 后端），证明**小程序代码逻辑健康**；与真实后端的联调（密码登录拿 token、term/map 免登录 200、消息中心）需在微信开发者工具/真机对"已部署新后端的 API"验证；term/map 的 401 修复需把重编译的 api 部署后才生效。
- 运行：`C:/Users/Administrator/.workbuddy/binaries/node/versions/22.22.2-2/node.exe tests/miniprogram-itest/static.mjs` 与 `logic.mjs`。

## 修复：登录后不跳转 + 仍有 wechat-login 调用
- 现象（开发者工具）：密码登录成功却没跳到对应首页，且登录页网络里持续出现 `/auth/wechat-login` 请求。
- 根因：
  1. `request.js` 对 `tokenOnly`（公开/匿名接口）请求遇 401 仍 `clearSession()`+`onAuthFail()` 跳登录页；`login()` 成功后立即 `await loadTermMap()`（term/map），若后端 term/map 返回 401（未部署我们之前 401 修复的旧后端），会把刚写好的登录态清掉并 reLaunch 回登录页 → "登录后没跳转/跳回登录"。
  2. `login()` 把术语加载 `await` 在主流程且无 try/catch，任一接口失败即中断整个登录。
  3. `login.js onLoad` 调 `tryWechatAutoLogin()` → `wechatSilentLogin()` → `/auth/wechat-login`，而用户明确暂不处理微信登录。
- 修复（4 处）：
  1. `core/request.js`：tokenOnly 请求遇 401 不再清登录态/不跳登录页（`if (status === 401 && !opts.tokenOnly)`），仅非 tokenOnly 的真实鉴权 401 才踢人。
  2. `core/auth.js login()`：loadTermMap / syncIndustryFromTenant 包独立 try/catch 静默，失败绝不阻断登录成功与跳转。
  3. `core/term.js`：term/map 保留 `tokenOnly`（公开接口，由修复1免疫 401）；tenant/industry 去掉 tokenOnly（带 Bearer，需租户上下文）。
  4. `pages/login/login.js`：移除 onLoad 的 `tryWechatAutoLogin()` 自动调用与 onLogin 里的 `bindWechat()` 调用；`login.wxml` 用 `wx:if="{{false}}"` 临时隐藏"允许微信登录"勾选框（恢复只需删该条件）。
- 验证：新增 `tests/miniprogram-itest/login-flow.mjs`（12/0 PASS），覆盖 term/map 200 与 401 两种场景——均确认 login 成功、登录态保留、goHome→/package-student/home/home、不发 wechat-login；重跑 `logic.mjs` 41/0 无回归。
- 结论：无论后端 term/map 是否已部署 401 修复，前端这版都不再被 401 踢回登录页（双保险）。仍建议在开发者工具对"已部署新后端"联调确认 term/map 免登录 200 与首页渲染。

## 重构：package-admin 页面按网页版 admin.html（租户管理员）重新组织
- 用户指出：小程序把**平台管理员**的统计管理（平台级 `tenantCount` 等跨租户汇总）错配给了**租户管理员**页面；要求按网页版 `frontend/admin.html`（租户管理员功能）重组 `package-admin`。
- 对照（来自 admin.html / platform_admin.html 菜单）：
  - 平台管理员(platform_admin.html)：平台总览/租户管理/用户管理/套餐管理/**运营统计**(平台级)/系统设置/消息中心/敏感词 + 系统维护。
  - 租户管理员(admin.html)：系统概览→**数据总览**；排期与预订→课程排期/预订审核；预约检视→上课通知；用户管理→教师管理/学生管理；消息中心→消息中心/敏感词管理；课程管理→课程模板/课程列表；系统维护→数据维护/审计日志。
  - 租户"数据总览"取数均为**租户级**（经租户插件按当前 token 的 tenantId 隔离），含 总教师/总学生/总课程/本月预定/本月课时 + 待审核(预约待确认/取消待确认/今日课程)。
- 改动：
  1. `shared/apiPaths.js` 新增租户级接口：`STAT_USER_BY_MONTH`/`STAT_COURSE_BY_MONTH`/`STAT_BOOKING_BY_MONTH`/`STAT_APPOINT_BY_MONTH`/`STAT_APPOINT_ON_DAYS`(均 `/api/v1/.../statistical/byMonth|onDays`)、`USER_PAGE`(`/api/v1/user/page`)、`COURSE_TEMPLATE_LIST`(`/api/v1/course/template/list`)。保留旧 `DASHBOARD_OVERVIEW`/`DASHBOARD_TENANT_USAGE`（已不被 dashboard 使用）。
  2. `package-admin/dashboard` 重写为**租户数据总览**（核心修复：去掉平台级 tenantCount 泄漏，改拉租户级月度统计 + 待审核计数；待审核用 `BOOKING_PAGE` 取 `total`）。
  3. `package-admin/home` 重写为**功能枢纽**：按 admin.html 七大分组渲染功能卡片 → `navigateTo` 到各功能页（含教师/学生管理复用 `user-list?role=`）。
  4. 新建功能页（均 `customErrorMsg:false` 自行兜底）：`user-list`(教师/学生,`?role`)、`course-list`、`schedule`(课程排期)、`booking-audit`(预订审核，`/course/booking/updateStatus` 载荷 `{id,status}`，状态串 booking/cancelling/booked/rej-booking/cancelled/rej-cancelling)、`course-template`、`sensitive`(词库+`/sensitive/test` 检测)、`lesson-notice`(已确认 booked 近期上课+提醒建设中)、`data-maintain`/`audit-log`(建设中占位)。
  5. `app.json` 在 `package-admin` subPackages 注册全部 11 个页面。
- 验证：`tests/miniprogram-itest/static.mjs` 4/4 PASS（41 .js / 31 .json / 25 页四件套 / 组件完整）；`logic.mjs` 41/0 无回归。
- 边界：列表页字段名按网页端推断做防御式映射（`item.x || ''`），与真实后端联调时若字段命名不一致需微调；`lesson-notice` 的批量提醒、`data-maintain`、`audit-log` 为占位，待后端能力接入；`dashboard` 依赖的租户级统计/待审核接口需**已部署新后端**才返回数据（旧后端无这些端点会优雅降级显示 `-`/0）。

## 审计：课程预约名额并发超卖排查
- 方法：穷举全工程 booking 写操作（结果仅 10 处，全在 BookingService(8) / CourseScheduleService(2)，controller 层无直连 mapper）→ 逐条核事务注解、加锁顺序、计数口径 → 查 DDL 索引兜底 → 连真库查现存脏数据 → git log 定位闸门引入时间区分"历史遗留 vs 现网缺陷"。
- **现有防线有效**（闸门 2026-09-11 14:39 提交 857a233 引入）：5 条占位写路径全走 `BookingSeatService.assertSeatsAvailable`；排期行锁 `selectByIdForUpdate`（courseScheduleMapper.xml:132）+ booking 锁定读 `selectOccupyingBookingIdsForUpdate`（BookingMapper.xml:206，刻意不写 COUNT+FOR UPDATE）；加锁顺序统一"排期行→booking 行"（已穷举无反向路径）；递补用 CAS `updateStatusIfCurrent`。
- 排除的误判：`countBookingByScheduleIdIgnoreTenant` 只用于 2 处只读展示（getAvailableSchedule / getSchedulesByTeacher），未进写路径；`/api/v1/course/booking/*` 不在 SecurityConfig 白名单 → 必有租户上下文，不存在被拼 tenant_id=-1 导致计数恒 0 而超卖。
- **闸门未覆盖的 3 个操作面（真缺口）**：
  1. P0 `updateScheduleSites`（CourseScheduleService:225 + xml:99）`SET available_sites = available_sites + inc` 无下界、无占位校验；字段是 tinyint(1) 可为负；已占 4 时改小到 3 → 超卖，非并发即可触发。
  2. P1 排期编辑 `update(ScheduleCreateDTO)`（:214）+ `CreateDtoToObject:354`：DTO 的 availableSites 是 Integer 而实体是 int（null 会拆箱 NPE）；传小于已占位数经 `updateById` 直接覆盖 → 超卖。
  3. P1 `BookingService.create`（:41）无"同一学生同一排期"去重（对照 asgn_student 有 selectLatestByScheduleAndStudent）；双击/重试即同学生占两位；booking 表无 (schedule_id,student_id) 唯一索引兜底。
- 次要：`asgn_student` 的 check-then-insert 竞态（selectLatest 普通读在排期锁之前，锁覆盖不到 check）；`updateById`/`updateStatus` 无 CAS（丢失更新，窗口窄）；course_schedule 无 `available_sites >= 0` CHECK。
- **现存脏数据仅 1 条**：schedule_id=`242968bd3414437aa4aef009a687daf9`（tenant=2 "A排期1"，available_sites=1，实际 booked=2，两学生 09-02 14:23 / 09-04 20:47，tenant 均为 2 一致）→ 早于闸门上线 7 天，判定**历史遗留**。全库无同学生重复占位；available_sites 分布仅 1(1054 个排期)/5(2 个)，无 0 或负值。

## 修复：名额超卖 P0/P1/P2（按序落地，已编译验证）
- **P0 席位调整过闸门**：`BookingSeatService` 新增 `assertSitesNotBelowOccupied(scheduleId,newSites)`（负值拒绝 + 排期行锁 + 锁定读计数 + `newSites < occupied` 拒绝）；`updateScheduleSites` 先取锁算 `current + inc` 再校验，并判 `rows==0` 视为失败；`courseScheduleMapper.xml` 的 updateSites 补 `AND available_sites + #{inc} >= 0` 纯 SQL 下界。
- **P1 排期编辑**：`update()` 先 `lockScheduleAndAssertExists`；dto 未传席位则回填库中现值（不让 `CreateDtoToObject` 的兜底默认值落库）；显式改小则校验不低于已占位。`CreateDtoToObject` 的 `setAvailableSites` 加 null 兜底为 1（原为 Integer→int 拆箱 NPE）。
- **P1 create 去重**：`BookingService.create` 改为 先取排期锁 → `selectLatestByScheduleAndStudent` 查重 → 已占位则拒绝、非占位则复用该行转正（与 asgn_student 语义对齐），不再无脑新插。status 为空时显式落 booked（updateStatus 无默认值兜底，传 null 会撞 NOT NULL）。
- **P2 asgn_student**：`lockScheduleAndAssertExists` 提到 `selectLatest` 之前，查重进入锁保护期。
- **P2 CAS**：`update` 的 status 变更走 `updateStatusIfCurrent`，rows==0 抛「已被他人修改，请刷新后重试」，随后把 `booking.status` 置 null 防 `updateById` 二次覆盖（MyBatis-Plus 忽略 null）；`updateStatus` 同样改 CAS。create 复用分支与 asgn_student 复用分支都在锁内且已有查重，无需 CAS。
- **P2 DDL（已执行到本地库）**：脚本 `api/beforeRun/sql/seat-oversell-hardening-20260918.sql`，含前置自检。已落地 `uk_booking_schedule_student(schedule_id,student_id)` 唯一索引、`course_schedule_chk_sites CHECK(available_sites >= 0)`、修正 `available_sites` 列注释为「可预约席位总数」。
- **验证**：`build_smoke.sh compile` BUILD SUCCESS（无新增告警）；两个 mapper XML 用 minidom 解析 OK（compile 不校验 XML）；class 字节码断言含 `assertSitesNotBelowOccupied`/`updateStatusIfCurrent`；information_schema 核验索引/CHECK/列注释全部落地。
- **调用方（API 行为有变更）**：`POST /api/v1/schedule/incSite`（ScheduleController:65）、`POST /api/v1/course/booking/{create,update,updateStatus}`。全项目检索 `SCHEDULE_INC_SITE` 在网页端与小程序**均只定义未调用**，故这些行为变更暂无前端受影响，但接口本身可被直接调用（本次修复正是要堵住它）。
- **遗留**：`available_sites` 仍是 `tinyint`（上限 127），将来开设 >127 人大班课需先改 `smallint`；历史脏数据那 1 条未处置，处置选项见脚本文件末尾注释。
