# 2026-09-26 工作日志

- 学生端「课程预订」卡片增强（frontend/js/student-bookingCards.js）：课程无已发布排期时，卡片隐藏「点击查看排期」占位文案，改为「待排期，可联系管理员」按钮；点击调用 window.openComposeMessage() 打开站内信发送页（学生默认 scope=tenant_admin 本租户管理员），预填标题/诉求正文模板，学生可编辑后发送。实现：loadAndRenderCourse_student 内并行 fetchScheduleList(cid,'active') 判定 __hasPublishedSchedule（会话内 Map 缓存 _courseSchedulePublishCache）；renderCourseCards 据标志渲染按钮（stopPropagation 防误触卡片选中）；新增 contactAdminForSchedule() 并挂 window。node build.js 重建 dist + cp 同步 api/beforeRun/dist，两副本字节一致。
- 学生端「课程预订」卡片点击滚动优化（student-bookingCards.js）：新增 scrollToScheduleSelect()，selectCourse 点击卡片后把视口滚动目标从整个「排期信息」区块(.section)改为「选择排期」那一行（getElementById('scheduleSelect').closest('.form-line') 后 scrollIntoView smooth）。深链路径仍滚到 .section（保留完整排期信息展示）。node build.js 重建 dist + cp 同步 api/beforeRun/dist，两副本字节一致，压缩产物已核对 scrollIntoView/closest 逻辑正确。
- 学生端「今日课程」t-cancelled 状态文案（public/appointmentNotes.js）：checkAppointmentStatus 新增 t-cancelled 分支——学生端显示「已取消（<教师行业词>）」（termText('teacher') 转换，如律师行业显示「已取消（律师）」）；教师/管理员显示「<教师行业词>已取消」，顺带修复原先返回原始枚举串 t-cancelled 的显示 bug。node build.js 重建 dist + cp 同步 api/beforeRun/dist，两副本字节一致。
- 全前端 t-cancelling/t-cancelled 显示统一行业词（4 文件 6 处）：t-cancelling 显示改为『申请取消（教师）』、t-cancelled 显示改为『已取消（教师）』，其中『教师』均经 termText('teacher') 行业词转换。涉及：public/appointmentNotes.js 的 checkAppointmentStatus（t-cancelled 由角色分支统一为全角色括号式）；student-bookingBrowserCards.js 的 getAppointmentStatusLabel；admin-AppointmentNotes.js 管理员筛选下拉选项（模板字面量 ${termText('teacher')}）；public/datamaintain_delete.js 的 checkStatus_schedule（t-cancelling 原误映射'已取消'已修正）。其余出现均为逻辑值（option value=/status 比较/传参/isNotifyActionable 列表）未动。node build.js 重建 dist + cp 同步 api/beforeRun/dist，4 文件两副本字节一致。
- 未登录学生师资展示落地页（确认项落地）：新增 frontend/student-landing.html（独立公开落地页，免登录，不破坏 student.html 登录守卫）+ frontend/js/teacherCardCarousel.js（教师职业信息横向滚动卡片组件，纯 CSS scroll-snap + 自动缓滚/hover 暂停/左右箭头）。
  按确认三项：①容器=新建独立公开落地页；②数据=先做前端原型（MOCK_TEACHERS，loadTeacherCards 内预留 GET /api/v1/teacher/published/public-list 真实接入位，免登录接口需按 public-endpoint-tenant-bypass 处理）；③点击卡片=跳登录页（index.html?from=landing&tid=，登录后回课程预订）。
  登录页 index.html 注册按钮旁新增「先浏览师资 ›」入口链到 student-landing.html；landing 支持 ?industry=lawyer 直接切前端行业词（绕过需 token 的 syncIndustryFromTenant）。
  node build.js 重建 dist（student-landing.html 与 teacherCardCarousel.js 均进 dist）+ cp 同步 api/beforeRun/dist，入口已在两副本 index.html 注入。
- 师资展示页支持登录态（扩展，确认项：侧边栏菜单项+跳公开详情页）：
  * student-landing.html IIFE 检测 localStorage.getItem('token')，已登录时把 header 的登录/注册替换为「前往工作台」(student.html)+「退出登录」(window.handleLogout 清 token 跳登录页，降级手动清)；未登录维持原样。
  * teacherCardCarousel.js 的 onTeacherCardClick 改为登录态分支——已登录跳 teacherPublishedProfile.html?id=<profileId>（mock 加 profileId 字段，真实接入时传 publishedProfileId），未登录跳 index.html?from=landing&tid=；卡片点击由传 teacherId 改为传索引 window.__onTeacherCardClick(i)。
  * student.html 侧边栏「我的工作台」组新增「浏览师资」菜单项(key=teacher_showcase)，loadAdminPageContent 的 switch 加 case 跳 student-landing.html，登录后学生可从工作台进入师资展示页。
  * node build.js 重建 dist + cp 同步 api/beforeRun/dist，三文件(landing/student/carousel)字节一致。
