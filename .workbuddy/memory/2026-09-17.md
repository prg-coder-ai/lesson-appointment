# 2026-09-17 工作记录

## 小程序登录页「标题在、正文无」根因定位与修复
- 现象（续 09-16 空白排查）：登录页导航栏标题"登录"正常显示，但输入框/角色选择/按钮整块不渲染。
- 深查：login.wxml/wxss/js/json 均无 BOM、合法 UTF-8；app.wxss 全局样式无隐藏正文规则；WXML 语法合法；import 链无模块级 throw；login.json 正常（故标题显示）。
- 真因定位：`pages/login/login.js` 第 6 行在**模块顶层裸调** `const app = getApp();`，而 project.config.json 开了 `"enhance": true`（glass-easel）。全代码库仅此一处未加守卫，core/request.js、core/auth.js 均用安全写法 `(typeof getApp === 'function') ? getApp() : null`。增强模式下页面模块求值期 getApp 可能未就绪，顶层裸调若抛错 → Page() 不注册 → 正文不渲染、标题（来自 login.json 独立加载）照常。
- 修复：删除顶层 `const app = getApp()`；onLoad 内改为 `const app = (typeof getApp === 'function') ? getApp() : null;` 并 `if (app && app.globalData)` 守卫；clearBinding 内 app2 同样改为安全守卫。与 core/* 风格统一。
- 验证：`node --check`（拷为 .mjs）SYNTAX_OK。
- 仍有残留风险：① 目录重构后开发者工具编译缓存脏数据（工具→清除缓存→全部清除→重编）；② 若修复+清缓存后仍空白，需用户提供调试器 Console 具体报错文本（看 WXML 编译错误 / Component not found / JS 运行时错误）再精确定位。
- 建议：其余页面/home/booking 等均无顶层 getApp 裸调（grep 全仓仅 login.js 有），不必改；但后续新增页面遵循「getApp 仅在 onLoad 内、且用 typeof 守卫」约定。

## 官方优化：启用按需注入 + 用时注入（lazyCodeLoading）
- 依据微信官方「小程序启动性能」提示，在 `miniprogram/app.json` 加 `"lazyCodeLoading": "requiredComponents"`（基础库 2.11.1+ 按需注入；2.11.2+ 用时注入生效）。
- 组件声明审计（官方注意事项要求移除未使用声明）：grep 全仓 9 处 `role-tabbar` 声明（mine/student-home/teacher-home/courses/profile/my-booking/booking/admin-home/dashboard）在各自 WXML 均真实使用 `<role-tabbar>` 标签，**无"声明未用"可删**；app.json 无全局 usingComponents（无"全局低使用率组件"问题）。审计通过，无需改任何页面 JSON。
- 影响：login 页纯原生 form、无自定义组件，不受该配置影响；role-tabbar 为各 tab 页高使用组件，注入效果良好。
- 说明：「用时注入」需额外为组件配 占位组件 才生效，本次未配置（按需注入已满足优化目标），未来若要进一步延迟注入再单独加。

## 修复：routeDone with a webviewId is not found（Page route 系统错误）
- 现象：Console 报 `SystemError (appServiceSDKScriptError) "[Page route 错误(system error)] routeDone with a webviewId 1731 is not found"`，lib 3.17.3 / DevTools 2.02.2608070。
- 根因：`core/auth.js` 的 `requireAuth()` 在无登录态时**于 onLoad 同步调用 `wx.reLaunch`**（首页/各页 onLoad 第一行即调 requireAuth）。页面 webview 刚创建、尚在注册，reLaunch 就把其拆掉并回发 routeDone，框架找不到该 webviewId → 系统错误。属路由生命周期竞争，通常非致命（reLaunch 仍完成、能落到登录页），但噪声大且与开启 lazyCodeLoading 后初始化时序变化更易暴露。
- 修复：`requireAuth()` 内把 reLaunch 包进 `setTimeout(() => wx.reLaunch(...), 0)`，延后到 onLoad 返回、webview 注册完成后再跳；一次性覆盖所有 onLoad 调 requireAuth 的页面。logout()/switchTenant() 的 reLaunch 由用户点击触发（非 onLoad），不竞争，未改。
- 验证：`node --check`（拷为 .mjs）SYNTAX_OK。
- 提示：该错误与「登录表单不显示」是两个独立问题；表单仍取决于 login.js 顶层 getApp 修复 + 清编译缓存。

## 真正根因（覆盖上两条假设）：登录页 onLoad 自 reLaunch 死循环
- 复核：用户贴 `routeDone webviewId not found` 后通读登录链路，发现 `pages/login/login.js` onLoad 调 `requireAuth()`，而 `requireAuth()` 无登录态时 `wx.reLaunch('/pages/login/login')`——即**重定向回登录页自身**，无限自重开循环。
- 后果：登录页 webview 不断销毁重建，表单来不及稳定渲染（"标题在、正文无"空白）；每次重建的 routeDone 指向正在被拆的 webview → 持续 `webviewId not found`。**这才是**空白与路由错误的真正根因；此前 getApp 顶层裸调(已修)、reLaunch 延后(已修)属治标/旁支。
- 修复：login.js onLoad 改用 `getSession()` 直接判已登录（已登录→goHome，未登录→正常渲染表单），不再调会 reLaunch 自身的 requireAuth；删其未使用 import。requireAuth 仍导出、受保护页继续使用，逻辑正确。
- 同步：登录页加隐私协议同意勾选（gate 登录按钮，openAgreement 占位），满足上线合规。
- 验证：node --check LOGIN_SYNTAX_OK；auth.js 仍导出 requireAuth。包体：主包 124KB / 分包 ~231KB，远低于 2MB 上限。

## 小程序合规审查（上线清单）
- HTTPS 强制【P0 上线前置】：app.js apiBase/msgBase 现为 `http://152.136.254.127:8080|8090`；微信生产 wx.request 必须 HTTPS+有效证书。上线前替换并后台配置 request 合法域名白名单。dev 期靠"不校验合法域名"可跑。
- 隐私合规【P1】：登录页已加同意勾选；真实《用户协议》《隐私政策》页+微信后台隐私协议网址待补。
- 包体【通过】：主包 124KB < 2MB。
- 可选增强：接入 wx.login 微信身份登录以"获得用户"（当前为账号/密码+tCode 的多租户 SaaS 登录，属合理设计）。
