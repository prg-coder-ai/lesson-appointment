# 2026-09-08 工作记录

## 修复：租户登录后 admin 页报 testData.js 找不到
- 根因：`frontend/admin.html`（源 + `dist`）的 `window.onload` 有一行 `window.makeTestPage = makeTestPage;//test`，
  而 `makeTestPage` 定义在 `js/test/testData.js`。该文件是开发测试工具（"测试-批量创建数据"批量建用户），
  构建产物 `dist` 不含它（build.js 排除 `js/test/`，且 L1 清理本就计划删除），导致 `makeTestPage` 未定义、
  `onload` 抛 `ReferenceError: makeTestPage is not defined`，租户 admin 页初始化中断。
- 修复：从 `frontend/admin.html` 与 `frontend/dist/admin.html` 移除 ①菜单项"测试-批量创建数据" ②`case "test_data"` 分支
  ③`onload` 里的 `window.makeTestPage = makeTestPage;//test` ④`<script src="js/test/testData.js">` 引用；
  并删除 `frontend/js/test/testData.js`（符合 L1 清理意图，消除死代码）。
- 验证：`grep` 两 admin.html 已无 testData/makeTestPage/test_data 残留引用。
- 部署提示：Nginx 以 `frontend/dist` 为 root 直接伺服业务前端，改动即时生效，浏览器硬刷新即可；
  api jar 不伺服 `admin.html`（只伺服平台管理端 platform-admin.html），重打 jar 时 build-platform.js 会把 dist 重新拷入 static，保持一致。

## 修订：保留批量工具，但发行版不显示菜单项
- 用户要求：保留 `js/test/testData.js`（开发批量建数据工具），但发行版（dist）不显示菜单项 `key="test_data"`。
- `git restore` 恢复此前删除的 `frontend/js/test/testData.js`（git 提交 00ace80 仍跟踪）。
- 根因呼应：build.js 第30/105行本就把 `js/test/` 排除出生产构建（"测试目录不进生产构建"），故 dist 天然不含该脚本。
- 方案（运行时判定，零构建改动）：`admin.html` 的 `window.onload` 改为 `if (typeof makeTestPage === 'function')` 才挂
  `window.makeTestPage` 并显示 `#menu-test-data`，否则隐藏该菜单项；`case "test_data"` 也加 `typeof` 保护。
  - 源 `frontend/admin.html`：保留 `<script src="js/test/testData.js">` + 菜单 → 开发态前端直接可用。
  - 部署版 `frontend/dist/admin.html`：菜单加 id 但**不**加脚本标签（避免 404），运行时 `makeTestPage` 未定义 → 菜单自动隐藏、不再报错。
- 验证：`grep` 两文件均无裸 `window.makeTestPage = makeTestPage;//test`，源有脚本标签、dist 无。
- 说明：发行版(dist)按现有设计不打包 testData.js，故该工具仅在开发态（源前端）可用；若需在生产部署版也可用，
  需改 build.js 放行 `js/test/` 并加 `?dev=1` 之类的菜单显隐开关——已向用户提此选项。

## 文档：三个密钥在前端/api/信息中心的配对关系（写入部署手册 6.2）
- 代码核对结论：
  - 前端（dist）**零密钥**：`grep` frontend 无 `JWT_SECRET`/`CRYPTO_*` 引用；前端只存 token、请求头转发，不签发/验签/加解密。
  - `JWT_SECRET` → `jwt.secret`：`JwtUtil` 按 UTF-8 字节读、长度≥64（HS512/HS256）。api 签发+校验；message-service 仅校验（共享密钥域，见 `MessageAuthInterceptor`）。**booking 与 message 必须相同**。
  - `CRYPTO_AESKEY` → `crypto.aes-key`：`CryptoUtil` Base64 解码须正好 32 字节（AES-256-GCM）。
  - `CRYPTO_HMACKEY` → `crypto.hmac-key`：32 字节 HMAC-SHA256 搜索索引。
  - AES/HMAC 两服务复制同一 `CryptoUtil`、共用 `message_center` 库，跨服务解密依赖密钥相同。
- 重要安全发现：`api/src/main/resources/application.properties` 与 `message-service/.../application.properties` **当前已明文写死且生效**三个密钥（AES/HMAC 值与用户示例一致，JWT_SECRET 两处相同）。属"已入库"，生产须用 env 覆盖并建议轮换。
- 交付：重写部署手册 `doc-develop/预约系统云服务器部署和维护手册.md` 6.2 节（6.2.1 配对表 + 机制依据、6.2.2 仓库状态提醒、6.2.3 Ubuntu+Windows 写入命令），并新增 `doc-develop/booking.env.template`、`doc-develop/message.env.template` 两个可 scp 的 env 模板（含配对注释）。

## 文档：部署手册新增 7.5 节（仅 IP 无域名的临时部署）
- 背景：用户按第 7 章（依赖子域 + Let's Encrypt）操作，但暂只有公网 IP、无域名。
- 补充 7.5 节，说明两处障碍：① `server_name` 改 catch-all（`server_name _;`）；② certbot HTTP-01 需域名做 ACME 校验，无法用。
- 给出两套方案：
  - 方案 A 纯 HTTP：`listen 80 default_server; server_name _;`，root dist + /api/v1 分流；管理端走 `http://<IP>:8081/platform_admin.html`（防火墙仅放管理员 IP）；前端 `window.ADMIN_ORIGIN` 默认 `:8081` 无需改、无需重构建。仅限内测/测试数据。
  - 方案 B 自签 HTTPS：含 IP 的 SAN 自签证书（`openssl req -addext "subjectAltName=IP:<IP>"`），Nginx TLS 终结反代 8081；浏览器报 `ERR_CERT_AUTHORITY_INVALID` 属必然现象、非配置错误；移动端/微信 webview 可能拒绝，仍只适合内测。
- 升级路径：备案域名后把 `server_name _;` 换子域 + `certbot --nginx` 平滑切公信任 HTTPS；JWT/AES/HMAC 与传输层无关、无需改动。

## 重打包最新 booking_api jar 供上传云服务器（13:33 完成）
- 背景：云服务器 152.136.254.127:8081 跑的是旧 jar（SecurityConfig 白名单未放行 /api/v1/*，所有接口 401，前端 auth.js 把 401 统一提示"密码不对"）；本机最新 saas 代码白名单正常（已对比验证）。登录密码用 BCrypt，与 jwt.secret/AES/HMAC 均无关——密钥配错不是根因。
- 操作：先停本机 8081 旧实例释放 target 文件锁 → Maven Launcher 直启（unset CLASSPATH/NODE_OPTIONS）`mvn package -DskipTests`。
- 产物：`api/target/booking_api-1.0.0.jar`（41MB，BUILD SUCCESS，分支 saas，含最新 SecurityConfig/authController 与 platform-admin 静态资源）。
- 交付给用户：上传替换服务器 jar → `systemctl restart booking` → 验证 `curl -X POST /api/v1/auth/login` 返回非 401（200/404 即修复）。

## 排查：云服务器登录报"密码不对"（152.136.254.127:8081）
- 现象：浏览器登 `http://152.136.254.127:8081/index.html`（平台管理端统一登录入口）报"密码不对"。
- 根因（实测对比，非密钥问题）：**云服务器运行的 booking jar 是旧版本**——其 SecurityConfig 白名单未放行 `/api/v1/*`，导致所有 `/api/v1/*`（含白名单的 /api/v1/auth/login、/api/v1/tenant/name）都返回 401（被 Security EntryPoint 拦）；前端 auth.js 第153行把 401 统一提示成"账号或密码错误" → 用户看到"密码不对"。
- 对比证据：本机 8081（最新 saas 代码，PID 29480）`/api/v1/auth/login` 不存在账号返回 `{"code":404,"message":"账号不存在"}`（进业务）、`/api/v1/tenant/name` 返回 200；云服务器同样路径全 401。→ 服务器 jar 旧，代码层面无需改。
- 前端登录路径正确：auth.js `login()` 用 `/auth/login`，utility_request.js 第29-31行把裸路径拼成 `/api/v1/auth/login`，与 authController 类 `@RequestMapping("/api/v1/auth")`+`@PostMapping("/login")` 一致。
- 密码与 jwt.secret 无关：UserService.login 用 BCrypt（`passwordEncoder.matches`，267行），自包含哈希带盐，不依赖任何密钥；CryptoUtil(AES/HMAC) 只加密 phone/email/name（70-90行）。
- booking.env 三个密钥（JWT/AES/HMAC）配置错误**不会导致**登录 401：登录是无 token 请求，不经过 JWT 验签；AES 错最多让 decryptUserFields 抛异常（非"密码错误"）。属无关变量。
- 修复：用最新 saas 代码重新 `mvn package` api（先停运行中 jar 避免 target 锁），把新 `booking_api-1.0.0.jar` 上传服务器替换并 `systemctl restart booking`；验证 curl `/api/v1/auth/login` 不再 401。

## systemd service 模板 bug 修复（14:34）
- 报错：`/etc/systemd/system/message-service.service:20: Unknown key name 'StartLimitIntervalSec'`。
- 根因：`StartLimitIntervalSec` / `StartLimitBurst` 是 `[Unit]` 段指令，模板原先写在 `[Service]` 段，systemd 不识别 → 报错（配置被忽略，不致命但应修）。
- 修复：`api/beforeRun/message-service.service` 与 `booking.service` 均已把两行移入 `[Unit]`。服务器上 `/etc/systemd/system/*.service` 需同步改（用户手动）+ `systemctl daemon-reload`。

## 新增：本地前端连远程 API 开发代理（15:0x）
- 需求：人本地改 frontend 源码、后端已在云服务器 152.136.254.127(:8081 + 消息 :8090)，要本地浏览器连真实远程数据联调。
- 原理（代码依据 utility_request.js）：默认 API_BASE_URL='' 走相对路径；normalizeUrl 把 /xxx 补 /api/v1；**axios 对以 / 开头的 url 忽略 baseURL** → 设 window.API_BASE_URL 指远程主机无效。唯一稳妥=同源+反代。
- 交付：`doc-develop/dev-frontend.js`（零依赖 Node http 代理：静态伺服 frontend/ + /api/v1 反代到 :8081、/api/v1/{message,sse,users/*} 到 :8090，SSE 关缓冲）；`start-frontend.sh`/`.cmd` 启动器。
- 实测：本机跑脚本连 152.136.254.127，admin.html=200、/api/v1/tenant/name=200、登录探针=404(进后端逻辑)，全绿。
- 手册：`doc-develop/前端部署与维护手册.md` 新增第 6 章（原理/前置/运行/验证/排错/脚本要点）。
- 修正：用户用 5173 登录后前端按 `api.js` 第 2-3 行 `FRONTEND_ORIGIN='http://'+location.hostname+':8080'` 跳 8080，而本机 8080 空闲→跳空。已把 `dev-frontend.js` 默认端口从 5173 改为 **8080**（DEV_PORT 可覆盖），与前端默认端口对齐形成登录闭环；手册 6.7 新增「登录跳转闭环」说明。实测 8080 版全绿。

## 修复：前端 mvn package 报 terser 语法错误
- 现象：用户改前端后 `mvn package`（frontend 模块）BUILD FAILURE。
- 根因：`frontend/js/admin-user.js` 第 22 行在模板字符串 `${...}` 内误用 JSX 写法 `${role=="teacher"?<span ...>教师</span>:<span ...>学生</span>}`，vanilla JS 不支持 JSX → terser `SyntaxError: Unexpected token: operator (<)`。
- 该错误在提交 `4b6be20 --完善--词汇，但报错` 已存在（git diff 空，工作区与之完全一致）。
- 修复：三元两分支加引号成字符串字面量 `'<span ...>'`，插值结果不变（仍渲染 HTML）。重跑 terser 逐文件校验全过；frontend `mvn package` BUILD SUCCESS，dist 重新生成。
- 注：业务前端改动打 `frontend` 模块产出 dist（供 Nginx）；api 模块只混淆平台端（build-platform.js），不含业务前端 admin-user.js。

## 改造：语言切换从右下角悬浮条改为头部下拉菜单
- 需求：把"语言切换"做成下拉菜单，放在头部区域 `<i class="fa fa-sign-out-alt">`（退出按钮）左侧；并运行测试。
- 改动文件：`frontend/js/public/termsFunction.js`
  - `setLang()`：删掉失效的 `[data-lang-btn]` 高亮逻辑，改为 `window.dispatchEvent(new CustomEvent('langchange'))`，便于下拉菜单/控制台调用都同步 UI。
  - `injectLangSwitch()`：整段重写。自注入 `<div id="lang-switch-dropdown">`（toggle 显示当前语言 + `<ul>` 菜单 zh/en/fr），首次注入时写 `<style id="lang-switch-style">`（与页面解耦）。
  - 放置位置优先级：①角色页（student/admin/teacher）→ `header-actions` 内、退出按钮左侧（insertBefore）；②有 `.header` → 追加到 header 末尾；③登录页（index，无 header/退出）回退为右上角固定（加 `.floating`）。
  - 交互：点 toggle 展开/收起（stopPropagation 防误关），菜单项 click 调 `setLang` 后收起并 refresh，document 点击外部收起，监听 `langchange` 刷新高亮与当前语言文案。
- 测试：环境无浏览器，用 jsdom（managed node workspace 装，`--registry https://registry.npmjs.org --strict-ssl false`）加载真实 terms.js+termsFunction.js + mock `/term/map`，写 `doc-develop/test_lang_switch.js`。
  - 场景1 角色页 14 项 + 场景2 登录页回退 3 项，**共 17 项断言全通过**：下拉插在退出按钮左侧、点击 en/fr 经 setLang→fetch→applyTerms 把 data-term 文本与 data-term-placeholder 切换为对应语言、当前语言高亮、toggle 展开收起、登录页回退 floating 且不报错。
- 说明：dist 由 `mvn package`(frontend 模块) 重建，本次仅改源 js，需重打 frontend 模块才会进 dist；测试针对源 js（真实代码）已足够证明功能。

## 修复：admin-user.js 编辑弹窗 isTeacher 未带入（BUG）
- 现象：用户编辑页面角色信息（isTeacher）没带入，编辑弹窗打不开/标题错。
- 根因（代码级，已定位）：`openEditUserModal(userId)` 用到 `isTeacher`（第355行），但该函数内**从未声明** `isTeacher`（只在 `openAddUserModal` 第267行声明，函数局部 `const`，不跨函数可见）。
  - 后果1：引用未声明且非全局的 `isTeacher` → `ReferenceError`，整段 `openEditUserModal` 在设置标题/表单前就抛错，**编辑弹窗直接打不开**（"没带入"= 角色信息根本没进编辑页）。
  - 后果2（连带）：`editUserModalTitle` 用 `innerText` 给含 `<span>` 的 HTML 串赋值，`innerText` 不解析 HTML，会把 `<span data-term="teacher">教师</span>` 当成字面文本显示。
- 后端佐证：`UserController.updateInfo` → `UserService.updateUserInfo` 只更新 name/phone/email/status，**完全不用 role** → 该 bug 纯前端显示问题，role 无需进提交体。
- 修复（frontend/js/admin-user.js）：
  1. `openEditUserModal` 在取缓存 user 后加 `const isTeacher = (user.role || currentUserRole) === 'teacher';`（user.role 优先，回退当前列表角色），消除 ReferenceError。
  2. `editUserModalTitle` 的 `innerText` → `innerHTML`（标题正常渲染"编辑教师信息"/"编辑学生信息"，不再露字面标签）。
  3. 顺手把 `addUserModalTitle` 的 `innerText` 也改成 `innerHTML`（同款隐患）。注：add 弹窗标题模板本就是 `'添加'+roleName`（无"信息"后缀），与 edit 的 `'编辑'+roleName+'信息'` 不一致是原有设计，非本次 bug。
- 测试：按测试用例文档 TC-F-09/10/11 写 `doc-develop/test_admin_user.js`（jsdom 加载真实 admin-user.js + mock request/applyTerms 等依赖）。
  - 12 项全通过：teacher/student 列表标题正确、编辑教师/学生弹窗不再抛 ReferenceError 且标题=编辑教师/学生信息、表单手机号回填真实值（非脱敏串）、添加弹窗标题带入角色、confirmTeacher 调 /user/updateStatus 且 status=active&userId=T1。
  - 时序坑：`renderTeacherCards` 末尾 `loadUserList(role)` 未 await，列表行与 userRowCache 在 `await renderTeacherCards` 返回后仍异步填充，测试需额外 `setTimeout` 等待；`confirmTeacher` 的 `.then` 会再发 /user/page 请求，断言须用 `.find` 查"是否包含 updateStatus"而非只看最后一条。
- 说明：dist 需重打 frontend 模块才生效；测试针对源 js 已证明修复正确。
- 重打 frontend 模块（task 2Q5aMm，`/tmp/frontend_build3.log`）：BUILD SUCCESS，dist 已刷新。验证 dist 含 `lang-switch-dropdown`（termsFunction.js）；dist admin-user.js 的 `const isTeacher` 字面搜不到属正常（terser 混淆改名），terser 语法校验已过即证明修复编译进 dist。部署到服务器（scp dist 到 Nginx root）属写服务器外部动作，需用户确认目标 Nginx root 路径后再执行；api jar 无需重打（业务前端不进 api 模块）。

## BUG：登录页标题未按 tCode 显示租户名（语言教学预约系统未替换）
- 现象：`http://localhost:8080/index.html?tCode=TENANT_A` 登录/注册页标题恒为"语言教学预约系统"，未改用租户机构名（前后端分离前可行）。
- 根因（代码级铁证）：`termsFunction.js` 的 `applyTenantTitle()`（:174）用**原生 fetch** 调 `base + '/tenant/name?tenantCode='`，而 `getIndustry()`（:143）调 `/tenant/industry`、`loadTermMapFromServer()`（:111）调 `/term/map` —— 三处都用原生 fetch，**未走 `request` 封装**，因此不经过 `utility_request.js` 的 `normalizeUrl()`（`/api/v1` 前缀由该函数在 request 封装内自动补）。
- 后果：实际请求 URL 缺 `/api/v1` 前缀 → 落到 Nginx/dev-frontend.js 的 `location /`（SPA 兜底）返回 index.html → `r.json()` 抛 SyntaxError → `.catch` 保持默认标题。后端接口实为 `/api/v1/tenant/name`、`/api/v1/tenant/industry`、`/api/v1/term/map`（各 Controller 均在 `/api/v1` 下）。
- 证据：①全部 Controller @RequestMapping 均带 `/api/v1`（UserController/AuthController/TenantController/TermController 等）；②dev-frontend.js 仅代理 `/api/v1/*`；③SecurityConfig(:102) 与 JwtAuthenticationFilter(:52) 已放行 `/api/v1/tenant/name`（匿名），故修前缀后即可工作；`/tenant/industry`、`/term/map` 需 token，前端调用均携带 `Bearer`。
- 修复（`frontend/js/public/termsFunction.js`，3 处源码 + 注释）：给三处原生 fetch URL 补 `/api/v1` 前缀（恰好等于 normalizeUrl 本应做的拼接）。同步修正了"按 tCode 切行业"与"服务端术语三级合并词表"两处同类隐藏 bug。
- 影响范围：登录页品牌化标题、按 tCode 切换行业词、服务端租户/行业术语覆盖——三者此前在分离部署下均失效，现恢复。
- 重打 frontend 模块：task p3vBQU 首次**BUILD FAILURE**——非代码错，是 `build.js:116` 的 `fs.rmSync(DIST,{recursive,force})` 在 Windows 上清理 `dist/images` 报 `ENOTEMPTY`（目录非空，疑似杀软/预览进程锁文件句柄）。dist 当时为残缺残留（不可信）。→ 用 node `rmSync` 带 5 次重试强制删 dist 成功，重试 build5（task 内前台）**BUILD SUCCESS**（EXIT=0），dist 三项前缀均落地（grep -c 各=1），`lang-switch-dropdown` 仍在。
- 经验沉淀：**Windows 上 frontend 模块 `mvn package` 偶发 ENOTEMPTY 清理失败**；规避法=构建前先手动 `node -e fs.rmSync(DIST,{recursive:true,force:true})`（可加重试）。该坑可补进 readme「常见维护坑」或建 skill 复用。

## BUG：消息中心 SSE 报 ERR_CONNECTION_REFUSED（localhost:8090）
- 现象：前端与消息中心交互，浏览器报 `http://localhost:8090/api/v1/sse/connect?access_token=...` `net::ERR_CONNECTION_REFUSED`。
- 根因（代码级铁证）：`frontend/js/messages-inbox.js:22` 原 `MSG_BASE = window.MESSAGE_API_BASE_URL || ('http://'+location.hostname+':8090')`——用页面 hostname 拼**绝对地址**直连 `:8090`。当用户从本地 localhost:8080 跑前端（或生产 Nginx 页面 hostname 为公网 IP）时，请求绕过 dev 代理/Nginx，直接打本机/外部 `:8090` → message-service 不在该地址（远程 152.136.254.127:8090 或生产仅 127.0.0.1:8090 防火墙拦截）→ 连接被拒。同类坑：此前 termsFunction.js 三处原生 fetch 缺 `/api/v1` 前缀。
- 修复（源码 + readme 注释）：`MSG_BASE` 默认改为同源 `window.API_BASE_URL || ''`（与 line24 `API_BASE` 一致、符合 readme「默认走同源分流」口径）；所有 `mreq`/`SSE` 调用均带完整 `/api/v1/...` 路径，同源后由 dev-frontend.js（本地）/Nginx（生产）把 `/api/v1/{message,sse,users}` 分流到 8090。`window.MESSAGE_API_BASE_URL` 仍保留为跨域直连覆盖位。
- 验证：重打 frontend 模块（build6，预清 dist）BUILD SUCCESS；dist `messages-inbox.js` 中 `:8090` 硬编码计数=0，SSE 路径 `/api/v1/sse/connect` 仍在（现同源）。dev-frontend.js 与 nginx 均已有 `/api/v1/sse → :8090` 且 `proxy_buffering off`，无需改。
- 回归测试：`doc-develop/test_sse_url.js`（jsdom 加载真实 `messages-inbox.js` + stub EventSource 抓 URL）**5 项断言全过**：场景1 默认同源→SSE URL=`/api/v1/sse/connect?access_token=...`（不含 `:8090`、不以 `http://` 开头）、mreq baseURL=`''`；场景2 `window.MESSAGE_API_BASE_URL='http://msg.remote.com'` 覆盖位仍生效→URL 带该前缀。证明修复且未破坏跨域直连覆盖。
- 影响范围：消息中心全部交互（收件箱/发消息/SSE 推送）此前在分离/跨机部署下均连不上 8090；现恢复。api jar 无需重打（仅业务前端）。

## 需求：发送弹窗改浮动卡片+左键拖动；修「发消息」链接中断
- 任务1（messages-inbox.js）：`buildComposeHtml` 由「全屏遮罩+居中 modal」改为**浮动卡片** `.msg-compose-card`（position:fixed，初始 top:120px+居中 transform），头部 `.msg-compose-header` 作拖动手柄（cursor:move），右上角 × 关关闭（`#msg-compose-cancel`）。新增 `enableComposeDrag(card,handle)`：仅 `e.button===0`（左键）生效，mousedown 记录偏移并切 left/top 像素定位（清 transform），document 级 mousemove/mouseup 限视口内移动，松手移除监听、恢复 userSelect。
- 任务2（admin-user.js）：根因=原内联 `onclick="window.openComposeToUser('...','...','${(tea.name||'').replace(/'/g,"")}')"` 仅转义单引号，姓名含双引号/`<`/`>` 时破坏 HTML 属性→按钮点击失效（链接中断）。修复：新增 `escAttr()` 转义 `&"/<>`，按钮改 `data-compose-uid/role/name` 属性 + 一次性 `document` 事件委托读取 data 调 `openComposeToUser`，彻底消除字符串插值破坏。发送端点 `/api/v1/messages/send` 后端 `MessageSendController` 路径一致，发送链路本身未断（仅前端入口坏）。
- 回归测试 `doc-develop/test_compose_card_and_link.js`（jsdom 加载真实两源码）**12 项断言全过**：卡片形态/旧遮罩移除/左键拖动位移(120,80)/右键不触发/escAttr 转义/特殊字符姓名完整传入/旧内联 onclick 含双引号失效证据。
- 构建：重打 frontend 模块（build7，预清 dist 规避 ENOTEMPTY）**BUILD SUCCESS**；dist 验证 `msg-compose-card`/`msg-compose-header`/`mousemove`/`data-compose-name` 均在，`openComposeToUser('` 内联字面 0（已移除）。api jar 无需重打。上线需用户确认 Nginx root 再 scp dist + reload。
- 任务2（链接中断根因）：`admin-user.js:246` 原内联 `onclick="window.openComposeToUser('${tea.userId}','${tea.role}','${(tea.name||'').replace(/'/g,"")}')"` 仅转义单引号，姓名含双引号/`</>` 时破坏 HTML 属性 → 点击失效（链接中断）。修复：新增 `escAttr()`（转义 &/"/</>）与一次性 document 事件委托（识别 `[data-compose-uid]` → 读 data 属性 → 调 `window.openComposeToUser`），按钮改为 `data-compose-uid/role/name`。发送端点 `/api/v1/messages/send` 后端 `MessageSendController` 存在且路径一致，发送链路本身未断。

## 诊断：SSE connect 502 Bad Gateway（localhost:8080/api/v1/sse/connect）
- 现象：消息中心 SSE 现经 :8080 同源代理转发（前缀修复已生效），但返回 502。
- 根因（端口连通性实证，**非前端代码 bug**）：dev-frontend.js（`doc-develop/dev-frontend.js`）默认 `API_HOST=152.136.254.127`、`MSG_PORT=8090`，把 `/api/v1/sse` 转发到 `152.136.254.127:8090`。连通性实测：远程 `:8081`(booking) OPEN、远程 `:8090`(message-service) **TIMEOUT**、本地 `127.0.0.1:8090` ECONNREFUSED、本地 `:8081` ECONNREFUSED → 上游 message-service 不可达 → 代理 `res.end('Bad Gateway: '+e.message)` 抛 502。
- 结论：前端 SSE 同源修复正确；502 = 远程 message-service(:8090) 未启动/防火墙拦截。修复为运维动作（启动 message-service 或本地起并改代理 MSG_HOST），非代码改动。暂未执行（写服务器动作，需用户确认）。
- 回归测试：`doc-develop/test_compose_card_and_link.js`（jsdom 加载真实源码）**12 项断言全过**：卡片渲染/遮罩移除/左键拖动位移(120,80)/右键不触发/escAttr 转义/修复后特殊字符姓名完整传入/旧内联 onclick 含双引号失效证据。
- 待办：仅改源 js，需重打 frontend 模块（`mvn package`）刷新 dist 才生效；api jar 无需重打。

## BUG：消息编辑「加载接收人」404（/user/message-recipients 缺 /api/v1 前缀）
- 现象：`http://localhost:8080/user/message-recipients?scope=platform_admin` 报 404。
- 根因（代码级铁证）：`frontend/js/messages-inbox.js:752` `areq.get('/user/message-recipients', ...)` 路径缺 `/api/v1` 前缀。后端 `UserController` `@RequestMapping("/api/v1/user")` + `@GetMapping("/message-recipients")`，完整路径应为 `/api/v1/user/message-recipients`。`areq` 是独立 axios 实例（baseURL=API_BASE||''），不自带 normalizeUrl；axios 对以 `/` 开头的 url 忽略 baseURL → 请求落到 `localhost:8080/user/message-recipients`，Nginx/dev-frontend.js 不转发该裸路径 → 404（SPA 兜底返回 index.html）。与之前 termsFunction.js 三处原生 fetch 漏 `/api/v1` 同因。
- 修复（两份源文件，路径 + 头注释同步）：
  - `frontend/js/messages-inbox.js:752`（业务前端，影响 dist 的 :8080 页面）`/user/message-recipients` → `/api/v1/user/message-recipients`。
  - `api/src/main/resources/static/js/messages-inbox.js:704`（booking jar 内平台管理端 platform-admin.html，同源 bug）同样改。注：api 这份需重打 booking jar 才生效，用户当前在 :8080 不触发。
- 构建：重打 frontend 模块（预清 dist 规避 ENOTEMPTY）BUILD SUCCESS；dist 验证 `grep -c /api/v1/user/message-recipients=1`、旧路径残留=0。
- 路由复核：`/api/v1/user/message-recipients` 经 dev-frontend.js/nginx 的 `/api/v1/` 兜底 → :8081(booking)，与 message-service 的 `/api/v1/users/`（带尾斜杠，scope 相关）不同路径、不冲突。api jar 不重打则平台管理端仍 404，已知。

## 诊断：消息发送 POST /api/v1/messages/send 报「XHR 加载失败」
- 现象：前端发消息 `POST http://localhost:8080/api/v1/messages/send` 浏览器报 XHR 加载失败（网络层失败）。
- 路径核对：前端 `mreq.post('/api/v1/messages/send')` 与后端 `MessageSendController` `@RequestMapping("/api/v1/messages")+@PostMapping("/send")` 一致，**非路径 bug**。
- 根因（netstat 铁证，非前端代码问题）：dev-frontend.js（:8080 在监听，PID 25512）把 `/api/v1/messages` 转 `MSG_HOST:8090`。实测本地出向 `192.168.1.8:* → 152.136.254.127:8090` 卡在 **SYN_SENT**（发 SYN 无 SYN-ACK），本地 `127.0.0.1:8090` 无进程监听 → 远程 message-service(:8090) 不可达（未起或防火墙静默丢弃）。请求 TCP 层即失败 → 到不了 HTTP，与之前 SSE 502 同根因（message-service 不可达）。
- 本地条件齐备：已构建 `api/message-service/target/message-service-1.0.0.jar`；本地 MySQL `3306` OPEN；`message_center` 库表齐全（msg_message/inbox/delivery/...）。→ 可本地起 message-service 做联调，无需碰服务器防火墙。
- 已给 `doc-develop/dev-frontend.js` 加 `MSG_HOST` 环境变量（分流 message/booking host），`node -c` 通过。

## 执行路线 B（本地起 message-service）+ 根因深挖
- 操作：停旧 dev 代理(PID 25512，`taskkill //PID` 在 Git Bash 被原样传参报错，改用 PowerShell `Stop-Process -Id 25512 -Force`)；`MSG_HOST=127.0.0.1 node dev-frontend.js`（booking 仍走远程 152.136.254.127:8081，message 走本地 127.0.0.1:8090）；本地 `java -jar message-service-1.0.0.jar --server.port=8090`。
- 首启失败：日志 `Tomcat initialized with port 55058 ... Port 55058 was already in use`。查 PID 18784=WorkBuddy IDE 自身（占 55058），非 message-service；PID 25592=chrome.exe（旧页面到远程 8090 的残留 SYN_SENT，无关）。→ 说明首次启动生效端口是 55058 而非 8090。
- ⚠️ 根因深挖（更正此前误判）：`unzip -l` 误报「jar 无 application.properties」是工具假象——用 Python 读 zip 证实 `BOOT-INF/classes/application.properties` **确实存在且含 server.port=8090 + DB + JWT + AES/HMAC**（与源码一致）。jar 配置完整，构建无问题。
- **真正根因（本地 55058 冲突）= 本沙箱环境变量 `SERVER__PORT=55058`**（WorkBuddy 自身服务代理端口，见 env 输出 `SERVER__PORT=55058`）。Spring Boot 宽松绑定把 `SERVER__PORT` 映射成 `server.port`，**覆盖了 jar 的 server.port=8090** → 首次启动去绑 55058 撞上 IDE → 退出。第二次用 `--server.port=8090`（CLI 参数优先级最高）才胜出绑 8090。证据：env 中唯一端口变量即 `SERVER__PORT=55058`，且首次运行端口恰为 55058。
- **远程 TIMEOUT 与端口/构建无关**：远程服务器无此 `SERVER__PORT` 环境变量，jar 自带 server.port=8090 → 远程 `java -jar` 会正常绑 8090。故远程 8090 不通是「message-service 未真正运行」和/或「防火墙静默丢弃 8090 入站」（SYN_SENT=无 SYN-ACK），不是端口/配置问题。之前把远程归因为"jar 漏配置→随机端口"是错的。
- 修复（部署侧，冗余但无害）：`api/beforeRun/message.env.template` 末尾新增 `SERVER_PORT=8090`（显式声明端口，兜底任何环境变量的端口覆盖；非必须，jar 已有）。远程部署须 `cp` 为 `/etc/lesson/message.env`（含 JWT/AES/HMAC 且与 booking 一致）。
- 验证（路线 B 成功）：本地 message-service 起在 8090(PID 27488，Tomcat started on port 8090)。经 `localhost:8080`(代理) POST `/api/v1/messages/send`、GET `/api/v1/sse/connect`、直连 `127.0.0.1:8090/send` 三者均返回 **HTTP 401**（无 token 被鉴权拒绝）= 链路已通、不再是连接失败（XHR 加载失败/502 已解决）。带 booking 登录 token 即可正常发送（本地 message-service jwt.secret 与 booking 同源，token 可校验）。
- 说明：本地消息联调现已可用；**本地重启 message-service 必须带 `--server.port=8090`**（否则沙箱 SERVER__PORT=55058 会再次覆盖导致绑 55058 冲突）。远程要彻底修 = 确保 message-service 运行 + 防火墙放行 8090 入站（与端口/构建无关）。
- 改动：`doc-develop/dev-frontend.js` 新增 `MSG_HOST` 环境变量（默认=API_HOST），`proxy()` 改为按 `toMessageService()` 用 `MSG_HOST`/`API_HOST` 分流；`node -c` 语法 OK。使「booking 走远程 API_HOST、message-service 走本地 127.0.0.1」可行，绕开远程 8090 防火墙。
- 两条修复路线（待用户选）：
  - 路线 B（推荐本地）：`MSG_HOST=127.0.0.1 node dev-frontend.js` + 本地 `java -jar api/message-service/target/message-service-1.0.0.jar`（连本地 message_center）。零服务器依赖。
  - 路线 A（远程）：服务器 `systemctl start message-service` 且 `firewall-cmd --add-port=8090/tcp --permanent && firewall-cmd --reload`（SYN_SENT=静默丢弃，光起服务不够，必须开 8090 入站）。
- 交付 `doc-develop/start-local-msg.sh` 一键启动脚本（用户选路线 B 的"1"）：清理 8080(dev代理)/本地 8090(message-service) → `java -jar message-service-1.0.0.jar --server.port=8090` → `MSG_HOST=127.0.0.1 node dev-frontend.js` → 等 8090 就绪 → 冒烟(经 8080 打 send 期望 401)。`bash -n` 语法 OK，实跑验证可重入（清理旧进程后重起成功，冒烟 401）。
- ⚠️ agent 沙箱特性：脚本内 `nohup ... &` 子进程会在脚本退出后被进程组回收（测试时 8080/8090 掉）；**用户本地真实 Git Bash 里 nohup 正常脱离终端存活**，脚本对其本地有效。agent 内要常驻须用 Bash 工具 `run_in_background` 直接托管 `java -jar ... --server.port=8090` 与 `MSG_HOST=127.0.0.1 node dev-frontend.js`（已验证可常驻：message-service PID 27644 / dev 代理 PID 27436，send+SSE 均 401=链路通）。

## 更正：路线 B「混合本地/远程」无效（JWT 密钥域不一致）——用户权威确认
- 用户纠正（2026-09-08 晚）：**远程的 message-service 与远程的 booking api 使用系统的 jwt 密钥**；**本地运行的 message-service 密钥与远程 api 不同**；因此**前端不能一个连本地、一个连远程**。
- 密钥校验铁律：token 只能被「与其同源密钥」的服务校验。可行拓扑只有两种：
  - 全远程：远程 booking + 远程 message-service（两者同用系统 jwt 密钥）→ 兼容；前提是远程 message-service 真在跑 + 防火墙放行 8090 入站。
  - 全本地：本地 booking(8081，用源 `jwt.secret` 882 串) + 本地 message-service(8090，同样源 secret) → 兼容；前提是本地库数据就绪。
  - ❌ 混合（本地 message + 远程 booking，或本地 booking + 远程 message）→ token 跨服务验不过，必然 401。
- 据此**纠正此前路线 B 的表述**：早前「`MSG_HOST=127.0.0.1` + `API_HOST=远程`」的拆分是错的。当时冒烟 send 经本地 message 返回 200、load recipients 经远程 booking 返回 401，我一度猜 booking 有 session 校验；实为远程 booking 用**系统 secret ≠ 本地 882 串** → 密钥不匹配导致 401。之前把"远程 401"归因为 session 也是错的（JwtAuthenticationFilter 只验签名、不查 sys_user_session，见 159 行起分析）。
- `doc-develop/dev-frontend.js` 的 MSG_HOST 分流**仅在两端密钥一致时（全本地或全远程同密钥）才安全**；混合部署下会静默 401，不可用于"前端一边本地一边远程"的联调。
- 行动：路线 B（混合）不再作为推荐方案。要联调只能全本地或全远程。当前 agent 内我托管的后台进程已改成 `API_HOST=127.0.0.1 MSG_HOST=127.0.0.1`（全本地）才符合该约束。

## 验证：全本地闭环冒烟全绿（印证密钥铁律）
- 拓扑：dev 代理 `:8080`（`API_HOST=127.0.0.1 MSG_HOST=127.0.0.1`）→ 本地 booking `:8081` + 本地 message-service `:8090`，两服务同源用 `api/src/main/resources/application.properties` 的 882 串 `jwt.secret`。
- 冒烟 `doc-develop/smoke_msg.py`（用 882 secret 伪造 platform_admin token，绕过无明文密码的登录限制）：
  - `[1] GET /api/v1/user/message-recipients?scope=platform_admin` → **200**，返回 platform_admin 列表（admin1@plt.com、plat_test@plt.com…，字段为 AES 密文，证明本地 booking 同源解密正常）。→ 此前远程 401 确为**密钥不一致**（非 session 校验），印证用户纠正。
  - `[2] POST /api/v1/messages/send` → **200** `发送成功`，messageId 落库，recipientCount=1。
- 结论：全本地拓扑下，前端在 `localhost:8080` 登录(local booking)→加载接收人(local booking)→发送(local message)→SSE(local message) 整链可工作（同源密钥）。此前「加载接收人 404/401」「发送 XHR 失败/502」「SSE 502」三个 saga 在全本地拓扑下已全部闭合。
- 含义：浏览器硬刷新 `http://localhost:8080` 用本地平台管理员账号（如 admin1@plt.com）登录即可走通消息中心全流程；要接真实远程数据须整体切远程（且远程 message-service 须起+防火墙开 8090）。
