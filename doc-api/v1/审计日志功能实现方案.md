# 审计日志功能实现方案

## 一、数据库设计

### 1.1 audit_log 表

```sql
CREATE TABLE `audit_log` (
  `id`            BIGINT       NOT NULL AUTO_INCREMENT  COMMENT '自增主键',
  `log_id`        VARCHAR(36)  NOT NULL                 COMMENT '日志唯一标识（UUID）',
  `user_id`       VARCHAR(36)  DEFAULT NULL             COMMENT '操作人ID',
  `user_name`     VARCHAR(100) DEFAULT NULL             COMMENT '操作人账号/姓名（冗余便于查询）',
  `user_role`     VARCHAR(20)  DEFAULT NULL             COMMENT '操作人角色（student/teacher/admin）',
  `action`        VARCHAR(50)  NOT NULL                 COMMENT '操作类型（见 1.2 枚举）',
  `resource_type` VARCHAR(30)  DEFAULT NULL             COMMENT '操作资源类型（user/course/schedule/booking/appointment）',
  `resource_id`   VARCHAR(36)  DEFAULT NULL             COMMENT '操作资源ID',
  `resource_name` VARCHAR(200) DEFAULT NULL             COMMENT '操作资源名称（冗余便于展示）',
  `method`        VARCHAR(200) DEFAULT NULL             COMMENT '执行的 Java 方法（类名.方法名）',
  `request_url`   VARCHAR(500) DEFAULT NULL             COMMENT '请求 URL',
  `http_method`   VARCHAR(10)  DEFAULT NULL             COMMENT 'HTTP 方法（GET/POST/PUT/DELETE）',
  `ip`            VARCHAR(50)  DEFAULT NULL             COMMENT '客户端 IP',
  `user_agent`    VARCHAR(500) DEFAULT NULL             COMMENT '客户端 User-Agent',
  `request_params` TEXT         DEFAULT NULL            COMMENT '请求参数（JSON，敏感字段脱敏）',
  `result_status` VARCHAR(20)  DEFAULT NULL             COMMENT '操作结果（success/fail）',
  `error_msg`     VARCHAR(500) DEFAULT NULL             COMMENT '失败原因（异常摘要）',
  `cost_ms`       INT          DEFAULT NULL             COMMENT '执行耗时（毫秒）',
  `created_at`    DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '操作时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_log_id` (`log_id`),
  KEY `idx_user_id`     (`user_id`),
  KEY `idx_action`      (`action`),
  KEY `idx_resource`    (`resource_type`,`resource_id`),
  KEY `idx_created_at`  (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='审计日志表';
```

### 1.2 操作类型枚举

```java
package com.reservation.audit;

public enum AuditAction {
    // 用户相关
    USER_LOGIN("用户登录"),
    USER_LOGOUT("用户登出"),
    USER_REGISTER("用户注册"),
    USER_APPROVE("用户审核"),
    USER_FREEZE("用户冻结"),
    USER_UNFREEZE("用户解冻"),
    USER_DELETE("用户删除"),
    USER_RESET_PASSWORD("重置密码"),
    USER_CHANGE_PASSWORD("修改密码"),

    // 课程相关
    COURSE_CREATE("创建课程"),
    COURSE_UPDATE("修改课程"),
    COURSE_DELETE("删除课程"),
    COURSE_PUBLISH("发布课程"),
    TEMPLATE_CREATE("创建模板"),
    TEMPLATE_UPDATE("修改模板"),
    TEMPLATE_DELETE("删除模板"),

    // 排期相关
    SCHEDULE_CREATE("创建排期"),
    SCHEDULE_UPDATE("修改排期"),
    SCHEDULE_DELETE("删除排期"),
    SCHEDULE_ASSIGN("分配学生到排期"),

    // 预约相关
    BOOKING_CREATE("创建预约"),
    BOOKING_CONFIRM("确认预约"),
    BOOKING_CANCEL("取消预约"),
    BOOKING_DELETE("删除预约"),

    // 课时相关
    APPOINTMENT_NOTE("发送上课通知"),
    APPOINTMENT_COMPLETE("完成课时"),
    APPOINTMENT_DELETE("删除课时"),

    // 教师信息
    TEACHER_PROFESSIONAL_UPDATE("修改教师职业信息"),
    TEACHER_PROFILE_PUBLISH("发布教师主页"),
    TEACHER_PROFILE_ARCHIVE("归档教师主页"),

    // 系统管理
    ADMIN_FORCE_LOGOUT("强制踢出用户"),
    SYSTEM_CONFIG_UPDATE("修改系统配置");

    private final String label;
    AuditAction(String label) { this.label = label; }
    public String getLabel() { return label; }
}
```

---

## 二、后端实现

### 2.1 Entity

```java
package com.reservation.entity;

import lombok.Data;
import java.io.Serializable;
import java.util.Date;

@Data
public class AuditLog implements Serializable {
    private static final long serialVersionUID = 1L;
    private Long id;
    private String logId;          // UUID
    private String userId;
    private String userName;
    private String userRole;
    private String action;          // AuditAction 枚举名
    private String resourceType;
    private String resourceId;
    private String resourceName;
    private String method;         // 类名.方法名
    private String requestUrl;
    private String httpMethod;
    private String ip;
    private String userAgent;
    private String requestParams;  // JSON 字符串
    private String resultStatus;   // success / fail
    private String errorMsg;
    private Integer costMs;        // 执行耗时
    private Date createdAt;
}
```

### 2.2 Mapper

```java
package com.reservation.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.reservation.entity.AuditLog;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface AuditLogMapper extends BaseMapper<AuditLog> {
}
```

### 2.3 Mapper XML

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.reservation.mapper.AuditLogMapper">

    <insert id="insert" parameterType="com.reservation.entity.AuditLog">
        INSERT INTO audit_log
          (log_id, user_id, user_name, user_role, action,
           resource_type, resource_id, resource_name,
           method, request_url, http_method, ip, user_agent,
           request_params, result_status, error_msg, cost_ms, created_at)
        VALUES
          (#{logId}, #{userId}, #{userName}, #{userRole}, #{action},
           #{resourceType}, #{resourceId}, #{resourceName},
           #{method}, #{requestUrl}, #{httpMethod}, #{ip}, #{userAgent},
           #{requestParams}, #{resultStatus}, #{errorMsg}, #{costMs}, NOW())
    </insert>
</mapper>
```

### 2.4 @Audit 注解

```java
package com.reservation.audit;

import java.lang.annotation.*;

/**
 * 标注需要审计日志的方法。
 * 加在 Controller 方法上，AOP 切面自动采集并入库。
 */
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
@Documented
public @interface Audit {
    /** 操作类型 */
    AuditAction action();

    /** 资源类型 */
    String resourceType() default "";

    /** 资源ID的 SpEL 表达式，如 "#id" 或 "#dto.courseId" */
    String resourceId() default "";

    /** 资源名称的 SpEL 表达式 */
    String resourceName() default "";

    /** 是否记录请求参数（默认 true） */
    boolean logParams() default true;
}
```

### 2.5 AuditAspect 切面

```java
package com.reservation.audit;

import com.reservation.entity.AuditLog;
import com.reservation.mapper.AuditLogMapper;
import com.reservation.utils.JwtUtil;
import com.fasterxml.jackson.databind.ObjectMapper;
import lombok.extern.slf4j.Slf4j;
import org.aspectj.lang.ProceedingJoinPoint;
import org.aspectj.lang.annotation.*;
import org.aspectj.lang.reflect.MethodSignature;
import org.springframework.stereotype.Component;
import org.springframework.web.context.request.RequestContextHolder;
import org.springframework.web.context.request.ServletRequestAttributes;

import jakarta.servlet.http.HttpServletRequest;
import java.util.Date;
import java.util.HashMap;
import java.util.Map;
import java.util.UUID;

@Aspect
@Component
@Slf4j
public class AuditAspect {

    private final AuditLogMapper auditLogMapper;
    private final ObjectMapper objectMapper;
    private final JwtUtil jwtUtil;

    public AuditAspect(AuditLogMapper auditLogMapper,
                       ObjectMapper objectMapper, JwtUtil jwtUtil) {
        this.auditLogMapper = auditLogMapper;
        this.objectMapper = objectMapper;
        this.jwtUtil = jwtUtil;
    }

    @Around("@annotation(audit)")
    public Object around(ProceedingJoinPoint joinPoint, Audit audit) throws Throwable {
        long start = System.currentTimeMillis();

        // 提前解析请求上下文（异常后仍可用）
        HttpServletRequest request = getRequest();
        AuditLog record = buildBaseLog(request);

        // 解析注解配置
        MethodSignature sig = (MethodSignature) joinPoint.getSignature();
        record.setAction(audit.action().name());
        record.setResourceType(audit.resourceType());

        // SpEL 解析 resourceId / resourceName
        record.setResourceId(resolveSpel(audit.resourceId(), joinPoint));
        record.setResourceName(resolveSpel(audit.resourceName(), joinPoint));

        // 记录方法信息
        record.setMethod(sig.getDeclaringTypeName() + "." + sig.getName());

        // 记录请求参数
        if (audit.logParams()) {
            record.setRequestParams(serializeArgs(joinPoint, sig));
        }

        Object result;
        try {
            result = joinPoint.proceed();
            record.setResultStatus("success");
        } catch (Throwable e) {
            record.setResultStatus("fail");
            record.setErrorMsg(truncate(e.getMessage(), 500));
            throw e;  // 异常继续抛出，不影响业务流程
        } finally {
            record.setCostMs((int) (System.currentTimeMillis() - start));
            saveAsync(record);  // 异步入库，不阻塞主流程
        }
        return result;
    }

    private AuditLog buildBaseLog(HttpServletRequest request) {
        AuditLog log = new AuditLog();
        log.setLogId(UUID.randomUUID().toString());
        if (request != null) {
            log.setRequestUrl(request.getRequestURI());
            log.setHttpMethod(request.getMethod());
            log.setIp(extractIp(request));
            log.setUserAgent(request.getHeader("User-Agent"));
            // 从 JWT Token 提取用户信息
            String token = extractToken(request);
            if (token != null) {
                try {
                    log.setUserId(jwtUtil.extractUserId(token));
                    log.setUserName(jwtUtil.extractUsername(token));
                    log.setUserRole(jwtUtil.extractRole(token));
                } catch (Exception ignored) { }
            }
        }
        return log;
    }

    private void saveAsync(AuditLog record) {
        // 异步保存到数据库，不影响主流程
        new Thread(() -> {
            try {
                auditLogMapper.insert(record);
            } catch (Exception e) {
                log.error("审计日志保存失败: {}", e.getMessage());
            }
        }).start();
    }

    // --- 工具方法 ---

    private HttpServletRequest getRequest() {
        ServletRequestAttributes attrs =
            (ServletRequestAttributes) RequestContextHolder.getRequestAttributes();
        return attrs != null ? attrs.getRequest() : null;
    }

    private String extractToken(HttpServletRequest request) {
        String header = request.getHeader("Authorization");
        if (header != null && header.startsWith("Bearer ")) {
            return header.substring(7);
        }
        return null;
    }

    private String extractIp(HttpServletRequest request) {
        String ip = request.getHeader("X-Forwarded-For");
        if (ip == null || ip.isEmpty()) {
            ip = request.getHeader("X-Real-IP");
        }
        if (ip == null || ip.isEmpty()) {
            ip = request.getRemoteAddr();
        }
        return ip;
    }

    private String serializeArgs(ProceedingJoinPoint joinPoint, MethodSignature sig) {
        try {
            String[] paramNames = sig.getParameterNames();
            Object[] args = joinPoint.getArgs();
            Map<String, Object> paramMap = new HashMap<>();
            for (int i = 0; i < args.length; i++) {
                if (paramNames != null && i < paramNames.length) {
                    paramMap.put(paramNames[i], maskSensitive(args[i]));
                }
            }
            return truncate(objectMapper.writeValueAsString(paramMap), 2000);
        } catch (Exception e) {
            return null;
        }
    }

    /** 脱敏：屏蔽密码、Token 等敏感字段 */
    private Object maskSensitive(Object obj) {
        if (obj == null) return null;
        String json;
        try {
            json = objectMapper.writeValueAsString(obj);
        } catch (Exception e) {
            return obj.toString();
        }
        // 正则替换密码、token 字段值
        json = json.replaceAll(
            "(\"(?:password|token|secret|oldPassword|newPassword)\"\\s*:\\s*)\"[^\"]*\"",
            "$1\"***\"");
        return json;
    }

    /** 简易 SpEL 解析（支持 #参数名 和 #dto.字段） */
    private String resolveSpel(String expr, ProceedingJoinPoint joinPoint) {
        if (expr == null || expr.isEmpty()) return null;
        if (!expr.startsWith("#")) return expr;

        MethodSignature sig = (MethodSignature) joinPoint.getSignature();
        String[] paramNames = sig.getParameterNames();
        Object[] args = joinPoint.getArgs();

        // 解析 #paramName 或 #paramName.field
        String cleaned = expr.substring(1);
        int dotIdx = cleaned.indexOf('.');
        String paramName = dotIdx > 0 ? cleaned.substring(0, dotIdx) : cleaned;
        String fieldName = dotIdx > 0 ? cleaned.substring(dotIdx + 1) : null;

        for (int i = 0; i < paramNames.length; i++) {
            if (paramNames[i].equals(paramName)) {
                Object value = args[i];
                if (fieldName == null) return String.valueOf(value);
                // 简单反射取字段值
                try {
                    java.lang.reflect.Field f = value.getClass().getDeclaredField(fieldName);
                    f.setAccessible(true);
                    Object fieldVal = f.get(value);
                    return fieldVal != null ? String.valueOf(fieldVal) : null;
                } catch (Exception e) {
                    return null;
                }
            }
        }
        return null;
    }

    private String truncate(String s, int maxLen) {
        if (s == null) return null;
        return s.length() > maxLen ? s.substring(0, maxLen) : s;
    }
}
```

### 2.6 在 Controller 上使用 @Audit 注解

在需要审计的方法上加注解，无需侵入业务逻辑：

```java
// authController.java
@Audit(action = AuditAction.USER_LOGIN, resourceType = "user")
@PostMapping("/login")
public Result<TokenDTO> login(@RequestBody LoginDTO dto) { ... }

// CourseController.java
@Audit(action = AuditAction.COURSE_DELETE, resourceType = "course",
       resourceId = "#courseId")
@DeleteMapping("/{courseId}")
public Result<Void> deleteCourse(@PathVariable String courseId) { ... }

@Audit(action = AuditAction.COURSE_CREATE, resourceType = "course",
       resourceId = "#dto.courseName", resourceName = "#dto.courseName")
@PostMapping
public Result<String> createCourse(@RequestBody CourseCreateDTO dto) { ... }

// ScheduleController.java
@Audit(action = AuditAction.SCHEDULE_CREATE, resourceType = "schedule",
       resourceId = "#dto.name", resourceName = "#dto.name")
@PostMapping
public Result<String> createSchedule(@RequestBody ScheduleCreateDTO dto) { ... }

@Audit(action = AuditAction.SCHEDULE_DELETE, resourceType = "schedule",
       resourceId = "#scheduleId")
@DeleteMapping("/{scheduleId}")
public Result<Void> deleteSchedule(@PathVariable String scheduleId) { ... }

// BookingController.java
@Audit(action = AuditAction.BOOKING_CREATE, resourceType = "booking",
       resourceId = "#dto.scheduleId")
@PostMapping
public Result<String> createBooking(@RequestBody BookingCreateDTO dto) { ... }

@Audit(action = AuditAction.BOOKING_CANCEL, resourceType = "booking",
       resourceId = "#bookingId")
@PutMapping("/{bookingId}/cancel")
public Result<Void> cancelBooking(@PathVariable String bookingId) { ... }

// UserController.java
@Audit(action = AuditAction.USER_APPROVE, resourceType = "user",
       resourceId = "#userId")
@PutMapping("/{userId}/status")
public Result<Void> updateUserStatus(@PathVariable String userId,
                                      @RequestBody StatusBody body) { ... }

@Audit(action = AuditAction.USER_FREEZE, resourceType = "user",
       resourceId = "#userId")
@PutMapping("/{userId}/freeze")
public Result<Void> freezeUser(@PathVariable String userId) { ... }

@Audit(action = AuditAction.USER_RESET_PASSWORD, resourceType = "user",
       resourceId = "#userId")
@PutMapping("/{userId}/reset-password")
public Result<Void> resetPassword(@PathVariable String userId) { ... }
```

### 2.7 LogController 浏览接口

```java
package com.reservation.controller;

import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import com.baomidou.mybatisplus.extension.plugins.pagination.Page;
import com.reservation.common.PageResult;
import com.reservation.common.Result;
import com.reservation.entity.AuditLog;
import com.reservation.mapper.AuditLogMapper;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("/api/audit-logs")
public class AuditLogController {

    private final AuditLogMapper auditLogMapper;

    public AuditLogController(AuditLogMapper auditLogMapper) {
        this.auditLogMapper = auditLogMapper;
    }

    /** 分页查询审计日志 */
    @GetMapping
    public Result<PageResult<AuditLog>> list(
            @RequestParam(defaultValue = "1") int page,
            @RequestParam(defaultValue = "20") int size,
            @RequestParam(required = false) String userId,
            @RequestParam(required = false) String action,
            @RequestParam(required = false) String resourceType,
            @RequestParam(required = false) String resultStatus,
            @RequestParam(required = false) String startDate,  // yyyy-MM-dd
            @RequestParam(required = false) String endDate) {

        LambdaQueryWrapper<AuditLog> wrapper = new LambdaQueryWrapper<>();
        if (userId != null && !userId.isEmpty())
            wrapper.eq(AuditLog::getUserId, userId);
        if (action != null && !action.isEmpty())
            wrapper.eq(AuditLog::getAction, action);
        if (resourceType != null && !resourceType.isEmpty())
            wrapper.eq(AuditLog::getResourceType, resourceType);
        if (resultStatus != null && !resultStatus.isEmpty())
            wrapper.eq(AuditLog::getResultStatus, resultStatus);
        if (startDate != null && !startDate.isEmpty())
            wrapper.ge(AuditLog::getCreatedAt, startDate + " 00:00:00");
        if (endDate != null && !endDate.isEmpty())
            wrapper.le(AuditLog::getCreatedAt, endDate + " 23:59:59");
        wrapper.orderByDesc(AuditLog::getCreatedAt);

        Page<AuditLog> pageResult = auditLogMapper.selectPage(
            new Page<>(page, size), wrapper);
        return Result.ok(PageResult.of(pageResult));
    }

    /** 查看单条审计日志详情（含完整请求参数） */
    @GetMapping("/{logId}")
    public Result<AuditLog> detail(@PathVariable String logId) {
        LambdaQueryWrapper<AuditLog> wrapper = new LambdaQueryWrapper<>();
        wrapper.eq(AuditLog::getLogId, logId);
        return Result.ok(auditLogMapper.selectOne(wrapper));
    }

    /** 操作类型枚举列表（供前端下拉框使用） */
    @GetMapping("/actions")
    public Result<List<String>> actions() {
        return Result.ok(java.util.Arrays.stream(
            com.reservation.audit.AuditAction.values())
            .map(Enum::name).toList());
    }
}
```

---

## 三、前端实现

### 3.1 审计日志浏览页 auditLog.html

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>审计日志</title>
    <link rel="stylesheet" href="/css/admin.css">
    <style>
        .audit-toolbar { display:flex; gap:8px; flex-wrap:wrap; margin-bottom:12px; }
        .audit-toolbar select, .audit-toolbar input {
            padding:6px 10px; border:1px solid #ddd; border-radius:4px; font-size:14px;
        }
        .audit-table { width:100%; border-collapse:collapse; font-size:13px; }
        .audit-table th { background:#f5f5f5; padding:8px; text-align:left;
            border-bottom:2px solid #ddd; position:sticky; top:0; }
        .audit-table td { padding:6px 8px; border-bottom:1px solid #eee; }
        .audit-table tr:hover { background:#fafafa; }
        .badge-success { color:#52c41a; }
        .badge-fail { color:#ff4d4f; }
        .badge-ERROR { color:#ff4d4f; }
        .badge-WARN  { color:#faad14; }
        .badge-INFO  { color:#1890ff; }
        .audit-detail { max-height:300px; overflow:auto; background:#1e1e1e;
            color:#d4d4d4; padding:12px; border-radius:6px; font-family:monospace;
            font-size:12px; white-space:pre-wrap; word-break:break-all; }
    </style>
</head>
<body>
    <div class="admin-container">
        <h2>审计日志</h2>

        <!-- 筛选工具栏 -->
        <div class="audit-toolbar">
            <select id="f-action">
                <option value="">全部操作</option>
            </select>
            <select id="f-resourceType">
                <option value="">全部资源</option>
                <option value="user">用户</option>
                <option value="course">课程</option>
                <option value="schedule">排期</option>
                <option value="booking">预约</option>
                <option value="appointment">课时</option>
            </select>
            <select id="f-result">
                <option value="">全部结果</option>
                <option value="success">成功</option>
                <option value="fail">失败</option>
            </select>
            <input type="date" id="f-start">
            <span>~</span>
            <input type="date" id="f-end">
            <input type="text" id="f-userId" placeholder="用户ID" style="width:160px">
            <button class="btn" onclick="loadAuditLogs(1)">查询</button>
            <button class="btn" onclick="resetFilter()">重置</button>
        </div>

        <!-- 日志表格 -->
        <table class="audit-table">
            <thead>
                <tr>
                    <th>时间</th>
                    <th>操作人</th>
                    <th>角色</th>
                    <th>操作</th>
                    <th>资源</th>
                    <th>结果</th>
                    <th>耗时</th>
                    <th>IP</th>
                    <th>详情</th>
                </tr>
            </thead>
            <tbody id="audit-tbody"></tbody>
        </table>

        <!-- 分页 -->
        <div id="audit-pagination" class="pagination"></div>

        <!-- 详情弹窗 -->
        <div id="audit-detail-modal" style="display:none;
            position:fixed;top:0;left:0;width:100%;height:100%;
            background:rgba(0,0,0,0.5);z-index:9999;">
            <div style="background:#fff;margin:5% auto;padding:24px;
                border-radius:8px;width:80%;max-width:800px;">
                <h3>审计日志详情</h3>
                <div id="audit-detail-content"></div>
                <button class="btn" onclick="closeDetail()"
                    style="margin-top:12px;">关闭</button>
            </div>
        </div>
    </div>
    <script src="/js/public/auth.js"></script>
    <script src="/js/auditLog.js"></script>
</body>
</html>
```

### 3.2 审计日志脚本 auditLog.js

```javascript
const PAGE_SIZE = 20;

// 初始化操作类型下拉框
async function initActions() {
    const actions = await request('/api/audit-logs/actions') || [];
    const select = document.getElementById('f-action');
    actions.forEach(a => {
        const opt = document.createElement('option');
        opt.value = a;
        opt.textContent = a;
        select.appendChild(opt);
    });
}

// 加载审计日志列表
async function loadAuditLogs(page) {
    const action     = document.getElementById('f-action').value;
    const resType    = document.getElementById('f-resourceType').value;
    const result     = document.getElementById('f-result').value;
    const start      = document.getElementById('f-start').value;
    const end        = document.getElementById('f-end').value;
    const userId     = document.getElementById('f-userId').value.trim();

    const params = new URLSearchParams();
    params.set('page', page);
    params.set('size', PAGE_SIZE);
    if (action)  params.set('action', action);
    if (resType) params.set('resourceType', resType);
    if (result)  params.set('resultStatus', result);
    if (start)   params.set('startDate', start);
    if (end)     params.set('endDate', end);
    if (userId)  params.set('userId', userId);

    const res = await request(`/api/audit-logs?${params}`);
    const records = res?.records || [];
    const total   = res?.total || 0;

    // 渲染表格
    const tbody = document.getElementById('audit-tbody');
    tbody.innerHTML = records.map(r => `
        <tr>
            <td>${formatTime(r.createdAt)}</td>
            <td>${escapeHtml(r.userName || '-')}</td>
            <td>${escapeHtml(r.userRole || '-')}</td>
            <td><span class="badge-INFO">${escapeHtml(r.action || '')}</span></td>
            <td>${escapeHtml(r.resourceType || '')}:${escapeHtml(r.resourceId || '')}</td>
            <td class="badge-${r.resultStatus === 'success' ? 'success' : 'fail'}">
                ${escapeHtml(r.resultStatus || '')}
            </td>
            <td>${r.costMs != null ? r.costMs + 'ms' : '-'}</td>
            <td>${escapeHtml(r.ip || '-')}</td>
            <td><a href="#" onclick="showDetail('${r.logId}');return false;">查看</a></td>
        </tr>
    `).join('');

    // 渲染分页
    renderPagination(page, Math.ceil(total / PAGE_SIZE), loadAuditLogs);
}

// 查看详情
async function showDetail(logId) {
    const detail = await request(`/api/audit-logs/${logId}`);
    if (!detail) return;
    const html = `
        <div style="margin-bottom:8px;">
            <strong>操作时间：</strong>${formatTime(detail.createdAt)}
        </div>
        <div style="margin-bottom:8px;">
            <strong>操作人：</strong>${escapeHtml(detail.userName || '-')}
            (${escapeHtml(detail.userId || '-')})
        </div>
        <div style="margin-bottom:8px;">
            <strong>角色：</strong>${escapeHtml(detail.userRole || '-')}
        </div>
        <div style="margin-bottom:8px;">
            <strong>操作类型：</strong>${escapeHtml(detail.action || '-')}
        </div>
        <div style="margin-bottom:8px;">
            <strong>资源：</strong>
            ${escapeHtml(detail.resourceType || '')} :
            ${escapeHtml(detail.resourceId || '')}
            ${detail.resourceName ? '(' + escapeHtml(detail.resourceName) + ')' : ''}
        </div>
        <div style="margin-bottom:8px;">
            <strong>方法：</strong>${escapeHtml(detail.method || '-')}
        </div>
        <div style="margin-bottom:8px;">
            <strong>请求：</strong>
            ${escapeHtml(detail.httpMethod || '')}
            ${escapeHtml(detail.requestUrl || '')}
        </div>
        <div style="margin-bottom:8px;">
            <strong>IP：</strong>${escapeHtml(detail.ip || '-')}
        </div>
        <div style="margin-bottom:8px;">
            <strong>User-Agent：</strong>
            ${escapeHtml(detail.userAgent || '-')}
        </div>
        <div style="margin-bottom:8px;">
            <strong>结果：</strong>
            <span class="badge-${detail.resultStatus === 'success' ? 'success' : 'fail'}">
                ${escapeHtml(detail.resultStatus || '-')}
            </span>
            ${detail.errorMsg ? ' - ' + escapeHtml(detail.errorMsg) : ''}
        </div>
        <div style="margin-bottom:8px;">
            <strong>耗时：</strong>${detail.costMs != null ? detail.costMs + 'ms' : '-'}
        </div>
        <div style="margin-bottom:8px;">
            <strong>请求参数：</strong>
        </div>
        <div class="audit-detail">${escapeHtml(detail.requestParams || '(无)')}</div>
    `;
    document.getElementById('audit-detail-content').innerHTML = html;
    document.getElementById('audit-detail-modal').style.display = 'block';
}

function closeDetail() {
    document.getElementById('audit-detail-modal').style.display = 'none';
}

function resetFilter() {
    document.getElementById('f-action').value = '';
    document.getElementById('f-resourceType').value = '';
    document.getElementById('f-result').value = '';
    document.getElementById('f-start').value = '';
    document.getElementById('f-end').value = '';
    document.getElementById('f-userId').value = '';
    loadAuditLogs(1);
}

// 工具函数
function formatTime(t) {
    if (!t) return '-';
    return t.replace('T', ' ').substring(0, 19);
}

function escapeHtml(s) {
    if (!s) return '';
    return s.replace(/&/g, '&amp;').replace(/</g, '&lt;')
            .replace(/>/g, '&gt;').replace(/"/g, '&quot;');
}

// 初始化
initActions();
loadAuditLogs(1);
```

### 3.3 在 admin.html 中添加入口

```html
<!-- 在管理后台菜单中添加 -->
<a href="auditLog.html" class="menu-item">审计日志</a>
```

---

## 四、数据流与处理流程

```
用户操作（如删除课程）
    │
    ▼
Controller 方法（标注了 @Audit）
    │
    ▼
AuditAspect 切面拦截
    ├── 执行前：采集用户ID/角色/IP/UA/请求参数
    ├── 执行业务方法
    ├── 执行后：记录结果（success/fail）、耗时
    └── 异步入库 audit_log 表（不阻塞主流程）
    │
    ▼
管理员打开审计日志页面
    │
    ▼
AuditLogController 分页查询
    ├── 筛选条件：操作类型、资源类型、结果、时间范围、用户ID
    └── 返回分页列表
    │
    ▼
前端表格渲染 + 详情弹窗
```

---

## 五、文件清单

| 层级 | 文件路径 | 说明 |
|------|---------|------|
| DDL | `migration-YYYYMMDD-audit-log.sql` | 建表语句 |
| Entity | `entity/AuditLog.java` | 实体类 |
| Mapper | `mapper/AuditLogMapper.java` | MyBatis-Plus Mapper |
| Mapper XML | `mapper/AuditLogMapper.xml` | insert 语句 |
| 注解 | `audit/AuditAction.java` | 操作类型枚举 |
| 注解 | `audit/Audit.java` | @Audit 注解定义 |
| 切面 | `audit/AuditAspect.java` | AOP 切面（核心逻辑） |
| Controller | `controller/AuditLogController.java` | 浏览/查询接口 |
| 前端页面 | `static/auditLog.html` | 日志浏览页面 |
| 前端脚本 | `static/js/auditLog.js` | 列表+详情+筛选 |
| 入口 | `static/admin.html` | 管理后台添加菜单链接 |
| Controller 注解 | 各 Controller 方法 | 添加 @Audit 注解 |

---

## 六、配置要点

### 6.1 Spring Boot 主类需开启 AOP

```java
@SpringBootApplication
@MapperScan("com.reservation.mapper")
@EnableAspectJAutoProxy  // 已由 spring-boot-starter-aop 自动配置，通常无需显式添加
public class SpringBootMergeApplication { ... }
```

### 6.2 pom.xml 添加依赖

```xml
<!-- AOP 支持（Spring Boot 已内置，通常无需手动添加） -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-aop</artifactId>
</dependency>
```

### 6.3 异步入库优化

生产环境建议用线程池替代 `new Thread()`：

```java
// 在 AuditAspect 中注入线程池
private final ExecutorService auditExecutor =
    Executors.newFixedThreadPool(2, r -> {
        Thread t = new Thread(r, "audit-log-writer");
        t.setDaemon(true);
        return t;
    });

private void saveAsync(AuditLog record) {
    auditExecutor.submit(() -> {
        try {
            auditLogMapper.insert(record);
        } catch (Exception e) {
            log.error("审计日志保存失败: {}", e.getMessage());
        }
    });
}
```

### 6.4 日志浏览接口权限

`/api/audit-logs/**` **不要**加入 JWT 白名单，确保仅登录的管理员可访问。如需更细粒度控制，在 `AuditLogController` 中校验 `role=admin`。
